# Threat Model

Status: initial repository baseline; expand with every release

## Security Objectives

Vitheim must preserve tenant isolation, authorization correctness, event and
evidence integrity, availability under bounded hostile input, confidentiality
of records and secrets, deterministic replay, safe recovery, and explainable
decisions. Secure failure is preferred to an unsupported fallback.

## Trust Boundaries

The complete design anticipates boundaries between:

- unauthenticated clients and public APIs;
- authenticated users and tenant-scoped command/read policy;
- tenants and every shared cache, queue, index, blob, log, and database;
- deterministic domain logic and hosted I/O;
- API processes, workers, ingest, indexers, agents, and operators;
- Vitheim and identity, database, storage, search, mail, webhook, and AI
  providers;
- host and capability-restricted WASM components;
- source repository, CI runners, build tools, release evidence, and artifacts;
- online systems, backups, exports, imports, and disaster recovery.

## Adversaries

- unauthenticated remote attackers;
- malicious or compromised tenant users and administrators;
- a tenant attempting cross-tenant access or exhaustion;
- compromised connectors, agents, plugins, identity providers, or AI models;
- malicious imported records, attachments, framework packs, and workflows;
- supply-chain attackers targeting source, tools, dependencies, CI, tags, or
  artifacts;
- insiders abusing privileged or break-glass access;
- accidental operator errors that produce equivalent security impact.

## Primary Threats And Required Controls

| Threat | Mandatory controls |
| --- | --- |
| Cross-tenant disclosure or mutation | Non-optional tenant types, policy at every boundary, partitioned tests, cache/index/blob isolation |
| Tenant data survives export/hold/erasure/closure | Foundation lifecycle descriptors, complete registry backfill plus prospective generated gate, explicit retention precedence, typed honest evidence, disposition receipts, closure policy/blocker |
| IDOR, authority/target-state TOCTOU, stale/deleted/superseded local target, remote resource ABA/validator downgrade or refresh, unconditional-exception scope reuse/revocation loss/final-attempt duplication, grant-lineage fork, two-stream dispatch, offline-human impersonation, or confused deputy | Typed commands, immutable effect binding, typed live/grant/service authority, one grant-lineage stream and guard, bounded dispatch-authority fences, typed local target fence, provider/account/resource/validator-bound remote profile, strong conditional mutation or one-owner scoped/expiring/attempt-bounded `RemoteMutationException`, co-located exception guard, provider-capability epoch, typed precondition failure, no silent refresh or worker-derived authority |
| Event/evidence tampering | Append-only journal semantics, expected versions, integrity chains, signed checkpoints, restore verification |
| Unauthorized provider-profile or evaluator activation, evaluator downgrade/revocation lag, semantic authority expansion, concurrent/ambiguous rotation, orphan credentials, credential-count exhaustion, circular remediation authority, unsound provider-policy comparison, unsafe quarantine clearing, out-of-band permission drift, partial-operation use of an over-privileged credential, replay, duplicated effects, stale/revoked profiles or credentials, bearer-token export, audience-sharing workers claiming one transmission, executor bypass through reusable credentials or unrestricted egress, permit leakage across service boundaries, indefinitely delayed admitted dispatch, or ambiguous provider outcomes | Single-aggregate local commits, stable effect ID/digest, immutable redemption/transmission deadline, current-fence transmission-start CAS, globally unique claim bound to exact worker instance and lease generation/fence, audience/provider/request/effect/permit-digest binding, trusted executor owning claim plus socket, immutable instruction-only split-service protocol, sealed consumed-by-value non-serializable permit, one-time process-local return, authoritative profile lineage plus monotonic profile/account/credential/broker epochs, typed control-plane profile governance with signed digest-bound admission, semantic expansion review, risk owner/quorum/separation, activation fences and revocation tombstone, one credential-lineage owner and co-located single-rotation guard, stable rotation/generation/idempotency/digest binding, authorized provider-inventory takeover, quarantined quota-counted orphan workflow and late-callback fencing, asynchronous evidence-driven rotation with atomic local activation and bounded unknown-state reconciliation, fresh credential-capability snapshots and local epochs, reviewed semantic provider-policy evaluators returning equal/subset/superset/incomparable/unknown with raw/AST/language/explanation evidence, signed binary/corpus-admitted evaluator lineage and epoch with emergency revocation/reevaluation/incompatible-node rejection, explicit safe-subset profile, whole-credential quarantine and incident, owned evidence-backed investigation/remediation/verification/resolution with separated resolver/consistency barrier/new capability generation/no old-work revival, independent cleanup-only credential-remediation authority or manual intervention, no automatic widening or break-glass promotion, revocation/redemption/first-credential-use linearization, exact-claim-bound scoped secret operations, non-exportable signing/mTLS/HSM profiles, bearer HTTP/TLS/socket wholly inside the hardened broker/executor TCB, memory/diagnostic canaries, no master-key/general DB authority, least-privilege credentials, destination/TLS/DNS/redirect enforcement, partitioned executor trust domains, explicit residual provider blast radius, restore anti-resurrection, status-only replay, monotonic start enforcement, ambiguous-claim reconciliation, distinct execution/outcome/resolution/manual/compensation types, provider-evidence strength, no transferable `1.0.0` permit or uncertain-start/blind privileged/non-compensable retry |
| Fleet-wide evaluator invalidation misses credentials between epoch change and job creation, loses or cannot discover a source-side intent, retires/splits/moves a source behind the cutoff, trusts an incomplete projection, loses a cross-shard move or late mutation, completes without every shard, causes a re-evaluation storm, provider-rate collapse, hostile-tenant starvation, stale-evidence reuse, lost jobs, or old-output use while queued | Epoch and campaign root commit atomically; canonical source-partition/topology manifest; monotonic outbox sequence/high-watermark; exact destination receipt; `MembershipDeliveryBlocked`; authoritative membership journals; sealed shard generations/high-watermarks; fenced moves; one scan receipt per manifest shard; delivery and membership barriers; independent capability-owner mismatch reconciliation; search/projection non-authority; generation-bound materialization; successor tombstones; durable queues; bounded concurrency/rates/fairness/cleanup lane; `ReevaluationRequired` remains an operation fence |
| Remediation authority is self-created, self-recovered, circular, lost with business credentials, restored stale, or unavailable when provider administrators/KMS fail | Signed first-admission and recovery ceremony; separated requestor/approver/executor/risk owner with quorum; two independently administered channels for automatic recovery; independent KMS/secret/identity domains where practical; offline/manual path independent of recovered authority; no self-approval/remediation; monotonic compromise/loss/recovery epoch; expiry/availability monitoring; periodic recovery exercises; explicit manual-only provider profile |
| An invariant is omitted from a hand-written registry, has no owner or competing owners, uses prose-only enforcement/tests/capabilities, transfers ownership asymmetrically, admits unsafe mixed versions/rollback, or restore/migration omits monotonic state | Declaration-derived bidirectional invariant coverage; exactly one ownership and lifecycle row; stable enforcement/capability/test/recovery/fence IDs; owner-updated guard and transaction placement; concrete negative/P/N/M/F cases; semantic storage negotiation; symmetric supersession; mixed-version rule and rollback floor; complete generated recovery manifest |
| A composite law activates a future tuple, accepts attacker-authored trust, merges owners, executes topology initialization before generation 2 is admitted, admits dual topology authority, replays a signed-old topology receipt, rolls back observation sequence, lets a stale placement operate after rollout/fence loss, lets the topology owner mint its own permission, treats attested identity as topology permission, pretends external epoch checks are atomic with topology CAS, mixes hardware/action-claim fields, loses/reissues authorization or workload claims, extends a topology grant through clock rollback/NTP step/suspend/snapshot/failover/skew or an expiry-versus-CAS race, leaves activation receipts forgeable, repeats an expired/revoked consumed action, clones identity, uses offline authority, accepts delayed authorization after revocation, runs competing rollouts without one pinned winner, mutates membership, admits bad receipts, loses revocation, trusts mutable storage/time, extends expiry, omits ancestry, or starts unresolved | Strict canonical catalog validation; separate owners; generation-1 epoch-13 convergence/local admission before handoff; exclusive topology source; VIT-LAW-007@g02 nonce/sequence/generation/expiry/uncertainty topology receipts and monotonic local observation ratchets at admission/readiness/dispatch/start; independent VIT-INV-061 authorization lineage with issuance-time current-epoch/quorum checks, unique mutation/manifest, immutable class-bounded `commit_before`, authenticated trusted-time profile/epoch/uncertainty/issuer-continuity evidence, fail-closed issuance, idempotent unknown-response recovery, future-issuance revoke/supersede, independent break-glass recovery, restore high-watermarks, and issuer/topology credential separation; VIT-INV-060 locally ratchets conservative trusted lower-bound/profile/continuity/expiry state and consumes authenticated profile-discriminated receipt/applicable workload proof/CAS/tombstones/fence outbox only when commit-before-expiry is proven, without distributed atomicity; closed claim/freshness/owner-protocol scope; authenticated activation receipts with MAC-role separation; claim tombstone-first historical replay/no new authority; immutable manifests; transactional messaging; fully fenced activation; canonical verification/time/checkpoint/restore; exhaustive P/N/M/F; future/drift/rollback rejection |
| Topology authorization passes a client-side expiry check but the process pauses, the database commits after `commit_before`, a timeout/cancel/connection loss is mistaken for absence, backend failover resurrects a pending transaction, schema migration omits time state, or a receipt authentication preimage omits a field | Canonical `TopologyMutationAuthorizationReceiptV1`; mandatory `DeadlineConditionalTopologyCasV1` with backend-authoritative commit-time predicate or hard no-late-commit fence; atomic consumer time-ratchet/receipt/claim/topology/member-fence/tombstone/outbox commit; typed committed-before-deadline/definitely-not-committed/reconciling outcomes; pause and failover conformance; capability refusal when unprovable; non-lossy migrations, downgrade rejection, greatest-value restore, and destination-mechanism import admission |
| Authorization issuance abuse or years of consumed/expired receipts exhaust storage; compaction races replay, deletes before checkpoint, forks or rolls back a checkpoint, loses an archive/key, or makes an old request look unused | Separate bounded authenticated-presentation, first-seen-request, successful-admission and outstanding limits at deployment, issuer/class and canonical principal/authority scope; monotonic request/issuance sequences; frozen exact replay horizon; authenticated predecessor-linked set/archive checkpoint committed before deletion; covered-through high-watermark and key ratchets; typed unavailable-history denial; fail-closed issuance on ambiguous proof or backlog saturation; migration/HA/DR/key-rotation/concurrent-replay tests; bounded storage accounting and alerts |
| Unique authenticated denied request IDs exhaust hot state, denial compaction deletes permanence, or an old denial is replayed after policy/approval change and treated as a new successful request | Monotonic request sequence and one request-rate charge for every first-seen canonical ID/digest; exact retries pay presentation rate again but reuse sequence/request charge/outcome; authenticated predecessor-linked denial checkpoint binds request/caller/class/request charge/outcome and success link before deletion; bounded exact horizon and archive proof; late compacted request remains historical; missing proof returns typed historical-state-unavailable; explicit row/byte/backlog/archive/decode/work/job/latency bounds; migration/failover/restore/backend conversion preserve sequence, checkpoint, high-watermark and key state |
| Replay storms bypass cost controls, concurrent identical requests receive multiple logical charges/outcomes, response-loss retry allocates twice, retry under saturation changes history, or changed-digest ID reuse escapes accounting | `TopologyAuthorizationPresentationRateBudgetV1` charges before protected idempotency lookup on every authenticated canonical presentation; `TopologyAuthorizationRequestRateBudgetV1` charges once per first-seen ID/digest and binds to the request sequence; presentation saturation returns a transient rate-limit result without reading/replacing immutable outcomes; canonical-key serialization produces one request charge/sequence/outcome; changed digest pays presentation rate then conflicts; replay/concurrency/response-loss/saturation/conflict tests run through adapters, HA, DR and soak |
| Pre-authentication floods exhaust bytes, connections, file descriptors, TLS workers, signature/MAC verification, canonical decode, executor queues or pools; normal saturation blocks emergency work; emergency floods starve recovery; or a listener route is mistaken for authorization | Independently provisioned, non-borrowable `Normal`/`Recovery`/`BreakGlass` accept queues, sockets/file descriptors, TLS workers, decode CPU/memory, executor queues and pools operate under one fail-safe global ceiling; `TopologyAuthorizationIngressWorkBudgetV1` caps each lane before durable state; only server-controlled listener/TLS configuration selects `TopologyAuthorizationIngressLaneV1`, and that route grants no authority; authenticated endpoint/audience/profile still derives the presentation lane and full authorization must exactly match it |
| A presentation debit is rolled back with a failed lookup, a crash between debit and request allocation refunds or reuses work, charge evidence is forged/exported/replayed, or credential rotation/revocation changes the lane mapping between authentication and commit | VIT-INV-061 solely owns the versioned presentation-lane mapping lineage; emergency promotion requires distinct requestor/approver/activator roles under current policy and change/incident authority. Stage one `ChargeTopologyAuthorizationPresentation` commits the debit before lookup and emits bounded internal single-use evidence bound to request/caller, both lanes, mapping identity/generation/fence/profile digest, budget epoch/sequence and continuity. Stage two `ConsumeTopologyAuthorizationPresentationCharge` consumes it, locks and rechecks the current mapping, and then commits request/outcome/issuance. Mapping drift returns `TopologyAuthorizationPresentationLaneChanged`; crash leaves an orphaned spent charge, retry obtains a new charge, fenced continuity invalidates old evidence, and checkpoint-before-delete preserves dispositions without turning evidence into authority |
| Charge-ledger exhaustion commits a debit without evidence, an unknown disposition creates an escape state, timeout changes an awaiting charge to refunded/abandoned, a terminal kind is rewritten, or compaction erases the result link | Stage one atomically commits debit, complete evidence, sequence and `ChargedAwaitingStageTwo`, or returns `TopologyAuthorizationPresentationChargeLedgerSaturated` before lookup with no write. The only successors are irreversible `Consumed`, `MappingChanged`, `ControlledAbortAbandoned`, or `ContinuityFencedOrphaned`; timeout is not a transition. `CheckpointedCompacted` follows only a committed predecessor-linked checkpoint and preserves the original terminal kind, debit and result/evidence commitment |
| A capacity shrink cancels an awaiting/terminalization/checkpoint obligation, Recovery capacity is reassigned, an aggregate increase exceeds physical resources, an old writer mutates the prior profile, or restore revives an older larger ceiling | VIT-INV-061-owned immutable capacity-profile lineage with stable ID, monotonic generation/epoch, digest, predecessor, closed state and expected-version CAS; separated authorization for emergency reductions/aggregate changes; successor stays pending or rejects until all lane usage/reservations/backlog/maintenance/protected reserves fit; no cross-lane transfer; authenticated adapter disk/I/O/worker provisioning evidence; downgrade rejection; restore selects the greatest authenticated committed activation record, reconstructs obligations, and never infers activation from raw generation or merges maximum numeric ceilings |
| Continuous admissions prevent a pending shrink from draining, a final predecessor-only admission races activation, rejection clears a fence while admission proceeds, a stale worker bypasses the drain, or failover/restore loses or duplicates it | Atomic lane-scoped `TopologyAuthorizationPresentationChargeLedgerCapacityDrainFenceV1`; one nonterminal successor/fence per predecessor; stage one locks and fits active plus pending profiles or returns typed pre-debit draining denial; existing obligations retain completion capacity; activation/rejection lock and consume the exact fence with expected-version CAS; authenticated fence recovery; competing/stale/missing/worker-cleared fence denial; Normal/BreakGlass fence cannot block Recovery |
| An initially safe or aggregate-only reduction directly activates and races a final admission, incomplete affected-lane coverage bypasses a reduced shared resource, recovery mistakes a higher proposed/rejected generation for active, or a helper invokes fence install/clear outside the profile transition | Every canonical reduction must traverse PendingDrain; overflow-safe typed full-profile comparison treats unknown/omitted/incomparable/mixed-schema fields as reductions; aggregate diffs canonically fence every capable lane; admission/activation lock the same lane/aggregate rows; recovery selects the greatest authenticated committed activation record and separately restores pending/fence state plus lineage/activation high-watermarks; multiple-active, contradictory, unreachable or half-state recovery denies; fence-installed/consumed are atomic transition events and direct helpers are unrepresentable |
| An actor starts an unauthorized Normal/emergency/aggregate drain, reuses or substitutes approval, treats an exact response-loss retry as an attack or a changed retry as idempotent, revives expired/revoked authority through clock/restore/key rollback, cycles install/rejection to deny service, or corrupts/deletes/reorders/forks an activation record/checkpoint so recovery chooses false active state | Exact single-action `CapacityDrainAuthorizationV1` binds scope/action/diff/coverage/policy/SoD plus not-before/issued/expiry, uncertainty, trusted-time profile/epoch, issuer continuity, signer/key epoch/profile, nonce and idempotency. Its closed Issued/Consumed/ExpiredUnused/RevokedUnused lifecycle atomically commits consumption/tombstone/canonical action result/mutation/event/audit/outbox; identical response-loss retry returns the result without side effects, changed bytes/digest/action conflicts, and consumed/expired/revoked authority is never reusable. Begin-drain validity is historical after consumption; activation needs fresh current authority and revocation leaves the fence safe. Monotonic time/key/consumption/result ratchets survive failover/restore/migration. Canonical non-wrapping predecessor-linked activation record commits atomically; its authenticated checkpoint covers activation and authorization replay/time/key state before deletion; gaps/forks/reorder/duplicates/wrap/active-row disagreement/missing checkpoint/high-watermark rollback deny |
| Hot drain replay state is compacted, a digest cannot reproduce the original action result, a dense watermark misclassifies a sparse arbitrary ID as unseen, changed late retry escapes conflict, or archive/key/chunk/proof loss permits re-execution | Select bounded authenticated sparse `CapacityDrainAuthorizationReplayCheckpointV1` plus archive, never permanent unbounded retention. Entries bind action/idempotency ID, canonical request/authorization digests, terminal lifecycle, complete result or authenticated reference, action/profile/scope/policy, predecessor checkpoint, encoding/key epoch and archive commitment. Exact membership returns result/conflict; exact-set non-membership is never dense inference. Bounded bytes/entries/chunks/depth/decode/work/jobs, durable cursor, verified immutable upload followed by atomic local head/cursor/exact-delete, and reserved Recovery capacity apply backpressure. Missing/unverifiable history returns typed `CapacityDrainHistoricalStateUnavailable` without execution |
| A reader proves non-membership against an old individual archive, concurrent consumption falls between snapshot and delete, object-store publication is mistaken for a database transaction, staged/orphan chunks become authority, coalescing forks the head, or cleanup/rollback removes committed history | One cumulative `CapacityDrainAuthorizationReplayHeadV1` per tenant/deployment binds non-wrapping sequence, predecessor/root/scope, CAS, key/publication and covered-row version. Proofs use the greatest committed head plus current hot rows. Immutable chunks move Staged→Verified before one local CAS transaction co-commits head and exact covered-row deletion; readers trust only committed head. Unknown external outcomes retain hot rows, unknown local commits reconcile the bundle, orphan GC requires no reference or authenticated successor equivalence, and no distributed transaction is assumed. Race consumption/compaction, stale readers, publishers, coalescing, visibility, GC and rollback |
| A proof for head `H` is paired with hot-row absence created by `H+1`, a reader ignores a changed head, an async replica/follower/cache exposes mismatched head and hot state, or concurrent first executions bypass replay uniqueness | `CapacityDrainReplayAdmissionGuardV1` verifies the bounded proof for writer-authoritative `H` outside the transaction, then one local write transaction locks head followed by exact key, requires the head still equals `H`, checks hot state and uniquely inserts the replay claim with the atomic result bundle. Head change returns typed no-write restart; unique conflict resolves retry/conflict, never execution. Compaction uses the same lock order and unsupported isolation refuses `VIT-CAP-061`. Fault schedules pause after proof, after head lock and before unique insert |
| Continuous publication advances the head until per-job budgets reset forever, failover/reconnect evades accounting, ambiguous adapter keys permit one-sided action/idempotency reuse, or Normal/unauthenticated churn starves Recovery or compaction | Canonical `CapacityDrainReplayKeyV1` binds tenant/deployment/kind/action/idempotency with action and idempotency IDs independently unique. Durable `CapacityDrainReplayRestartBudgetV1` monotonically bounds automatic restarts, cumulative bytes/decode/work/time and observed advances across crash/failover/cursor/adapter retry. Exhaustion returns typed no-write `CapacityDrainReplayAdmissionContended`, distinct from unavailable history. Finite authenticated-admission/compaction quanta, bounded caller-independent yield and protected Recovery capacity prevent starvation and compaction pinning |
| Authenticated callers allocate duplicate or abandoned attempt budgets, a crashed/stale owner continues after takeover, cancellation resets work, a no-write terminal later succeeds, cleanup removes replay evidence, or Normal attempt floods exhaust rows/terminalization and block Recovery | Closed `CapacityDrainReplayAdmissionAttemptV1` permits one Active/RestartPending attempt per key; identical requests join and changed material conflicts. Workload/boot/lease/fence/CAS ownership makes takeover exclusive while preserving counters/deadline. Terminals are irreversible and success co-commits with replay/action state. `AdmissionAttemptCapacityV1` bounds rows/bytes/queues/principals/takeovers/terminalization/cleanup by lane/deployment, reserves completion before admission, protects Recovery, and permits cleanup only after authenticated result/audit/checkpoint linkage |
| Capacity reservation and attempt creation split, joins allocate duplicate completion capacity, stale/expired execution races terminalization, checkpoint or deletion releases the wrong leg twice, a new profile recomputes old reservations, or an adapter journals deletion but not checkpoint settlement | `AdmissionAttemptCapacityLedgerV1` atomically locks replay head→optional settlement journal head→key/attempt→capacity→domain, reserves the complete original set and creates one attempt; joins allocate nothing. Success rechecks and transfers atomically. Stable per-reservation-set/leg IDs make checkpoint and deletion release distinct original buckets exactly once. One unified settlement protocol makes checkpoint decrements/per-leg rows/journal append/attempt checkpoint/audit/result indivisible and applies the same contract to deletion legs; adapters cannot choose another checkpoint replay path |
| Settlement hot rows are kept forever, compacted evidence becomes unverifiable, a late checkpoint/deletion duplicate decrements twice, trigger or leg bytes alias, one head falsely means both local append and verified publication, missing history is treated as released capacity, recovery omits/defaults one head/linkage/trigger/leg set/balance, or an attacker labels a singular head local/archive/both/empty to invent history | Domain-separated settlement checkpoint/archive reuses authenticated sparse publication with separate `CapacitySettlementJournalHeadV1` and `CapacitySettlementArchiveReplayHeadV1`. Every checkpoint/deletion settlement atomically advances the local journal with its original-bucket decrements, ordered per-leg rows, audit/result and attempt checkpoint or envelope deletion. After upload/verification, publication locks archive head→journal head→exact covered rows, rechecks version/continuity, and atomically CAS-installs archive head plus deletes only those rows; mixed-trigger archives retain exact identity. Lookup revalidates H and combines archive head, hot version and journal continuity; absent-envelope non-membership cannot decrement. Recovery requires both non-wrapping chains/coverage, exact rows, settlement/trigger/bundle/result/linkage, settled/remaining legs and conservative balances. No Vitheim release admitted a singular schema: preflight quarantines it before mutation, never fabricates chains/coverage or genesis, retains charges and leaves the target unready. Exact duplicate returns the result, changed trigger/leg conflicts, unavailable proof retains charge, tombstones survive coalescing, and between-stage restore cannot reopen or pre-settle legs |
| Millions of individually valid migration/import records, cryptographic-proof floods, temporary staging, cursor/retry churn, response loss, many tenant jobs, or hostile quarantine payloads bypass per-decode limits and starve Recovery | `MigrationImportWorkBudgetV1` binds job/material, tenant/deployment, source/destination, schema/manifest, principal, immutable budget profile and fenced owner. Durable overflow-checked operation plus principal/tenant/deployment counters cover bytes, objects/chunks, allocation/hash/signature/proof work, temporary/staged state, files/streams, resumes/retries/time, cleanup and concurrency across crash/failover. Creation reserves staging/verification/result/cleanup and non-borrowable Recovery capacity; each bounded quantum precharges before work. Duplicate material resumes one lineage and changed reuse conflicts. `MigrationImportBudgetExceeded` fences promotion, preserves source, enters bounded cleanup, and quarantine retains bounded metadata/digests only |
| Import budget exhaustion, cancellation, takeover, partial preparation, cleanup, response loss, failover or restore races activation; a forged pseudo invariant catalog, stale/future VIT-LAW-009 generation or dependency injection/omission changes owners; registry forges a receipt; revocation arrives before authorization admission or expires in transit; an unrelated sequence suppresses a target revocation; VIT-INV-062 imports/replaces its live coordinator state or collides/cycles with a source job; authorization is reused, expired, key/sequence-rolled-back or continuity-substituted; one owner accepts while another rejects; or prepared state becomes readable | `VIT-INV-062` owns only destination operation/job/budget/lifecycle/fence/candidate/authorization/revocation-inbox/barrier/result/cleanup control state. Trusted code derives `MigrationImportOwnerManifestV1` only from source/destination schemas, migration plan, contributor algorithm and the authenticated dependency set of the currently admitted destination VIT-LAW-009 manifest; it types VIT-INV-062 as non-importable coordinator and all other applicable entries as domain contributors. Owners authenticate dormant receipts through owner-held capabilities. Explicit authorization admission shares one row with activation/expiry/revocation. Authenticated revocation has a per-authorization sequence and target lifetime, creates RevokedBeforeAdmission when absent or RevokedUnused when Issued, and late admission joins the permanent tombstone. Nonterminal source jobs and cycles deny; terminal history is post-activation inert archive only; identity collisions conflict; coordinator-schema succession uses a separate local predecessor-owned bootstrap. `VIT-LAW-009` atomically activates every selected domain owner with the live coordinator barrier/job result/audit/outbox, or none |
| A predecessor-started VIT-INV-062 mutation commits after coordinator handoff; an old binary omits the guard; a coordinator self-authorizes, replays, cancels or rolls back bootstrap; successors compete; bootstrap revocation is only a remote message; one action sequence suppresses another; or history append collision/failure rewrites or obscures activation | Every mutation locks/rechecks the deployment-retirement fence first and the non-wrapping active coordinator generation next. Stable-ID bootstrap has a closed lifecycle, independent exact-predecessor/successor authorization and SoD, bounded work/reservations/cleanup, atomic generation/fence handoff and exact result/conflict. One action-discriminated revocation protocol binds exact grant/action, issuer continuity, target-covering lifetime and target-scoped sequence; effect is only the destination-local inbox/tombstone/bootstrap-result transaction. History append is authenticated, sequenced, idempotent, bounded and post-activation only with a separate durable disposition. Fault tests cross handoff, response loss, restore and failover; guard-unaware binaries deny readiness and archive outcome never changes activation |
| NotRequested bypasses custody; disposition is substituted; manifest operational-active/transition-candidate heads are deployment-defined, forked, selected by raw generation or rolled back with a locally self-consistent old database; profile distrust and activation race without one durable winner; independent signatures impersonate CAS, status is non-linearizable, request identity or final result is rebound, compaction drops reconciliation membership, root retirement is omitted, replacement authority is inherited, or a stale/shared profile slot admits unbounded attempts; backend rational storage cost rounds down, overflows or copies source counters; child release and parent credit split, leak or duplicate, timeout mints another transfer or recovery guesses one side; an external checkpoint/witness/key is unavailable or substituted; absent fence/registry genesis implies healthy; ordinary saturation blocks fencing; repeated clear→re-fence cycles allocate fresh scopes and reset work/reserve capacity; registry advancement strands an Open scope; expected anchor sources are weakened by an unadmitted or remotely revoked grant whose revocation preimage omits identity/time/key/reason/idempotency fields; competing grants reset lifetime proof work; field-wise joins invent state; invalid rebuild proposals consume the parent or an unadmitted candidate failure becomes permanent rejection; corruption is mislabeled conflict | An independently authorized destination-local manifest bootstrap stages every transition Prepared→ProposalPublishedVerified→LocalActivationCommitted→Active with Aborted/PermanentlyUnresolved terminals; a non-recursively anchored publication-profile lineage binds exact witness sources, quorum, identity/generation/digest, compatibility and a distrust ratchet, while one concrete linearizable governance-root activation-fence port serializes profile transition/distrust and activation with immutable request/final-result binding, linearizable status, authenticated compaction membership, consensus assumptions and anti-rollback finality; each tenant/deployment profile has one exact attempt slot, and root loss/full compromise creates immutable retirement while a separately authenticated replacement inherits no authority/sequencing state; every attempt reserves terminalization capacity before publication; proposal and active high-watermarks are separate and local CAS is followed by an externally witnessed activation receipt only after the current profile/ratchet recheck. Restore/import derives latest only from the externally Activated journal lifecycle; local-below-active-watermark or unavailable selected-Active profile/receipt continuity is typed rollback/unavailable and unready, while the operational-active head remains distinct from a reachable unresolved transition-candidate descendant. Activation binds that witnessed head, creates Healthy fence/registry generation zero, reserves non-borrowable control-state capacity and creates one obligation-wide control lineage. Every fence episode uses a lifetime-work ledger with nondecreasing WorkSpent and a separate physical ledger for ReservedUnoccupied/Occupied/ReclaimPending/Released rows, checked rational backend-costed bytes-stored with golden vectors, audit and outbox under kind-specific transfer IDs, destination storage-cost profiles and separate child conservation equations; one co-located Recovery parent ledger atomically pairs child allocation/release with parent debit/credit and its own equation, one logical operation has one deterministic transfer identity, and restore verifies both sides without guessed compensation; clearance never resets or double-charges them, insufficient future capacity permanently quarantines, and custody-safe release follows one typed obligation→fence→lineage→checkpoint order. Ordinary control reserve reaches Released only after authenticated journal, committed archive, exact deletion and atomic parent credit; a workspace leg additionally permits verified transfer only when the identical archive/legal-hold custody member is atomically charged before source credit. Registry advancement atomically rebinds Open scope state and stale-terminalizes a live grant without losing charges; terminal scope bytes do not move. Weakening and permanent rejection use complete admission/pre-admission revocation/Issued revocation/expiry/consumption protocols whose revocation preimages bind issuer/continuity, signer/key/epoch/profile, issued/not-before/target-covering expiry, time uncertainty/profile/epoch, reason, nonce and distinct revocation idempotency. One fence-wide scope preserves lifetime charges/tombstones; typed algebra separates G-counters, exact ceilings, derived capacity, causal heads and consistent snapshots; one rebuild parent permits bounded attempts and one successor. Fenced remains an outcome and every prior custody/authority control remains |
| Lost publication root cannot sign retirement; abort races activation before local CAS; a signed cost successor silently weakens charging; or parent restore performs an unbounded scan/resettable verification | A separately rooted retirement authority alone commits Pending→Retired/EvidenceUnavailable; empty new-identity bootstrap remains available while unavailable old custody stays permanently quarantined. Activate/Abort/Seal/Rotate/Distrust/Retire share one linearizable governance sequence and per-transition terminal map, so journal projections cannot contain dual terminal meanings. Cost profiles have independently rooted lineage/head/high-watermark, complete-domain exact weakening classification, destructive authorization and complete atomic re-cost checkpoint before backend/schema activation. Parent transfers atomically maintain aggregates, exact membership commitments and predecessor checkpoints; restore gates each partition while protected monotonic cursor-budget streaming reaches Ready or Fenced without free rescan or unbounded startup work |
| A compromised retirement signer retires a live deployment without an admitted one-shot grant; cached work bypasses a local retirement fence; a large re-cost campaign partially charges children and cannot activate or abort safely; a crafted profile explodes classifier work; or a valid maximum parent snapshot always exhausts restore verification | Retirement uses an independently issued, destination-admitted Absent/RevokedBeforeAdmission/Issued/Consumed/ExpiredUnused/RevokedUnused grant whose exact target/time/quorum/SoD/nonce material is consumed atomically with Pending, a genesis-created Operational→RetirementPending fence and a complete non-borrowable terminalization reserve; every authority and send/commit boundary rechecks the first-rank fence, while grant consumption, Pending, fence advance, completion reserve and audit/outbox commit atomically or not at all. Re-costing fixes a pre-cut snapshot, logs live allocation/release under a parent campaign epoch, reserves the full delta in RecostPending and stores separate pending successor charges; explicit owner-authorized abort reverses every pending amount with stable inverse transfers. The classifier admits only bounded rounded-affine inputs and closed-form symbolic proofs; invalid or over-budget classification is InvalidOrUnverifiable terminal no-write and small-domain exhaustive vectors test equivalence. Checkpoint creation derives and reserves complete verification capacity before the head becomes restorable, so bounded restore quanta can finish every admitted maximum snapshot |
| RecostPending or workspace disappears from parent conservation; workspace total is rewritten after settlement; Released rolls back; a leg settles fractionally/twice; two successors overlap; evidence loss mints a refund; finalization scans an attacker-sized log; or activation splits selector/accounting state | The parent equation includes active child + campaign RecostPending + pending successor + workspace. Workspace OriginalTotal equals the initial parent transfer and is immutable; Released is monotonic; remaining parent member derives as OriginalTotal−Released. Each complete leg atomically leaves its exact live bucket, enters Released, decrements the workspace member, credits ParentAvailable and appends inverse/settlement/checkpoint/result evidence. Parent restore verifies all equations and tombstones. Existing unique slot, bounded recovery/fold, same-cut activation and physical peak reservation controls remain |
| A preflight release credits a nonexistent bucket; recovery is unbounded; Closed reopens; workspace capacity releases without deletion; unfair locks let new foreground work starve old cleanup; successful activations grow terminal backlog without bound; two checkpoints describe different cuts; remote revocation is treated as local; or revocation of Consumed/Expired loops on conflict | Existing release/product/cut controls remain. After slot clearing, cleanup uses the current slot only for rank serialization and the old Closed fence. A co-located protected cleanup lane and non-resettable contention budget force a cleanup quantum after bounded foreground grants; a finite release burst cannot reset it. Soft backpressure and hard terminal workspace count/row/byte/encumbrance/work/backlog maxima prevent accumulation. Permanent-quarantine revocation remains issuer-intent/destination-apply; one total first-terminal-wins outcome table makes CAS losers and later valid operations return stored quarantine, expiry or revocation results, while changed target/digest/scope/sequence/idempotency alone conflicts |
| Broader custody release credits a quarantined workspace only in the parent ledger, partially settles its legs, rewrites Released, or restores parent-released/workspace-encumbered divergence | One precharged whole-member custody-release transaction verifies retention/legal-hold/custody/no-future-dependency evidence, settles every remaining complete leg, advances Released exactly to OriginalTotal, removes and identically credits the workspace parent member, records domain-separated tombstones/checkpoint/result/audit/outbox and moves the workspace to distinct CustodyReleased. The generic lineage release cannot credit the member separately; crash or mismatch retains the fully encumbered predecessor |
| A publisher advances the custody archive replay head before covered hot rows are deleted, a reader trusts a staged receipt, or a crash exposes head-without-delete/deletion-without-head | Publisher/storage adapters create only non-authoritative receipt evidence through typed Stage/Verify commands; readers ignore it. Commit alone locks predecessor head→receipt state and atomically moves Verified→ConsumedByCommit while installing the proposed head, deleting exact rows and committing authorization/settlements/results. Response loss reconciles that indivisible transaction; failed commits retain prior head/rows. MarkOrphan races Commit by the same lock/CAS, FinalizeGc accepts only OrphanGcEligible with authenticated deletion, and ConsumedByCommit/Collected never convert |
| Custody release credits workspace shadow/index/WAL/cleanup capacity because bytes are logically unused although they remain physically present, or transfer shifts bytes into an uncharged archive/legal-hold store | Every complete remaining leg has exactly one closed physical disposition. AuthenticatedDeleted binds storage generation/root and a verified idempotent terminal deletion receipt. TransferredToCustodyLedger requires an exact Begin-created TransferPending reservation and Commit conversion into the conservatively calculated custody member before exact source-parent credit. Unknown or any partial/stale/mismatched receipt preserves PermanentlyRetained or Quarantined and the pending charge; restore proves both ledgers, reservations and tombstones |
| An external custody transfer creates destination bytes before capacity is reserved, Commit expires after transfer, or timeout/cancellation refunds an uncertain transfer | Begin atomically moves the governed conservative maximum Available→TransferPending and the adapter accepts only that reservation. Commit converts it to the final member. Unknown or destination-present outcomes retain the full pending charge across authority expiry/failover; typed reconciliation releases only with backend-authenticated definitely-never-transferred or exact destination-deleted evidence, using precharged orphan/reconciliation capacity |
| Source shadow/index/WAL/worker units are copied as numerically identical compressed/encrypted/replicated archive charges, or crafted factors round down/overflow | Immutable versioned CustodyCapacityCostProfile binds both storage generations, complete unit mapping, compression/encryption/replication/index/object overhead, verified encoded size, checked rational ceiling/overflow and backend limits. Begin reserves a conservative maximum and Commit recomputes a final destination charge no greater than it. Exact source credit and destination charge share a transfer ID but may differ; unknown/incomparable/zero-rounded/overflowed mappings deny before external work |
| A definitely-never-transferred or destination-deleted reconciliation releases the old reservation but strands ReleasePending; retrying Begin recreates capacity, or a replacement plan reuses old grants, receipts or reservation identities | Begin creates monotonic custody-release plan generation 1 and a PreparingOpen attempt. Independently authorized Replan alone advances the head after every old external outcome is terminal and no Commit won; one transaction fences/supersedes the old attempt/grants/receipts and creates a new bundle/PreparingOpen attempt/reservation set under unique generation-bound identities. Independently authorized Abandon only fences an attempt and enables later Replan; it never refunds or reverses Begin. Full Abort is unsupported because committed local releases and admitted external effects are not exactly reversible |
| Current cost-profile drift strands a valid transferred object; a weakening successor deletes its evaluator while reservations depend on it; or streaming/finalization exceeds the admitted custody charge | Custody reservations reuse the governed backend-storage cost-profile lineage and pin exact Active generation/digest/evaluator dependencies. Superseded evaluators remain until dependencies terminalize; compatible nonweakening successors serve new reservations while old work drains, whereas incompatible/weakening activation is fenced and weakening consumes destructive authority. Before each bounded chunk the adapter proves running charge within the reservation; extra bytes require atomic bounded extension first, and failure aborts before overflow/finalization while retaining the original pending charge |
| MarkOrphan collects a receipt while a future Commit is still possible because authorization is temporarily absent, expired or revoked, causing repeated republish/liveness attacks | Every plan has one durable PreparingOpen, PreparingRevalidationRequired, CommitEligible, Superseded, Abandoned or Consumed attempt. MarkCommitEligible binds the exact lineage/begin/version, plan/bundle, Verified receipt, dispositions, pinned profiles and reservations. Commit consumes CommitEligible; Replan/Abandon fence and terminalize the same row; MarkOrphan accepts only Superseded/Abandoned under archive-head→plan-head→attempt→receipt locking. Temporary authorization state never implies ineligibility, and missing attempt state is corruption |
| An authorized Replan installs a replacement whose own mutation or future Commit exceeds backend atomic limits, or retained old artifacts consume capacity omitted from its proof | Trusted `CustodyReleaseReplanPreflightV1` freshly proves both replacement and future Commit under current backend row/byte/lock/read/write/index/output/work/time limits. The full-rank local transaction atomically consumes the Replan grant, capacity reservation, plan-head/attempt CAS and replacement reservations while charging the new bundle plus every retained old plan/receipt/reservation/evaluator/reconciliation/GC obligation; failure leaves the old head authoritative |
| A permanently Unknown transfer keeps ReleasePending, profile drains, evaluator retirement and recovery queues alive forever, or an operator converts uncertainty into free capacity | After bounded reconciliation, only independently authorized QuarantineUnknownTransfer converts the full pending maximum without surplus return into a permanent custody member, fences the exact transfer/destination namespace and retains policy/legal-hold/evidence. It terminalizes the dependency at the fixed charge; later evidence is append-only and never automatic refund. Replan reserves entirely new capacity and identities |
| Repeated authorized Replan generations accumulate unbounded durable plans, attempts, grants, fences, receipts, dependencies and recovery work, or compaction erases anti-reuse evidence | Immutable `CustodyReleasePlanLineageBudgetV1` cumulatively bounds every generation/artifact/operation/byte/row/transfer/work/time counter and cannot be raised/reset by policy, grant, backend, restart or restore. Each operation precharges its maximum. Exhaustion preserves a still-possible Commit or, after terminal attempt, enters non-refunding BudgetExhaustedRetained. Compaction requires authenticated predecessor-linked checkpoint/archive coverage preserving exact replay/conflict/counters/identity tombstones |
| A publisher stages or verifies new material after its attempt is Superseded, Abandoned or Consumed, growing durable storage and GC work despite having no Commit path | Stage allocates and authorizes upload only for the current PreparingOpen attempt; Verify rechecks that tag. PreparingRevalidationRequired structurally denies both. Stage, Verify, MarkEligible, Replan, Abandon, Commit and MarkOrphan share plan-head→attempt→receipt ordering after the archive head. Closed/non-current/missing attempts return typed no-write AttemptClosed before upload, verification or budget allocation; existing receipts remain bounded orphan candidates |
| A compromised or incorrect pinned evaluator remains executable until natural drain, a node cannot run a live evaluator after upgrade/restore, or migration to a replacement silently lowers the charge | Bind evaluator binary/algorithm/ABI and conformance-corpus digests and require authenticated node readiness for every pinned dependency. Emergency distrust epochs immediately deny chunks, extension, eligibility and Commit; unknown distrust is distrusted. Independent reservation migration fences old use, proves complete-domain compatibility, reserves positive delta and preserves at least original/current maximum plus accepted-byte/transfer identity. Uncertain external effects take permanent quarantine |
| A delayed adapter chunk, stale credential or retried multipart completion mutates the destination after its root/size receipt but before Commit | Closed Open→SealPending→Sealed transfer state gives Open exclusive write/extension authority. SealPending binds exact mutation version, object/root/encoded size/ETag/version/multipart identity and reconciles unknown provider outcome without guessing. Sealed requires an authenticated provider hard fence against stale writes and admits no append/overwrite/extension/finalize/namespace reuse; MarkEligible rechecks it. Providers without immutable versions/conditional creation/equivalent fencing are unsupported |
| Evaluator distrust reveals corrected charge above the reservation and plan maximum, but accounting clamps it or permanent quarantine retains only the known-insufficient historical amount | Trusted reassessment records unclamped checked corrected charge and explicit deficit. A deployment/tenant non-borrowable emergency reserve atomically covers the full deficit; otherwise uncovered liability fences tenant/backend readiness. Unknown reassesses before quarantine. Separate authorization alone provisions additional capacity, verifies exact deletion or migrates to sufficient capacity with legal-hold checks and immutable reassessment/distrust/quarantine history |
| An admission path creates durable or external work without charging the cumulative lineage budget, exact retry charges twice, or early settlement lets a tenant cycle capacity | Every destination Admit/revocation Apply, publication, reservation/seal/reconciliation, evaluator/deficit, unknown-member, plan-attempt and GC transaction creates or joins one stable pessimistic budget charge binding original bucket, ordinary/Recovery class, settlement identity/disposition and checkpoint membership. Exact retry/CAS loser joins; changed material conflicts. Settlement requires the named terminal result plus authenticated checkpoint/archive, and Recovery capacity is non-borrowable |
| A retained unknown member is never reclaimable despite later proof, is silently refunded on late evidence, or grows past platform capacity | Deployment/tenant hard count/base/deficit/evidence/work ceilings bound admission. A separate complete authorization family owns RetainedUnknown→ConfirmedPresent, DeletionVerified or PermanentlyUnresolvable. Confirmation covers reassessed deficit and never lowers charge; deletion proves every object/version/multipart operation under the fenced namespace absent and settles exact base/emergency capacity only when legal hold permits; all successors retain the immutable quarantine decision |
| QuarantineUnknownTransfer or MigrateDistrustedEvaluatorReservation wins the Issued authorization row but the generic table cannot return its stored result | The exhaustive Issued cell returns the matching action-specific result for every one of the six release actions. Admission, expiry, revocation and action consumption race on the same row; exact terminal observations join and changed material alone conflicts. Conformance enumerates all six actions |
| A corrected evaluator deficit is covered but final Commit converts only the historical reservation, stranding emergency capacity or creating an undercharged permanent member | EffectiveCharge is checked base plus covered deficit with immutable provenance. Commit atomically moves both into one member and records one DeficitSettlement; unknown response/restore observes the whole conversion or retains both encumbrances. Remediation-authorized migration exceeds the historical maximum only for the exact already-covered delta, and typed outcomes select resumable Commit, verified-deletion Replan or permanent fenced retention |
| Deficit remediation or retained-unknown deletion consumes a reusable signed grant without admission, destination revocation or a first-terminal winner | Each family independently implements complete six-state destination authorization: Admit/Expire, scoped issuer Revoke, destination Apply/inbox/tombstone and outcome/NotAdmitted/conflict. Consumption, expiry and applied revocation race by one CAS; VerifyDeletion rechecks quorum/SoD/legal hold and consumes the exact Issued row in the same transaction as capacity release |
| BeginRelease admission evades accounting or deadlocks because the lineage budget it must charge does not exist until Begin | A deployment/tenant pre-Begin pool with hard ceilings and protected terminalization reserves a stable candidate charge. Winning Begin atomically transfers the original charge identity/bucket/amount into the new lineage budget with plan/result/reservations. Expired/revoked losers remain charged until authenticated no-Begin proof; competing grants, crash and retry cannot activate or release twice |
| Seal reconciliation sees no current completion, reopens writes and then the old provider completion succeeds late | SealPending→Open requires an authenticated SealDefinitelyNeverCompleted receipt binding the provider operation, old epoch/token/object/multipart generation and newer cancellation/fence guarantee. Timeout, current absence or accepted cancellation does not suffice; without permanent rejection proof the transfer remains SealPending and no new writes begin |
| A failed or Unknown provisioning/migration attempt falls through into permanent retention, an action returns an unrelated continuation, or permanently fenced capacity has no stored member | A closed four-row action/result matrix permits only provision→ResumableCommit, deletion→VerifiedDeletionAndReplan, migration→MigratedPlanReadyForReplan and explicit retention→PermanentlyFencedRetained. Failed/Unknown work retains the predecessor and charge under its non-reusable admitted attempt. Only independently authorized retention atomically creates the named permanently fenced member with base plus covered deficit |
| A quarantined unknown member outlives final Commit but Completed rejects its resolution, or Completed remains mutable and obtains fresh work after plan compaction | Completed is split With/WithoutResidualCustody. Final Commit transfers each surviving terminal member plus exact protected budget, authorization, deficit, fence and provenance to its own residual obligation under one membership root, aggregate budget and routing head. Later permitted resolution/revocation/evidence/checkpoint/GC charges only the routed child/aggregate owner; the completed lineage admits proof maintenance and cannot reopen or lend capacity |
| One pre-Begin candidate wins, but loser cleanup either requires globally absent plan state forever or loosely releases the winner | Durable winner mapping names the exact activated candidate. Cleanup proves no artifact references the losing generation/charge, observes absent/different winner, requires terminal authorization, CASes only its CleanupPending row and uses a settlement identity disjoint from activation. Many losers clean independently without winner mutation |
| Remediation dispatches while its grant remains Issued; response loss then lets expiry/revocation win or retry dispatch the external deletion/migration twice | BeginDeficitRemediation atomically consumes execution authority and creates one exact effect/request/target/capacity/constrained-capability attempt before dispatch. Only that attempt dispatches. Complete reconciles the same effect without redispatch; Unknown retains admitted authority and charge, definite no-effect requires a fresh grant, and deletion release/permanent retention consume separate finalization authority |
| Multiple residual members share one Active/Resolved state or budget, so resolving one closes, settles, compacts or releases siblings with different outcomes | Final Commit creates one obligation and non-borrowable budget slice per member plus an exact sorted membership root and aggregate terminalization budget/counters. Each child has its own state/result and mixed outcomes are normal; child transitions cannot mutate sibling state/capacity or settle aggregate work before all terminal checkpoints |
| Authorization admission/revocation/resolution races final residual transfer and is omitted, charged twice or represented under both lineage and residual owners | A generationed LineageOwned/ResidualOwned routing head with exact membership root is locked first by Admit, Expire, destination Apply, Resolve, evidence, checkpoint and GC. Final Commit CASes it with child/root/budget transfer and completed disposition. CAS losers reread/reroute once; issuer intents do not choose ownership; restore rejects duplicate owner rows/sequences/inboxes/charges |
| A restrictive hold/policy, rotated credential, new distrust fence or ownership change lands after Begin but the stale capability still deletes or migrates | Dispatch—not Begin—is the legal/policy cut. A credential-isolating broker locks current routing/state-head/counter-capacity/attempt-set/attempt state, rechecks every bound epoch, atomically consumes its writer charge, redeems one-use capability and records EffectDispatched before it alone may call the provider. Pre-call drift closes definitely-no-effect with zero traffic; post-CAS uncertainty is Unknown, provider identity is stable/idempotent and worker takeover cannot redeem again |
| The fixed residual membership root hashes mutable state/budget bytes and becomes stale after resolution, or concurrent siblings overwrite an aggregate decrement | Membership hashes only immutable obligation descriptors and transfer results in a distinct domain. Versioned child state, child budget state, immutable settlement and aggregate state rows advance together under routing→aggregate→child→authorization lock order; siblings serialize on aggregate CAS and restore recomputes the exact equation |
| Routing-head absence is interpreted as LineageOwned, or competing Begin/Replan paths create, replace or roll back generation 1 | The exact winning custody-release Begin atomically creates routing generation 1 LineageOwned with lineage/plan/budget/stable-owner identity and canonical-empty root. Replan only version-checks/preserves it; final Commit alone changes owner. Missing, inconsistent, duplicate, inferred or rolled-back genesis is no-write and fails readiness/restore |
| An attacker rolls back/deletes a child state, its budget/settlement and matching aggregate row as one internally consistent older snapshot | A cumulative predecessor-linked ResidualCustodyStateHead commits every mutable child/budget/settlement/checkpoint plus aggregate reference/state and routing/membership identity. Child settlement advances it atomically; signed checkpoints publish an independent external high-watermark, and restore below or discontinuous with that anchor is unready |
| MarkEligible or final Commit wins while an effect is ExecutionAdmitted, EffectDispatched or Unknown, or while a broker capability/reconciliation obligation remains live | A complete remediation-attempt-set head/root is advanced by every covered writer. MarkEligible and Commit refuse every nonterminal attempt/capability/obligation; CommitEligible fences new Begin/Dispatch and Commit rechecks the unchanged complete root. No effect work is implicitly transferred or omitted |
| Empty aggregate row absence is confused with valid zero-member state at routing genesis or zero-survivor final Commit | A closed AggregateReference is explicitly NoneCanonicalEmpty or SomeAggregate. Begin and zero-survivor Commit authenticate None in the residual-state/routing heads and create no aggregate row; nonzero Commit authenticates Some with exact digests. Absence supplies neither state |
| An adapter claims same-effect safety but has a narrower effect-ID scope, finite dedup gap, inconsistent query, unsafe credential rotation, late duplicate or forgeable no-effect response | Governed ProviderEffectConformanceProfile/Result artifacts and adversarial vectors bind every semantic and horizon. Dispatch requires a current passing result; after the unique transmission claim terminalizes only query is allowed, definitely-no-effect requires authenticated evidence, and unsupported providers refuse destructive remediation |
| Authorization, capability, reconciliation, evidence or archival state changes without advancing the remediation attempt-set head, or Dispatch acquires attempt before the head and deadlocks with final Commit | A closed writer matrix names every covered command. Each uses routing→residual-state→counter-capacity-state→attempt-set→capacity-archive-high-watermark→capacity-archive-publication→capacity-archive-replay-head→commit/remediation-attempt→auxiliary→result order and atomically consumes one typed writer charge with the covered rows, immutable mutation record and successor head. Ordinary writers must take/read the high-watermark guard before omitting unrelated publication/replay rows; no direct, reversed or narrower writer exists |
| Revocation, evidence, checkpoint or compaction legitimately changes the attempt-set root after CommitEligible, leaving Commit unable to use the old root and eligibility unable to refresh | The writer proceeds and atomically selects PreparingRevalidationRequired with mandatory invalidation ID and exact old/new roots. MarkEligible repeats the complete original validation against the successor; Commit and writer serialize, restrictive processing never blocks and only PreparingOpen can admit Begin/Dispatch or Stage/Verify |
| A provider deduplication horizon is misread as permission for an application resend after timeout, crash, lease loss or takeover | Dispatch persists exactly one application-level ProviderEffectTransmissionClaim as a VIT-INV-006 realization. Only the winning executor receives an unreconstructable process-local permit; durable row/digest is not bearer authority. Only transport retransmission inside the uninterrupted invocation is permitted; return or uncertainty terminalizes it into query-only reconciliation |
| An adapter omits the invalidation/fence join or restore loses that evidence and interprets revalidation-required state as ordinary open preparation | The authoritative attempt row itself carries PreparingRevalidationRequired with mandatory invalidation/root payload. Missing/corrupt cross-proof makes it unready; unknown/truncated tags fail decode. It cannot default to PreparingOpen, which is the only tag granting effect/artifact admission |
| A retry, queue consumer or restored node treats ProviderEffectTransmissionClaim or its digest as a portable permit and invokes the provider | VIT-INV-006 applies unchanged: one winning trusted-executor transaction receives process-local unreconstructable permit material exactly once. Exact retry returns status only, post-commit loss cannot reconstruct it and durable claim/digest/status is explicitly non-bearer |
| A second revocation, evidence, reconciliation or checkpoint mutation advances the attempt-set head while the attempt is already PreparingRevalidationRequired, stranding MarkEligible on its earlier required root | Each such writer atomically CASes the full Required payload and appends an immutable checked `u128` RevalidationAdvance binding original invalidation/eligibility root, previous/current required roots, latest mutation and cumulative commitment. MarkEligible verifies the complete chain or authenticated checkpoint plus suffix against the locked current head. Concurrent/replayed advances are exact; missing, reordered, forked or rolled-back evidence denies, while the lifetime-capacity proof guarantees every admitted advance and rollover fits |
| A finite attempt-set or revalidation counter reaches its maximum, but the system claims it can still identify and atomically append the triggering restrictive mutation | Begin/Replan proves checked lifetime headroom for every writer class across fixed `u128` counters and reserves `u128::MAX` solely as an absorbing CounterExhaustionFence identity. Insufficient or overflowed proof denies before external work. Unexpected last-ordinary state installs the sentinel fence instead of the mutation and permanently unreadies all effect/Commit paths; an already-used unauthenticated sentinel is corruption, and no arbitrary post-exhaustion write is promised |
| A capacity proof states a writer maximum, but no authoritative state consumes it, or retry/Replan/compaction resets usage so more head writes occur than admitted | One lineage-wide predecessor-linked CounterCapacityState owns closed per-class admitted/consumed/remaining counts, total head writes, revalidation advance/rollover subsets, sentinel reservation and current counter tuple. Every head writer locks it before the head and atomically commits one immutable class+command WriterCharge with mutation/head/result/audit/outbox. Exact retry rejoins, CAS losers consume none, class substitution conflicts and Replan/compaction/restore/migration preserve consumption plus count/head equations |
| Capacity charge rows are compacted behind a broad lineage checkpoint that cannot prove exact retry, changed-material conflict or genuinely unseen identity, or an archive disappears after hot-row deletion | A dedicated exact-set CounterCapacityCheckpoint commits every covered charge/material/result, all capacity and sentinel equations, captured hot rows and publication/key/encoding epochs. A predecessor-linked CapacityArchiveReplayHead selects only the greatest verified checkpoint plus hot suffix. Final head installation and exact captured deletion are atomic, its own history-lifecycle charge remains hot, and missing/forked/rolled-back history returns typed historical-state-unavailable without authorizing a charge or head advance |
| Capacity archive chunks have no owned lifecycle, so an adapter declares “verified,” Commit races orphan GC, verification is unbounded or cleanup borrows ordinary security capacity | Dedicated manifest/state/receipt/budget/cursor rows and typed Stage/Verify/MarkOrphan/FinalizeGc results enforce Staged→Verified→ConsumedByCommit or OrphanGcEligible→Collected. Verification is bounded/resumable and reconciliation/GC use non-borrowable Recovery capacity. CAS closes Verify/Commit/orphan races; prepared/referenced data cannot be collected, and storage adapters remain evidence-only |
| A locally consistent rollback removes capacity state, replay head and publication registry together, restoring already consumed writer capacity | Before external witness traffic, Prepare atomically consumes the first of three core charges and installs a local PreparePending ordinary-writer fence. An independent authority signs an acyclic proposal for the exact checkpoint/replay-head, publication/epochs, capacity-state successor, terminal-disposition and ArchiveFinalize preimages and deletion set. Terminal Reconcile and Commit consume the remaining two core charges. Only authenticated definitely-not-witnessed evidence may abort; unknown stays fenced. Restore reads the witness first and exact-completes its deterministic successor or remains unready |
| An ordinary writer skips the high-watermark guard or an archive writer locks replay head before publication/high-watermark, writing past PreparePending or deadlocking another backend | One universal rank applies. Begin creates the stable guard; every ordinary head writer acquires the routing→residual→capacity→attempt-set→high-watermark common prefix, denies PreparePending/Witnessed and may omit unrelated later rows. Archive writers acquire the full high-watermark→publication→replay-head suffix. Every acquired subset preserves relative order, no omitted earlier rank may be acquired later, and backend traces reject inversion |
| An immediate witness callback mutates PreparePending directly, or immediate versus queried response imports different state/charges/results | Adapters are evidence-only. Every immediate, delayed, queried or replayed outcome enters exclusively through typed Reconcile. One stable witness-disposition ID owns exactly one terminal disposition charge/state/mutation/head/result; exact retry rejoins, timeout is read-only and changed receipt/predecessor/publication/manifest/successor/profile material conflicts |
| Witness backends disagree on monotonic CAS, equivocate after failover or forge “definitely not witnessed” under stale keys | A governed tenant/lineage-scoped witness conformance profile/result binds authority identity, expected-predecessor CAS, non-equivocation, signature/signer/key epochs, rotation/distrust, authenticated negative semantics, read-after-write durability and failover. Prepare requires a current passing result before mutation/traffic |
| The signed witness commits a Reconcile or Commit result that itself contains the witness receipt/signature, creating a self-referential digest or cross-epoch encoding ambiguity | A canonical CapacityArchiveWitnessProposal binds semantic transition/result preimages but excludes all future signature/receipt and Reconcile/Commit result digests. The authority signs only its domain-separated canonical digest; receipt binds proposal, Reconcile binds proposal+receipt and Commit binds proposal+receipt+Reconcile. A generated schema-reference/hash-dependency graph rejects every cycle, signature-field inclusion, alternate encoding and epoch substitution |
| Capacity accounting merges Prepare and terminal Reconcile or overlooks one of Prepare, disposition and ArchiveFinalize, allowing a core head advance to disappear behind compaction | The no-query witnessed path has exactly three independently identified hot history-lifecycle charges and head advances: Prepare, terminal disposition Reconcile and ArchiveFinalize Commit. With `q` query admissions the equation is `3 + q`; checkpoint coverage excludes them, retries return each result independently and timeout/absence never creates the disposition charge |
| Unknown witness status drives unlimited external queries, retries a durable claim as bearer authority or treats local budget exhaustion as proof of non-witnessing | Begin/Replan funds a non-borrowable Recovery reconciliation budget with hard call/encoded-byte/verification-work/elapsed/concurrency bounds, stable non-wrapping attempt IDs and durable backoff. Each admission consumes its own class charge/budget/head before one process-local unreconstructable permit; replay yields no permit. ReconciliationBudgetExhausted forbids further traffic, stays fenced and never implies definitely-not-witnessed |
| Capacity checkpoint or replay-head sequence reaches/wraps its maximum even though the lifetime proof covered only attempt-set/revalidation counters | The capacity proof/state includes one fixed-`u128` archive sequence pair with equal values and one paired advance only for the successful Commit-capacity-archive-publication charge named ArchiveFinalize, never FinalizeGc. Current plus every admitted ArchiveFinalize is at most `u128::MAX - 1`; one atomic `(MAX, MAX)` pair sentinel/fence handles unexpected terminal exhaustion, while any one-sided or unequal sentinel is corruption |
| Lost credentials/keys or inconsistent deletion evidence leaves ActivatedCleanupPending permanently blocking foreground work; unknown is treated as deleted; retention self-authorizes or refunds; late evidence rewrites activation | DeletionOutcomeUnknown enters bounded CleanupReconciling with durable budgets. Independent SoD retention authority alone can atomically move the still-encumbered workspace into a precharged hard-bounded PermanentlyRetained pool and remove it from the active cleanup lane. No credit or campaign-result mutation occurs; late evidence is bounded append-only custody evidence, and only whole-member custody release can later reach CustodyReleased |
| An aborted workspace cannot reconcile unknown deletion, resumes as activation cleanup, or retention binds the wrong campaign terminal | Closed Activated/Aborted CleanupOrigin plus origin-discriminated terminal reference is persisted through CleanupReconciling. Both pending states may enter; only the matching state may resume. Retention and late-evidence tests bind the exact activation or abort checkpoint and never rewrite either result |
| Retention or custody approval prose/evidence is treated as command authority; revocation is remote-only; BeginRelease is replayed as final release; or CAS losers create conflicting terminal meanings | Complete six-state destination-admitted retention and lineage-release authorization families with action-scoped issuer intent/sequence, destination inbox/apply/tombstone, expiry, first-terminal outcome and changed-material conflict. BeginRelease and CommitCustodyRelease require separate grants consumed with their exact transactions; evidence alone cannot invoke them |
| Whole-member release acquires lineage before linked campaign fences, two workers lock old fences in different order, or individually bounded workspaces overflow the combined backend transaction | One canonical archive/journal→parent→current-slot→old-fences sorted by canonical campaign ID→control/lineage/checkpoint→authorization/custody→outputs rank applies to begin and final release. CustodyReleaseBundleHardMaximum bounds aggregate ordinary/workspace rows, bytes, locks, reads, writes, indexes, outputs, work and time; checked preflight is no-write before ReleasePending when unsupported |
| Archive publication or a storage adapter directly performs ReleasePending→Released, final payload fields are rebound, response loss repeats deletion/credit, or a generic release command bypasses action checks | Explicit BeginLineageRelease and CommitLineageCustodyRelease commands with separate payload/result/conflict types are the only mutation boundaries. Commit rechecks stored begin result, authenticated publication receipt, exact heads/bundle/grant, expected lineage version and idempotency, then returns one stored final result. Publishers/adapters create evidence only; the generic family identifier is non-dispatchable |
| A revocation issuer omits a command/sequence/result, mutates the destination directly, retry emits a second intent, absent expiry/consumption creates state, late admission escapes pre-revocation, or a terminal is converted | Explicit issuer Revoke commands atomically allocate target/action-scoped non-wrapping sequence plus signed intent/result or changed-material conflict and have no destination capability. Destination Apply alone executes the shared complete six-state table: absent expiry/consumption is no-write NotAdmitted, late admission joins RevokedBeforeAdmission, CAS losers/terminals return stored outcomes, and stale/lower or changed material conflicts |
| A sparse consumer advances a dense compaction watermark across never-presented receipts, accepts a forged/incomplete issuer range, or treats first late presentation as unused; alternatively normal floods starve emergency repair, or break-glass becomes an unlimited quota/replay bypass | Issuer-authenticated complete sequence/digest/deadline range manifest; consumer sparse commitment by default; dense `ConsumerCompactionEligibleThrough` only after complete range plus trusted-time horizon/deadline and terminal/permanently-unresolved proof; late presentation remains historical; closed normal/recovery/break-glass counters, independent rate ceilings, non-borrowable emergency reserve and recovery lane; no break-glass exemption from time, SoD, receipt, deadline-CAS, single-use, replay or archive-proof denial |
| Issuance partially commits quota/sequence/receipt/outbox state, client timeout frees a live grant, duplicate settlement underflows counters, one compromised caller monopolizes normal capacity, or a huge/deep range proof exhausts memory/CPU | One VIT-INV-061 local atomic quota/reservation/sequence/receipt/idempotent-result/outbox transaction; layered deployment/issuer/canonical-caller ceilings; outstanding capacity released exactly once only by authenticated stable terminal evidence under settlement ID; bounded root/chunk bytes, entries, decode allocation, verification work, proof depth and chunks per job; pre-allocation limit checks, durable cursor, and partial/cyclic/substituted chain denial |
| Issuer lineage is revoked/superseded while its receipt remains live, current policy/key/epoch differs from the original quota claim, an issuer forges consumer terminal evidence, settlement races partially release or underflow counters, or denied requests bypass/consume the wrong rate ledger | Lineage change blocks only new issuance and retains live reservations; separate presentation/request/admission/outstanding ledgers; successful atomic issuance embeds the original deployment/issuer/principal budget keys, epochs, class, reserve source, units and quantities; settlement locks and decrements those original counters all-or-none under a stable ID; release accepts only consumer-authenticated consumption/definite absence/permanent-unresolved state, conservative exact-deadline expiry, or a receipt-specific VIT-INV-060 fence/tombstone proven by an authenticated consumer terminal receipt; immediate receipt revocation is a separate consumer-serialized protocol and VIT-INV-061 cannot mint its proof |
| Consumer terminal evidence omits/substitutes ownership, receipt, intent, outcome, result, tombstone, time, sender, key, message or outbox fields; uses an open/unknown outcome; rolls back sequence; replays a conflicting result; lets the issuer sign as consumer; or treats reconciliation as terminal | Canonical versioned terminal and reconciliation authentication preimages bind every field; closed `TopologyAuthorizationConsumerTerminalOutcomeV1` permits only revoked-before-consumption, already-consumed, expired, definitely-not-committed and permanently-unresolved; `TopologyAuthorizationConsumerDispositionV1` separates terminal outcomes from `TopologyAuthorizationConsumerReconciliationEvidenceV1`; reconciliation uses a distinct receipt and settlement accepts only `TopologyAuthorizationConsumerTerminalReceiptV1`; VIT-INV-060 sender-only authentication and VIT-INV-061 verify-only credentials; canonical-none optional intent; stable message/idempotency plus monotonic result/outbox sequences; conflicting/unknown/incomplete/rolled-back/reconciling evidence retains capacity |
| Partial quota reservation, cross-partition transaction, capacity duplication/loss, free-at-both-ends transfer, protected-class conversion through transfer/adjustment/policy abuse, floor-reduction-then-spend approval reuse, incomplete rollout parent set, stale post-finalization parent activation, independent rollback after partial activation, cancelled prepared-parent capacity stranded or independently restored, superseded rollout replay, platform-floor key confusion or downgrade, non-atomic capacity-policy activation, transient multi-parent over-allocation, stale delayed activation authority, deadlock/retry duplication, token/member substitution, refund/write-off fraud, cross-kind settlement, liability loss, or recovery starvation | Local non-aggregate quota authority, canonical all-or-none sets, immutable transfer hierarchy/class/authorization, existing-capacity class immutability, one-parent `QuotaCapacityPolicy` lineage, co-located parent-ledger CAS, independent protected-floor capability/history, operational fences and obligation simulation, durable versioned/digested fully scope-keyed platform-floor admission ratchet, total overflow-checked key-set migration, strict mixed-version floor and stale-node startup rejection, cross-command separation, root-owned canonical parent manifest/membership epoch and single active rollout generation, atomic permanent successor supersession, direct cancellation only before preparation, complete root-owned cancellation-recovery successor after preparation, conservative parents/no independent restore, idempotent restore-safe recovery receipts and deadline escalation, complete bound preparation set and total conservation, root finalization as permission only, fresh local parent activation CAS over active generation/ledger/floor/obligations/root/current fences with blocked/reconciliation state on drift, successor-root rollback over current actual limits, cancelled/superseded message and restore rejection, conservative multi-parent rollout, per-transition current tenant/principal/policy fences, stable receipts, authenticated ack plus old-epoch fence, conservative double-entry conservation, canonical composite lock/retry, whole-set restore/quarantine, active/active rejection, per-kind evidence, separate compensation, fair recovery partition |
| Parser/resource exhaustion | Explicit byte/item/depth/allocation/work limits, cancellation, quotas, fuzzing and load tests |
| Injection and unsafe content | Canonical parsers, contextual encoding, attachment quarantine, no arbitrary scripts or SQL exposure |
| Workflow or policy bypass | Deterministic IR, version pinning, simulation, approval, signed activation, fail-closed evaluator |
| Plugin escape or secret extraction | Capability manifest, no direct database access, no plaintext credential in guest memory, brokered authenticated operations, memory/fuel/time/output/network limits, signed rollout |
| Workload identity, bearer theft, or issuer confusion | External-issuer resource-server validation, sender-constrained privileged tokens, restricted lower-assurance bearer policy, immutable issuer-subject/audience/tenant binding, no local token endpoint |
| AI prompt/tool abuse | Permission-filtered context, provenance, tainting, structured proposals, independent policy and human approval |
| Secret leakage | Non-extractable handles, host-brokered secret operations, redacted errors/logs, no core or Wasm plaintext secret storage, rotation and best-effort zeroization policy |
| Supply-chain compromise | No current dependencies, pinned tools/actions, lockfile, SBOM, read-only CI, signed provenance and pentest gate |
| Availability failure | Tenant quotas, per-tenant/work-class recovery ceilings and fair share, starvation bounds, non-borrowable emergency reserve, backpressure, poison queues, bounded retries, leases, provider-outage reconciliation and chaos testing |
| Historical authority conflicts with mandatory deletion | Rollup-substitution gate, independent disposition for raw/rollup/manifest/result/cache/export/checkpoint metadata, permitted non-sensitive tombstone, unknown history, immutable authority-loss fact |

## Initial `0.1.0` Attack Surface

The initial workspace has no server, networking, storage, parser, plugin, AI,
or executable product. Its attack surface is repository and build integrity:
malicious contributions, CI workflow changes, toolchain compromise, accidental
publication, policy bypass, misleading claims, and release-evidence forgery.

Controls include `publish = false`, no registry/git dependencies, read-only CI,
full action SHA pins, strict lints, source-size enforcement, SBOM generation,
versioned pentest reports, and a readiness gate that refuses a non-PASS report.

## Non-Goals And Non-Claims

Memory-safe Rust does not prove authorization, tenant isolation, cryptographic
correctness, side-channel resistance, availability, or safe operations. A
pentest is additional evidence, not a replacement for review, fuzzing,
conformance, formal methods, or recovery testing. `0.x` milestones are not
production-supported unless explicitly stated.

## Required Delta Process

Every release records new assets, actors, entry points, data flows, trust
boundaries, abuse cases, mitigations, tests, residual risk, and removed surface.
The pentest scope must cover that delta and relevant regression paths from the
previous tag.
