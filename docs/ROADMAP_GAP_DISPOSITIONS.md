# Roadmap Gap Dispositions

Status: incorporated planning record

This record explains how the reviewed gap analysis strengthened the existing
roadmap. It is not a substitute for the version handoffs and does not copy the
analysis wholesale.

| Gap | Disposition and version context |
| --- | --- |
| Release report does not bind the candidate | Reopen `0.1.0`: validate candidate tree/parent and signed detached or evidence-only attestation before its pentest exit. |
| SBOM check is name-only | Reopen `0.1.0`: compare normalized semantic package/source/version/license/relationship content and bind it into release evidence. |
| Permissive version parsing | Reopen `0.1.0`: strict numeric SemVer parsing with negative tests; never interpolate untrusted version text into regex. |
| N0/N1 is mostly prose | Add layer metadata and checked DAG/build-script/proc-macro/FFI/alloc/facade rules to `0.2.0`; maintain them thereafter. |
| Universal polymorphic write risk | Strengthen `0.2.0`, `0.31.0`, and `0.33.0`: typed IDs/commands/events/streams, component-only `WorkItem`, no patch/set-state escape hatch, configured lifecycles only narrow aggregate law. |
| Tenant proof arrives too late | Move structural tenant scope to `0.2.0`, `0.7.0`, journals at `0.11.0`, twin-tenant conformance at `0.22.0`, and adapters at `0.23.0–0.30.0`; `0.51.0` becomes formal cross-surface assurance. |
| Command transaction/idempotency ambiguity | Strengthen `0.13.0`, `0.16.0`, and `0.17.0`: exact-version normal writes, request digests, atomic receipt/outbox/inbox/effects, fenced exceptional `Any`, bounded replay horizon. |
| Journal chain lacks external anchor | Strengthen `0.19.0`: partition commitments, external signed anchors, cross-signed rotations, optional independent timestamps, recovery verification. |
| Privacy, retention, holds, erasure absent | Add classification at `0.5.0`, immutable-safe event metadata at `0.8.0`, dedicated sensitive-payload lifecycle at `0.8.1`, hosted foundations at `0.28.0–0.28.2`, custody/disposition at `0.79.0`, and production freeze at `0.140.5`. |
| Key lifecycle incomplete | Expand `0.19.0`, `0.28.0`, `0.79.0`, `0.145.0`, and `0.147.0`; select crypto/KMS profile at `0.140.1`. |
| Authenticated API precedes real identity | Limit `0.40.0` to static/fake internal test authentication; production OIDC/WebAuthn remains Phase F and production profile selection is `0.140.3`. |
| OIDC and WebAuthn are over-bundled | `0.53.0` owns OIDC; add corrective milestone `0.53.1` for independently pinned WebAuthn profile and review. |
| ITSM transitions could collapse into generic mutation | Make sealed domain transition commands/events and side-effect-free rejection universal in Phase E; `0.41.0` supplies the first explicit fixtures. |
| Policy decisions lack one complete contract | Strengthen `0.56.0` with tenant/subject/action/resource/field/purpose/environment/version input and obligation-bearing decisions; prove every interface at `0.58.0–0.60.0`. |
| Authorization inventory is manual | Strengthen `0.58.0` and `0.60.0`: generated interface registry and mandatory cases for routes, reads, exporters, notifications, plugins, workflows, and AI. |
| Raw SecOps evidence could be normalized away | Strengthen `0.72.0–0.74.0`: normalization, grouping, and suppression create derived assertions while raw source bytes remain immutable. |
| Graph time/correction/merge/traversal semantics incomplete | Strengthen `0.84.0–0.88.0`: four distinct time claims, correction identities, non-destructive equivalence, immutable source facts, filter-before-traversal, policy-safe paths. |
| Evidence custody lacks preservation/disposition | Strengthen `0.79.0`: envelope encryption, clock quality, access/copy/transform/hold/disposition events, signed derived-artifact manifests. |
| WASM capabilities lack host/OS detail | Strengthen `0.111.0–0.120.0`; select runtime/process/egress profile at `0.140.4`. |
| AI broker isolation is mostly prose | Strengthen `0.121.0–0.130.0`: no DB/dispatcher authority, immutable context packs, opaque citations, server-side resolution, current-policy recheck, no policy-incompatible fallback; decide enablement at `0.140.8`. |
| Production options were implicit | Add `0.140.1–0.140.10` production-profile freeze decisions covering dependency/crypto, storage/tenancy, identity, plugins, privacy, deployment, SDK publication, AI, interchange, and federation before Phase O. |
| Production technology decisions occurred after implementation | Require a version-bound implementation-admission record at every first consuming hosted milestone (`0.19.0`, `0.23.0–0.28.2`, `0.36.1`, `0.53.0–0.53.2`, `0.93.1`, `0.112.0`, `0.142.1`); redefine `0.140.x` as revalidation/freeze, never first selection. |
| Early integration gates depended on later phases | Make `0.50.0`, `0.75.0`, and `0.80.0` certify deterministic port fakes only; require fake-versus-real integration retests at `0.70.0`, `0.88.0`, and `0.100.0`. Later authority-bearing surfaces must register before their own exit. |
| Security audit architecture lacked an owner | Add the audit envelope/authoritative intent at `0.15.1`, make it atomic with command outcome at `0.16.0`, and use `0.20.1` for the rebuildable audit projection plus pre-release read/download receipts and streaming completion/abort facts. |
| Personal/secret event data was protected too late | Add schema classification at `0.5.0`, safe event metadata at `0.8.0`, and `0.8.1` for encrypted/erasable payloads, tombstones, crypto-erasure, hash authority, rebuild-after-erasure, and forbidden metadata sinks. |
| Hosted HA capabilities lacked implementation owners | Assign S3 at `0.28.1`, KMS/secrets at `0.28.2`, process secret handling at `0.28.3`, journal-backed queue at `0.30.1`, cache at `0.30.2`, malware scanner at `0.36.1`, service measurements/paging/status at `0.38.2`/`0.39.4`/`0.48.2`, distributed sessions at `0.53.2`, PostgreSQL search at `0.93.1`, and telemetry exporters at `0.142.1`. |
| Tenant lifecycle was implicit | Add `0.51.1` for provision/suspend/export/hold/close/delete/key-destroy, identifier non-reuse, cleanup proof, and topology migration. |
| Quotas were process-local or underspecified | Add semantic durable reservations/refunds/fairness/reconciliation at `0.18.1` and production stress evidence at `0.146.0`. |
| Event evolution and cross-aggregate law were incomplete | Strengthen `0.8.0`, `0.11.0`, `0.14.0`, `0.29.0`, and `0.148.0` with schema registry, one-stream transactions, process managers, pure upcasters, original-byte authority, quarantine, and mixed-version corpora. |
| Read consistency was not externalized | Carry `0.15.0` consistency/read-your-write tokens through `0.40.0` and the external API candidate at `0.138.0`. |
| Workflow history and policy lifecycle lacked operational limits | Strengthen `0.68.0` for checkpoint/continue/archive/heartbeat/orphan handling and add `0.56.1` for policy simulation, approval, activation, rollback, bootstrap and lockout recovery. |
| Scanner verdict, notification, plugin-state, and observability details were implicit | Bind scanner versions/reevaluation at `0.36.0–0.36.1`, render protected notification references at `0.39.0`, define stateless-or-namespaced plugin state at `0.114.0`, establish tenant-safe telemetry semantics at `0.20.2`, and implement exporters at `0.142.1`. |
| Stable API milestone bundled unrelated responsibilities | Make `0.138.0` the external API/codec candidate, `0.138.1` private SDK candidate, `0.138.2` staged import, and `0.138.3` policy-snapshot/manifest export. |
| Five production databases create excessive pre-v1 surface | Keep memory semantics, SQLite single-node, and PostgreSQL HA as default candidates; classify MySQL/MongoDB/SurrealDB milestones experimental unless `0.140.2` promotes an evidenced profile. |
| Interchange support was vague | Assign implementation owners at `0.53.3` (SAML), `0.54.1` (one selected SCIM direction), `0.71.1` (authenticated alert-bearing syslog/security webhooks), `0.71.2–0.71.3` (STIX/TAXII ingestion and marking enforcement), `0.78.5` (CVE/CVSS/VEX), `0.86.1` (conditional concrete CMDB), and `0.89.1` (SPDX/CycloneDX); `0.140.9` can only revalidate implemented evidence or defer a profile. |
| Interface flexibility needed a governed “Lego block” model | Strengthen `0.132.0` and add `0.132.1` for typed composable blocks, drag/drop layouts, tenant/team/role/user ownership, safe custom blocks, configuration-as-code, accessibility, budgets, review, versioning, and rollback without arbitrary scripts or raw queries. |
| Small and large organizations need different interface structures | Add `0.132.2` with unified small-company and separated enterprise information-architecture profiles plus governed custom profiles that preserve identical API/domain/RBAC semantics. |
| RBAC needed explicit product-wide scope | Strengthen `0.55.0` for platform/tenant/workspace/shared-space/resource scopes, custom roles, separation of duties, provenance/expiry, and a law that dashboards/navigation/plugins never grant capabilities. |
| Organization-to-organization cooperation was absent | Add `0.120.1–0.120.5` for bilateral instance trust, shared spaces, federated work exchange, managed-service asset/security delegation, and multi-instance conformance/revocation/recovery; add optional production freeze at `0.140.10`. |
| API-first architecture and UI separation were not early enough | Add architecture laws and `0.40.1` for separate API contract/application/transport and UI crates, forbidden UI-to-domain/storage dependencies, headless conformance, and UI/API differential tests; `0.138.0` later freezes API v1. |
| Plugin ecosystem lacked a storefront and concrete security connectors | Add `0.116.1` for a governed signed catalog/storefront, `0.118.1` for Microsoft Defender/Sentinel ingestion, and `0.118.2` for Tenable asset/vulnerability ingestion; discovery, install, permission approval, activation, and upgrade remain separate actions. |
| Vulnerability management was too compressed | Expand `0.78.1–0.78.4` for advisory/finding/affected-asset identities, evidence-based prioritization, remediation campaigns/exceptions/verification, and coverage/program conformance reporting. |
| Cross-domain graph correlation needed explicit ownership | Add `0.88.1–0.88.3` to connect teams/services/assets/software/vulnerabilities/alerts/incidents/changes/evidence/remediation through typed provenance-aware edges, bounded evidence-based correlation, and an authorized analyst workspace. |
| Mandatory audit was not crash-atomic with commands | Insert `0.15.1` before mutation, strengthen `0.16.0`, and freeze the indivisible command variant at `0.16.1`: successful protected commands commit events/head/receipt/audit/outbox/integrity/uniqueness together; denied commands commit only integrity-linked idempotent outcome plus audit authority. The quota-bearing complete work family follows at `0.18.2`. |
| Generic cases could recreate the universal ticket | Strengthen `0.136.0` with non-substitutable `CaseId`, distinct streams/ports, immutable schema/lifecycle binding, narrow commands, protected namespaces, reviewed process-manager migrations, and compile-fail separation from every built-in domain. |
| Federation trust semantics were incomplete | Strengthen `0.120.1–0.120.5` with signed remote actor/assurance/delegation assertions, authorization intersection, canonical signed sequenced/hash-linked envelopes, fork detection, and honest separation of capability revocation from unverifiable peer plaintext retention. Move federation out of the kernel family. |
| Telemetry conventions arrived only at production packaging | Add `0.20.2` for bounded tenant-safe log/metric/trace/health semantics used by every later hosted component; keep `0.142.1` as exporter and graceful-drain implementation only. |
| Semantic search lacked a production adapter | Add conditional `0.98.1` for admitted hosted vector storage and `0.98.2` for independently admitted embedding generation, with tenant/purpose/policy partitioning, provenance, deletion/erasure, model migration, rebuild, outage, and leakage evidence; otherwise `0.125.0` and the production AI profile keep similarity disabled. |
| Core service-operations capabilities lacked typed owners | Add `0.38.1` for SLI/SLO/error budgets, `0.39.1–0.39.3` for on-call/paging/preferences, `0.48.1` for stakeholder/status communications, and `0.82.1` for provenance-aware service health; assign production measurement/paging/status paths at `0.38.2`, `0.39.4`, and `0.48.2`; repeat workflow/alert integration at `0.70.0` and `0.80.0`. |
| Individual ITSM milestones still assumed later search/graph implementations | Make `0.45.0` and `0.46.0` depend on deterministic search/topology ports and fakes, with real integration retested at `0.100.0` and `0.88.0`. |
| Plugin catalog dependency security was package-only | Strengthen `0.116.1` with bounded dependency resolution/locking, transitive capability diffs, cycle limits, revocation cascades, threshold root signing, compromised-publisher response, and freeze/rollback protection. |
| Optional product families could leak into core | Make federation, AI, vendor connectors, plugin storefront, and hosted semantic index depend inward on stable ports; kernel and first-party domains must build and operate without them. |
| Storage atomicity omitted newer kernel authorities | Add `0.16.1` command, `0.17.1` consumer, and `0.18.2` timer/activity/poison work-commit variants; bind receipts/audit/fencing/quota into commitments, anchor denial-only sequences, require every adapter to negotiate all-or-unsupported, expand PostgreSQL ownership, and make `0.22.0` destroy each component independently. |
| Provenance and four-clock semantics arrived after SLOs | Add shared N1 fact primitives at `0.20.3`; require SLI/SLO, service health, alerts, evidence, vulnerabilities, assets, and services to reuse them; redefine `0.84.0–0.85.0` as asset/service specialization and conformance. |
| Service principals lacked workload authentication | Add `0.52.1` as an external-issuer OAuth resource-server profile: external clients may authenticate to that issuer with selected `private_key_jwt`/mTLS profiles, while Vitheim validates short-lived audience-bound tokens, maps a stable tenant-bound principal, handles issuer rotation/revocation, reevaluates exact capabilities, and exposes no PAT/API-key/token endpoint. |
| Service-operations ports and fakes lacked production data paths | Add `0.38.2` for an authenticated append-only customer-measurement plane, `0.39.4` for an evidenced paging/notification provider, and `0.48.2` for the built-in hosted status API and reconciliation; bind `0.82.1` health to real measurements. Keep customer observations separate from `0.20.2` Vitheim telemetry. |
| Vector storage was conflated with embedding generation | Keep `0.98.1` responsible for the hosted semantic index and add `0.98.2` for the separate generator/model/tokenizer/chunking/provenance/residency boundary; `0.125.0` and `0.140.8` require both or keep semantic similarity disabled. |
| Syslog wording could imply a full SIEM | Restrict `0.71.1` to authenticated, pre-filtered, alert-bearing profiles with bounded raw evidence; unauthenticated UDP cannot create alert authority. General raw-log storage/query and detection-rule execution require a separate future architecture and are deferred by default at `0.140.9`. |
| Process-memory secret assurances were implicit | Add `0.28.3` for non-displayable/non-serializable secret wrappers, scoped reveal, bounded caches, admitted best-effort zeroization, crash/core-dump/swap policies, prohibited-sink canaries, and truthful per-platform limitations. |
| Threat-intelligence markings could disappear after ingestion | Add `0.71.3` to propagate effective markings through search, graph/correlation, federation, export, notifications, plugins, and AI; unknown or conflicting obligations fail closed. |
| Optional interchange and discovery directions were ambiguous | Make `0.54.1` choose exactly one SCIM direction, make `0.71.2` ingestion-only, add conditional concrete CMDB connector `0.86.1`, and require `0.140.9` to support or defer SCIM direction, STIX publication, SIEM/detection, and CMDB profiles explicitly. |
| Atomic command bundles did not protect inbox/timer/activity effects | Add `0.17.1` so inbox receipt and emitted effects commit together, then finalize the `0.18.2` discriminated command/consumer/timer/activity/poison family after fencing and quota semantics exist. Extend `0.22.0` destructive adapters and Phase G/HA evidence across every variant. |
| Tenant lifecycle could omit stores introduced by later phases | Add mandatory generated `TenantDataSurface` registry at `0.51.2`, inventory all earlier surfaces, require every later surface to register lifecycle behavior before exit, and block closure until every store/cache/index/backup/external copy has disposition evidence. |
| Workload authentication blurred OAuth server and resource-server roles | Make `0.52.1` an external-issuer OAuth resource-server profile only: Vitheim validates access tokens and mappings but stores no client private keys and exposes no token endpoint. Keep local agent enrollment separate at `0.119.0`; any future OAuth authorization server needs its own milestone. |
| Measurement downsampling could destroy historical authority | Add `0.38.3`: retain raw observations through supported raw windows and, before expiry, commit immutable integrity-bound authoritative rollups with completeness, watermark, manifest digest, method/version/fidelity, corrections, and admissible SLO windows. Missing authority yields unknown, never a mutable approximation. |
| Plugin plans disagreed about plaintext secrets | Strengthen `0.28.3`, `0.114.0`, `0.117.0`, and `0.140.4` with one invariant: plaintext credentials never enter Wasm guest memory. Use opaque handles and host-brokered authenticated HTTP/signing/token/certificate operations; raw-secret integrations are separately isolated hosted profiles outside the plugin claim. |
| External identity linking remained underspecified | Strengthen `0.52.0` with issuer-plus-immutable-subject link identity, no email/name auto-linking, controlled JIT, OIDC/SAML/SCIM correlation, reviewed merge/split/unlink/IdP migration, recreation/race protection, provenance, and audit. |
| Atomic work wording could imply multi-aggregate or distributed transactions | Refine `0.17.1–0.18.2`: every bundle advances at most one authoritative aggregate stream and commits only local durable effects. Cross-aggregate continuation is outbox/process-manager work; timer dispatch and result are distinct transitions; network/provider execution is explicitly at least once and returns through a later bundle. |
| External client authentication did not prove token sender constraint | Refine `0.52.1`, `0.56.0`, `0.60.0`, `0.117.0`, `0.119.0`, and `0.140.3`: privileged workload tokens require an admitted sender-constrained request profile; any bearer profile is short-lived, narrow-audience, visibly lower assurance, and non-privileged. Replay caches do not prevent first use of stolen bearer tokens. |
| External-copy disposition evidence overstated certainty | Refine `0.51.2`, federation, `0.140.5`, and production gates with typed locally verified deletion, controlled-key erasure, provider-attested deletion, unconfirmed request, and unverifiable disclosed-plaintext states. Closure policy evaluates them without relabeling attestations or uncertainty as cryptographic proof. |
| Raw measurements could expire before rollup authority was externally durable | Refine `0.38.3` and recovery gates so rollup-substitution expiry requires committed rollup/manifest, verified integrity chain, inclusion in a retrievable external signed checkpoint, and successful restore/reverification evidence; lag or missing proof blocks authority promotion visibly. |
| Rollup preservation could obstruct mandatory deletion | Refine `0.38.3`, `0.51.2`, `0.140.5`, and production gates: full proof remains mandatory for rollup-substitution expiry, while mandatory erasure/maximum-retention/closure deletion proceeds despite missing proof. Independently decide rollup and related-surface retention/erasure, mark affected history unknown, and record allowed authority-loss evidence immutably. |
| At-least-once remote work lacked an ambiguous-outcome contract | Refine `0.16.0–0.18.2`, `0.30.1`, `0.66.0`, `0.70.0`, and `0.113.0` with stable effect identity/request digest, provider idempotency scope/retention, separate execution/outcome/resolution/manual/compensation types, query/retry declarations, and no blind retry of unknown privileged or non-compensable effects. Local atomicity prevents duplicate protected commits, not every possible remote execution. |
| The future tenant-surface registry appeared retroactively enforceable | Place a neutral lifecycle descriptor schema inward at `0.8.1`; make `0.51.2` backfill and validate all earlier surfaces, then activate its compile/registration gate prospectively. Earlier crates never depend outward on Phase F solely to register. |
| Effect lifecycle, provider truth, resolution evidence, manual work, and compensation were collapsed | Refine `0.16.0`, `0.18.2`, `0.66.0`, `0.70.0`, `0.113.0`, and production gates with separately typed models, admissible resolution sources/evidence strength, deadlines/escalation, fenced late-evidence/manual races, and privileged resolver authorization. Operator assessment never becomes verified provider outcome. |
| The `0.66.0` compensation deliverables were textually truncated | Restore the complete milestone with compensation IR/state-machine/codecs, immutable original-to-compensation linkage, separate outcome/evidence/quota records, current authorization, reconciliation/escalation, operator view, and deterministic race fixtures. Add a semantic checker anchor so a present-but-truncated `Deliverables:` label cannot pass again. |
| Delayed external effects lacked an authorization-freshness decision | Require authorization at intent commit with immutable tenant/initiator/delegation/capability/target/purpose/request/policy/assurance bindings. A reviewed immutable low-risk capability may be `CommitBound`; privileged, destructive, secret-bearing, containment, and compensation effects are always `CommitAndDispatch` with a fenced single-use current-authority receipt. Apply revocation, stale assurance, confused-deputy, and target-substitution proof at `0.16.0`, `0.18.2`, `0.58.0–0.60.0`, `0.70.0`, `0.113.0`, and HA/production; freeze authentication, plugin-broker, and deployment implementations at `0.140.3`, `0.140.4`, and `0.140.6` without making the invariant optional. |
| Quota disposition was not bound to ambiguous external-effect outcomes | Refine `0.18.1–0.18.2`, `0.30.1`, `0.66.0`, `0.70.0`, and `0.146.0` with typed claim lifecycles and declared settlement boundaries. Provider-dependent claims remain charged/held for unknown outcomes; eligible refunds remain exactly once and evidence-backed; assessment cannot invent proof; compensation is separate; and audited non-borrowable recovery capacity protects reconciliation/security cleanup. `0.140.4` and `0.140.6` freeze implementations/capacity profiles, never permission to omit the control. |
| Scheduled non-interactive work incorrectly depended on a live initiating session | Add `EffectExecutionAuthority` with `LiveSubjectAuthority`, immutable approval-created `ApprovedExecutionGrant`, and narrowly scoped `ServicePrincipalAuthority`. Grants bind tenant/effect/request/target version, purpose, approvers/quorum/separation, assurance at approval, window, attempts, policy, and revocation; workers authenticate as themselves and redeem rather than impersonate. Session expiry alone does not revoke a grant; target drift, explicit revocation, tenant suspension, exhaustion, and required approver/policy revalidation fail closed. Carry issuance/redemption/revocation through `0.16.0`, `0.18.2`, `0.46.0`, `0.58.0–0.60.0`, `0.63.0–0.64.0`, `0.70.0`, HA/profile freezes, and production acceptance. |
| One unknown-outcome quota rule conflated concurrency, rate, cost, and storage | Replace the singular effect reservation with a bounded atomic `QuotaClaimSet` whose typed concurrency, consumable-operation, provider-rate, estimated-liability, and retained-byte claims settle at independent lease/admission/dispatch/transmission/storage boundaries. Administrative write-off stays distinct from provider refund evidence. Partition recovery capacity by tenant/work class with ceilings, global fair share, starvation bounds, and a scoped emergency reserve so one tenant cannot monopolize reconciliation. Carry this through `0.18.1–0.18.2`, `0.30.1`, `0.66.0`, `0.70.0`, `0.140.4`, `0.140.6`, `0.143.0`, `0.146.0`, and production acceptance. |
| Execution-grant lifecycle ownership was ambiguous under the one-stream law | Give every grant lineage exactly one authoritative owner. Inline grants share the approval aggregate stream; dedicated grants are created later in one lineage stream from an immutable approval receipt through an idempotent outbox/process manager. Stable lineage/generation uniqueness lets pre-issuance revocation defeat delayed issuance; successor creation atomically and permanently supersedes the predecessor in that owner stream. Apply crash/reorder/duplicate/fork proof at `0.18.2`, `0.22.0`, `0.24.0`, `0.30.1`, `0.46.0`, `0.63.0`, `0.70.0`, `0.140.6`, `0.143.0`, `0.145.0`, and production acceptance. |
| Atomic quota sets lacked a linearization and recovery model | Declare quota ledgers/claim sets co-transactional local authority, never additional aggregate streams. Reserve every normalized claim all-or-none under canonical deadlock-free resource ordering, bind the immutable ordered set to `QuotaClaimSetId`/digest/opaque token, and make bundles transition that exact pre-reserved set without member reacquisition. Key transitions by set and claim identity; restore/reconcile a complete verified set or quarantine it, never reconstruct part. Apply overlap/deadlock/partial-crash/failover/corruption proof at `0.18.1–0.18.2`, `0.22.0`, `0.24.0`, `0.30.1`, `0.70.0`, `0.140.6`, `0.143.0`, `0.145.0`, `0.146.0`, and production acceptance. |
| Dedicated-grant redemption could still require two authoritative streams | Freeze the preferred local-authority model: lineage-owner issuance/revocation/successor transactions create, fence, or replace a co-located `GrantRedemptionGuard`; dispatch advances only the effect stream while atomically compare-and-claiming one stable attempt and recording its receipt/outbox. The guard serializes revocation/final-attempt races, retry reuses the committed claim, and restore cannot resurrect consumption. Reject eventual guard projection, non-co-location, and grant-plus-effect stream mutation at `0.18.2`, `0.21.0–0.24.0`, `0.30.1`, `0.58.0`, `0.60.0`, `0.64.0`, `0.70.0`, `0.140.2`, `0.140.6`, `0.143.0`, `0.145.0`, and production acceptance. |
| Exact quota sets lacked an explicit sharding/regional topology | Co-locate every claim set with its work bundle in one local quota partition. Represent global/regional limits as fenced hierarchical capacity leases allocated to local partitions in separate transactions, with parent/child conservation and safe expiry/reclamation/failover. Never open a cross-shard/region distributed work transaction. `1.0.0` supports authoritative-region writes with fenced failover and rejects active/active authoritative multi-region writes. Apply at `0.18.1–0.18.2`, `0.21.0–0.24.0`, `0.30.1`, `0.70.0`, `0.140.2`, `0.140.6`, `0.143.0–0.146.0`, and production acceptance. |
| Non-grant authority rechecks still had a read-before-dispatch TOCTOU window | Add a bounded canonical `DispatchAuthorityFenceSet` of applicable monotonic local tenant, subject/principal, session/credential/mapping, delegation, group/role/relationship, and policy epochs. Each owner command increments its epoch with the event; dispatch locks the complete set while committing effect/receipt/outbox/grant/quota local state. External-only bounded-stale facts cannot authorize privileged effects. Apply at `0.18.2`, `0.21.0–0.24.0`, `0.30.1`, `0.51.1–0.60.0`, `0.64.0`, `0.70.0`, `0.140.3`, `0.140.6`, `0.143.0`, `0.145.0`, and production acceptance. |
| Hierarchical quota-lease expiry could release capacity still encumbered by claims | Bind every lease to quota kind/unit/period/settlement. Expiry stops new reservations but retains spent/encumbered capacity; parents reclaim only proven free remainder. Retained bytes, unknown liabilities, charged operations, spent tokens, and other live claims settle against their original encumbrance or a receipt-idempotent fenced successor transfer over at-least-once delivery. Prove child loss, late evidence, duplicate transfer, and reclamation/failover races at `0.18.1–0.18.2`, `0.22.0`, `0.24.0`, `0.30.1`, `0.70.0`, `0.140.6`, `0.143.0`, `0.145.0`, `0.146.0`, and production acceptance. |
| Composite work transactions lacked one cross-component acquisition and retry law | Acquire authoritative stream head, typed authority fences, target fence, remote-mutation-exception guard, grant guard, quota lease/keys, uniqueness claims, then receipts, with canonical ordering inside each class. Retry only classified serialization/deadlock failures under a bounded policy that preserves command/claim/transition identity, digest, expected version, and fence epochs and never repeats provider I/O. Apply at `0.18.2`, `0.21.0–0.24.0`, `0.30.1`, `0.60.0`, `0.64.0`, `0.70.0`, `0.140.6`, `0.143.0`, `0.146.0`, and production acceptance. |
| Exact target bindings could race target deletion, merge, migration, or supersession when effect and target aggregates differ | Add typed `DispatchTargetFence`. Same-aggregate targets use expected stream version/digest; different local target owners update an authoritative co-located row beside target events, binding tenant, kind/ID, version/digest, lifecycle, and never-reused deletion/supersession epoch. Dispatch locks it after authority fences without advancing the target stream. Reject remote, cross-shard, or projection-only current-target semantics. Apply at `0.18.2`, `0.21.0–0.24.0`, `0.30.1`, `0.58.0`, `0.60.0`, `0.64.0`, `0.70.0`, `0.87.0`, `0.140.2`, `0.140.6`, `0.143.0–0.146.0`, and production acceptance. |
| Fenced cross-partition capacity transfer sounded like distributed exactly once | Define `QuotaCapacityTransferState` as a durable outbox/inbox process manager from parent reservation through in-transit, child activation, return, and reclaim, with stable identity, source/destination epochs, digest, sequence, authenticated acknowledgement, old-child fence proof, receipts, and original claim/transfer lineage. Local transitions are exactly once by receipt; delivery is at least once. Uncertainty remains conservatively charged and double-entry recovery may overcharge but never frees capacity at both ends. Apply at `0.18.1–0.18.2`, `0.21.0–0.24.0`, `0.30.1`, `0.70.0`, `0.140.2`, `0.140.6`, `0.143.0–0.146.0`, and production acceptance. |
| Provider-owned mutable targets cannot use a local target fence | Keep remote state separate from `DispatchTargetFence` and add `RemoteTargetConcurrencyProfile`: not applicable, admitted conditional mutation, narrowly reviewed unconditional mutation, or unsupported. Conditional intent binds provider/account/resource, exact validator/strength/provenance/time, provider capability/version, request digest, and idempotency key. Precondition failure is typed non-acceptance; workers never refresh silently, weak/ABA/downgraded conditions fail privileged use, and response loss remains unknown. Apply at `0.16.0`, `0.18.2`, `0.21.0–0.22.0`, `0.30.1`, `0.48.2`, `0.58.0–0.60.0`, `0.64.0`, `0.70.0`, `0.113.0`, `0.117.0`, `0.119.0`, `0.140.4`, `0.140.6`, `0.143.0–0.146.0`, and production acceptance. |
| Capacity could be reclassified while a transfer still conserved amount | Bind transfer to tenant/global accounting owner, hierarchy root/parent lease, accounting period, work/recovery lane, business/reconciliation/security-cleanup/emergency capacity class, residency/region, and source/destination authorization decisions. Ordinary transfer cannot change them; destination acknowledgement is authorized for the exact hierarchy. Existing capacity is class-immutable. Apply at `0.18.1–0.18.2`, `0.21.0–0.24.0`, `0.30.1`, `0.70.0`, `0.140.6`, `0.143.0–0.146.0`, and production acceptance. |
| Reviewed unconditional mutation lacked revocable dispatch authority | Make `UnconditionalReviewed` reference one exact `RemoteMutationException`: a one-owner, scope-bound, approval/quorum/separation, policy/provider-versioned, time- and attempt-bounded authority. Its co-located guard serializes revocation, expiry, supersession, provider-capability change, and final-attempt claims while dispatch advances only the effect stream. Retry reuses the claim receipt and restore cannot resurrect authority. Apply at `0.16.0`, `0.18.2`, `0.21.0–0.24.0`, `0.30.1`, `0.48.2`, `0.58.0–0.60.0`, `0.64.0`, `0.70.0`, `0.113.0`, `0.117.0`, `0.119.0`, `0.140.4`, `0.140.6`, `0.143.0–0.146.0`, and production acceptance. |
| Cross-class adjustment remained a protected-capacity escape hatch | Forbid every class change to an existing lease, reservation, claim, transfer, or encumbrance regardless of privilege, including emergency/security-cleanup/reconciliation to business. Resize only future proven-unallocated parent capacity through a versioned control-plane `QuotaCapacityPolicy` with separation of duties, impact simulation, protected reserve floors, monotonic epochs, and no tenant-work entry point. Each delayed transfer step rechecks current local tenant/principal/policy epochs. Apply at `0.18.1–0.18.2`, `0.21.0–0.24.0`, `0.30.1`, `0.58.0–0.60.0`, `0.70.0`, `0.140.4`, `0.140.6`, `0.143.0–0.146.0`, and production acceptance. |
| Redeemed dispatch authority could remain usable indefinitely before provider transmission | Persist `redeemed_at` and immutable `transmit_before`, bound to the effect attempt, worker/service audience, provider/account/request, and admitted authority/target/exception/capability epochs. Derive the deadline from the earliest validity bound plus a platform maximum. Immediately before I/O, atomically recheck current fences and claim one short monotonic start permit. Definite expiry requires fresh authority; uncertainty after that claim is `OutcomeUnknown` and cannot be ordinarily retransmitted. Restore, failover, pause, or clock rollback cannot extend it. Apply at `0.18.2`, `0.21.0–0.24.0`, `0.30.1`, `0.48.2`, `0.58.0–0.60.0`, `0.64.0`, `0.70.0`, `0.113.0`, `0.117.0`, `0.119.0`, `0.140.1–0.140.4`, `0.140.6`, `0.143.0–0.146.0`, and production acceptance. |
| `QuotaCapacityPolicy` activation lacked a single atomic owner and safe multi-parent rollout | Scope each policy lineage to exactly one parent-capacity authority. In one local transaction, append its event, CAS-update the co-located parent ledger, validate base policy epoch, parent epoch/high-watermark, exact deltas and simulation digest against an independently governed floor-set version, record audit, and create outbox intents. The policy cannot lower its own floor. Multi-parent change is a receipt-idempotent process-manager rollout whose partial state applies the conservative per-class intersection of old and new limits; rollback is a new version. Apply at `0.18.1–0.18.2`, `0.21.0–0.24.0`, `0.30.1`, `0.58.0–0.60.0`, `0.70.0`, `0.140.2`, `0.140.6`, `0.143.0–0.146.0`, and production acceptance. |
| A shared service audience could let multiple replicas obtain one transmission permit | Add a globally unique `TransmissionStartClaimId` and bind the CAS to one exact authenticated worker instance, work-lease generation/fence, receipt/effect attempt, service audience, and permit digest. Return opaque non-persisted permit material exactly once; every replay returns status. Ambiguous claim response/permit delivery and any replacement worker observing a claimed receipt enter `OutcomeUnknown` reconciliation. Backup or restore cannot reconstruct permit authority. Apply at `0.18.2`, `0.21.0–0.24.0`, `0.30.1`, `0.48.2`, `0.58.0–0.60.0`, `0.64.0`, `0.70.0`, `0.113.0`, `0.117.0`, `0.119.0`, `0.140.2–0.140.4`, `0.140.6`, `0.143.0–0.146.0`, and production acceptance. |
| Multi-parent policy rollout lacked an authoritative complete parent set | Give the hierarchy root one `QuotaCapacityRolloutRoot` authority that freezes a canonical manifest/digest of the root membership epoch, exact unique parent IDs/generations/regions/classes/periods, and per-class conservation totals. Every member prepares a receipt bound to rollout plus manifest. Finalization CAS-validates the unchanged root epoch and exactly one preparation for every member; parent activation accepts only the finalized manifest. Add/remove/reparent/generation changes invalidate preparation, and coordinator failover cannot discover or omit parents. Apply at `0.18.1–0.18.2`, `0.21.0–0.24.0`, `0.30.1`, `0.58.0–0.60.0`, `0.70.0`, `0.140.2`, `0.140.6`, `0.143.0–0.146.0`, and production acceptance. |
| Protected-floor reduction lacked governance separate from spending released capacity | Give `QuotaProtectedFloorSet` an append-only one-owner history and separate `ManageQuotaProtectedFloors` capability, quorum, risk owner, and approval lineage. Recheck tenant/hierarchy/incident/emergency/policy fences, simulate outstanding protected obligations, and enforce non-configurable platform minima. A `FloorReductionReceipt` binds released capacity; any policy spending it must prove that actor, approver, risk owner, and approval lineage are disjoint from the reduction. Epochs cannot be reused by rollback/restore. Apply at `0.18.1–0.18.2`, `0.21.0–0.24.0`, `0.30.1`, `0.58.0–0.60.0`, `0.70.0`, `0.140.2`, `0.140.6`, `0.143.0–0.146.0`, and production acceptance. |
| Root finalization could authorize stale parent preparation state | Treat `RolloutFinalized` only as permission to attempt activation. Every parent then locks its exact `Prepared` state and locally CAS-revalidates finalized root generation/manifest, parent ledger epoch/high-watermark/proven unallocated capacity, floor profile/set, protected obligations, and current tenant/hierarchy/incident/emergency/principal/policy fences. Drift preserves the conservative intersection as `ActivationBlocked` or `ReconciliationRequired`; stale deltas never apply. Apply at `0.18.1–0.18.2`, `0.21.0–0.24.0`, `0.30.1`, `0.58.0–0.60.0`, `0.70.0`, `0.140.2`, `0.140.6`, `0.143.0–0.146.0`, `0.150.0`, and production acceptance. |
| A non-serializable transmission permit had no valid split-service ownership boundary | Make a trusted `TransmissionExecutor` own both `ClaimTransmissionStart` and the provider socket. Upstream/split workers send immutable authenticated instructions and receive status; permit material never crosses RPC, IPC, queues, logs, or caller memory. The sealed non-`Clone`, non-serializable permit is consumed by value and best-effort zeroized; its persisted digest is evidence only. Duplicate instruction, response loss, executor failover, or stale process enters reconciliation. Transferable capabilities are unsupported for `1.0.0` and require a future explicit security milestone. Apply at `0.18.2`, `0.21.0–0.24.0`, `0.30.1`, `0.48.2`, `0.58.0–0.60.0`, `0.64.0`, `0.70.0`, `0.113.0`, `0.117.0`, `0.119.0`, `0.140.1–0.140.4`, `0.140.6`, `0.142.0–0.143.0`, `0.145.0–0.147.0`, `0.150.0`, and production acceptance. |
| `PlatformSafetyFloor` lacked upgrade, downgrade, mixed-version, and restore semantics | Give every compiled floor a stable profile ID/version/canonical digest and persist a never-reused per-class admitted minimum epoch/high-watermark. Nodes advertising less reject startup; mixed versions enforce the stricter profile; a higher floor uses a governed conservative capacity migration. Downgrade, rollback, lower software defaults, failover, backup, and restore never release capacity or lower the ratchet. Lowering the durable minimum is outside ordinary configuration and requires a future explicit security-design milestone. Apply at `0.18.1–0.18.2`, `0.21.0–0.24.0`, `0.29.0–0.30.1`, `0.58.0–0.60.0`, `0.70.0`, `0.140.2`, `0.140.6`, `0.141.0`, `0.143.0`, `0.145.0–0.148.0`, `0.150.0`, and production acceptance. |
| Mandatory deletion did not independently cover rollup-derived surfaces | Refine `0.38.3`, `0.51.2`, `0.140.5`, `0.145.0`, and `1.0.0`: raw observations, rollup payload, source manifest, derived result, cache/projection, export, and linkable checkpoint metadata each receive their own disposition. Retain a non-authoritative rollup only when independently permitted; otherwise erase covered surfaces and retain only allowed non-sensitive tombstone/authority-loss evidence. |
| Phase G lost network-call and crash-point verification text | Restore the `0.70.0` network-call-in-transaction rejection, crash-point, and stale-fencing cases, then make the implementation-plan checker require that semantic anchor alongside the new effect-model and derived-surface anchors. |
| The trusted executor still had broad credential and egress authority | Add immutable versioned `ProviderExecutionProfile` admission: executor instances have no master-key ring or general database writes, redeem only exact claim/tenant/provider/account/action/request/destination-bound opaque secret operations, use least-privilege short-lived credentials where supported, and enforce destination allowlists, TLS identity, DNS-rebinding/redirect policy, and socket ownership without becoming a general proxy. Partition pools by tenant/account or an explicitly bounded trust domain, reject unrestricted credentials shared across unrelated tenants, and document residual provider-specific blast radius. Prove arbitrary unclaimed socket use, handle/account substitution, cross-tenant compromise, and egress bypass from `0.18.2` through storage, queues, HA, workflows, plugins/agents, profile decisions, Phase O, and production acceptance. |
| Partial capacity rollout rollback and supersession lacked root-level linearization | Give every `QuotaCapacityRolloutRoot` one monotonic `ActiveRolloutGeneration`. A successor atomically and permanently supersedes the prior generation; parent activation revalidates that it is still active, rollback is a complete successor rollout over current manifest and actual limits, blocked parents remain conservative until successor preparation or explicit reconciliation, late superseded messages fail, and restore cannot reactivate old authority. Distinguish cancellation before finalization from supersession after any activation with separate typed states. Carry concurrency, late-message, blocked-parent, rollback, and restore tests through `0.18.1`, storage/queue/HA/workflow milestones, `0.140.2`/`0.140.6`, `0.143.0`–`0.150.0`, and production acceptance. |
| `PlatformSafetyFloor` keys could be reinterpreted across versions | Replace scalar/per-class interpretation with a complete `PlatformSafetyFloorKey` binding accounting owner and hierarchy root, `QuotaKind`, unit/scale, period semantics, capacity class/lane, region/residency, and settlement-policy version. Compare high-watermarks only within an identical key. Any key-set change requires a total, explicit, overflow-checked, non-lossy old-to-new mapping; missing, duplicate, incompatible, lossy, scope-changing, or rounding-dependent mappings fail startup. Prove kind/unit/scale, daily/hourly, lane/class, region, settlement-version, omission, overflow, rolling-upgrade, rollback, and mixed-profile cases through `0.18.1`, migrations/adapters, conformance/HA, profile freezes, Phase O, and production acceptance. |
| Provider execution profiles and credentials lacked revocable authority epochs | Give each admitted scope one authoritative `ProviderExecutionProfileLineage` with active/suspended/superseded/revoked generations and a never-reused profile epoch. Include provider-account lifecycle, credential-lineage/version, and broker-policy epochs in `ClaimTransmissionStart` and scoped handle redemption. Local successor activation atomically makes the predecessor non-redeemable; restore cannot resurrect an old profile, credential, policy, or handle. Revocation before redemption denies, while redemption first admits only that already bounded attempt. Prove emergency revocation, account suspension, secret rotation, credential ABA, stale queued instructions, broker-policy change, and restored handles from `0.18.2` through storage/queue/HA/workflows/plugins/agents, pre-production freezes, Phase O, and production acceptance. |
| The no-plaintext executor claim contradicted bearer-token socket ownership | Define explicit `ProviderCredentialOperationProfile` values. Non-exportable signing/mTLS/HSM profiles expose only bounded operations. For bearer tokens/API keys, the hardened credential broker itself owns authorization-header serialization, redirects, TLS, `ClaimTransmissionStart`, and the provider socket and therefore joins the `TransmissionExecutor` TCB. Bearer bytes may briefly exist in that broker memory but never in upstream workers, Wasm guests, general connector code, RPC/queues, durable state, logs, diagnostics, or crash artifacts. A separate broker process—not its caller—claims and writes; providers requiring export to a general connector are unsupported. Carry HTTP/TLS/redirect/diagnostic/crash/core-dump/swap memory canaries and honest residual limitations through `0.18.2`, `0.28.3`, hosted adapters/workers, plugin/agent milestones, `0.140.1`/`0.140.4`/`0.140.6`, `0.142.0`–`0.150.0`, and production acceptance. |
| Pre-finalization cancellation could strand prepared parent capacity | Cancellation before any preparation terminates directly. Once any parent has prepared, cancellation atomically creates exactly one root-owned `CancellationRecoveryGeneration` over the complete manifest and actual effective parent limits. Prepared parents stay at the conservative intersection, never restore independently, and recover only through current ledger/floor/obligation/authority rechecks. Recovery receipts are idempotent and restore-safe; late cancelled-generation preparation fails; a bounded operational deadline escalates stuck cancelled/prepared parents. Prove cancellation after every preparation point, lost delivery, coordinator failover, concurrent successor creation, parent drift, and restore through `0.18.1`, storage/migration/queue/HA/workflow milestones, `0.140.2`/`0.140.6`, Phase O, and production acceptance. |
| Provider execution profile creation and activation lacked privileged governance | Make proposal, approval, activation, suspension, revocation, and supersession distinct typed control-plane commands on the authoritative lineage. Bind signed implementation admission, exact digest, risk owner, required quorum/separation, and semantic diff; every credential/destination/redirect/trust-domain/residual-radius expansion requires new approval. Activation rechecks current tenant/account/policy/approver fences and a pre-activation revocation tombstone before atomically changing active generation/epoch. Emergency revocation removes authority but cannot activate a replacement. Prove unauthorized/self-approved activation, hidden expansion, stale approver/policy/account fences, delayed activation after revocation, and emergency-replacement escalation from `0.18.2` through persistence/queues/authorization/workflows/plugins/agents, `0.140.4`/`0.140.6`, Phase O, pentest, and production acceptance. |
| Remote credential rotation was incorrectly described as wholly atomic | Define one authoritative `ProviderCredentialRotationState` from proposal through provisioning, verification, atomic local activation, provider revocation pending, and completion, with fail-closed unknown/failed/manual states. Verify new provider identity/account/effective permissions before local activation; local activation advances the epoch and permanently denies predecessor redemption. Provider creation/revocation response loss is `OutcomeUnknown` and reconciles without blind retry; retain old identity/evidence until confirmed revocation, bound overlap and escalation, require an outage profile for single-credential providers, and forbid restore from making both generations locally redeemable. Prove every crash point, duplicate creation, lost response, eventual consistency, continued old-key validity, rollback, and restore from `0.18.2` through secret adapters/migrations/queues/HA/connectors/agents, the option freezes, Phase O, pentest, and production acceptance. |
| Out-of-band provider permission changes bypassed the local credential epoch | Give each credential generation a versioned `ProviderCredentialCapabilitySnapshot` binding provider/account/principal, requested and observed effective permissions, role/group/trust provenance, provider policy revision/validator, observation source/time/freshness, admitted profile/credential generation, and never-reused local capability epoch. Authenticated event or bounded-poll reconciliation advances the epoch; privileged transmission rejects stale, unverifiable, insufficient, broader, wrong-revision, or restored snapshots and never performs remote permission discovery inside its transaction. Prove expansion/reduction, role/group/cross-account changes, callback reorder, polling staleness, IAM eventual consistency, and restore from `0.18.2` through storage/queue/HA/workflows/plugins/agents, `0.140.4`/`0.140.6`, Phase O, pentest, and production acceptance. |
| Provider permission breadth had no canonical semantic comparison model | Require each provider adapter to supply a reviewed, versioned permission evaluator and conformance corpus. It returns exactly `Equal`, `StrictSubset`, `StrictSuperset`, `Incomparable`, or `Unknown`; unsupported constructs, ambiguous conditions, and expansion/work/depth-budget failure return `Unknown`. Snapshots bind raw policy and normalized AST digests, evaluator implementation/version, provider policy language/version, comparison result, and explanation evidence. Only equal or an explicitly admitted safe subset may operate. Prove wildcard/`NotAction`, explicit deny, resource/tag/time/network/identity/session conditions, boundaries, organization policy, inheritance, role/group/cross-account trust, evaluator downgrade, and complexity budgets from `0.18.2` through adapters/conformance/plugins/agents, `0.140.1`/`0.140.4`, Phase O, pentest, and production acceptance. |
| Concurrent credential rotations and remotely created orphans lacked serialization and recovery | Give every credential lineage one owner and a co-located `ProviderCredentialRotationGuard` allowing one non-terminal rotation. Bind stable rotation ID, intended successor generation, provider idempotency key, and request digest; unknown provisioning/revocation blocks ordinary successors. A separately authorized takeover inventories provider credentials. Remotely created unassociated credentials enter bounded discovered/quarantined/revocation-pending/unknown/manual orphan workflows, never become redeemable, and remain provider-credential-count quota charged until evidenced revocation. Late callbacks require current generation/digest. Keep state advancement and local activation in one lineage-owner transaction. Prove simultaneous requests, timeout-then-create, late callbacks, takeover, credential-count exhaustion, orphan revocation loss, and restore from `0.18.2` through secret adapters/storage/queues/HA/connectors/agents, option freezes, Phase O, pentest, and production acceptance. |
| Over-privileged or semantically uncertain credentials were denied only per operation | `StrictSuperset`, `Incomparable`, or `Unknown` atomically advances the capability epoch and enters `CredentialCapabilityQuarantined`. Every handle and queued instruction becomes non-redeemable and no provider operation—including nominally non-privileged work—may use the credential until reconciliation or an approved replacement. Emit a security incident binding protected raw evidence, evaluator version/explanation, and affected executions. Forbid automatic profile widening and break-glass promotion. `StrictSubset` enters distinct reduced-permission drift; continuation requires an explicit profile and proof the exact operation remains covered. Serialize quarantine with handle redemption and first credential use. Prove queued/claimed races, non-privileged actions, evaluator downgrade, provider inconsistency, restore, replay, and widening attempts from `0.18.2` through all effectful adapters/workflows/plugins/agents, `0.140.4`/`0.140.6`, Phase O, pentest, and production acceptance. |
| Permission evaluator versions were evidence but not governed authority | Give each provider one `ProviderPermissionEvaluatorLineage` with proposed/active/suspended/superseded/revoked generations, signed implementation admission, binary/corpus digests, exact provider/language support, and a never-reused epoch bound into snapshots/start claims. Activation or ordinary/emergency revocation advances the epoch and marks all older outputs `ReevaluationRequired`; credentials remain unusable until retained raw evidence is evaluated under the current generation and a new capability epoch commits. Nodes unable to execute or validate the active evaluator reject startup/readiness. Prove security-fix/semantic/corpus upgrades, mixed versions, downgrade, emergency revocation, partial reevaluation, and restore from `0.18.2` through adapters/migrations/queues/HA/plugins/agents, `0.140.2`/`.4`/`.6`, Phase O, pentest, and production acceptance. |
| Whole-credential quarantine lacked an authoritative exit | Add a credential-capability-owner state machine from quarantine through investigation, remediation, replacement/revalidation verification, and resolution. Resolution requires the current evaluator, fresh authenticated strong-revision provider evidence, equal or explicitly safe subset, current profile/account/credential/broker/evaluator epochs, incident/remediation evidence, a resolver separated from the widening actor, and repeated observations or provider consistency barrier. It creates a new capability generation and permanently tombstones old handles/receipts/queues/effect authority. Forbid generic flags, break-glass, snapshot rollback, incident-only closure, partial evidence, and restore clearing. Prove every transition and race from `0.18.2` through persistence/queues/authorization/workflows/extensions, option freezes, Phase O, pentest, and production acceptance. |
| Quarantining the sole administrative credential could deadlock recovery | Define `ProviderCredentialRemediationAuthority` as an independently admitted administrative credential or recovery channel scoped only to inventory/create/disable/revoke through rotation/takeover. Give it separate profile, lineage, approval, audit, epochs, egress, security-cleanup quota, and outcome reconciliation; forbid business operations, derivation/delegation from quarantine, direct resolution, or cross-tenant substitution. Provider-count exhaustion remains accounted. Providers without an independent path declare the limitation and enter manual intervention without break-glass reuse. Prove sole-key quarantine, remediation compromise, circular dependency, outage/response loss, substitution, count exhaustion, restore, and HA from `0.18.2` through adapters/workflows/extensions, `0.140.1`/`.4`/`.6`, Phase O, pentest, and production acceptance. |
| Authority-bearing invariants were distributed prose without executable ownership | Add `0.18.3` and `docs/INVARIANT_OWNERSHIP.md`: each stable ID has exactly one owner, owner-updated guard/authority row, milestone, transaction domain, enforcement points, semantic storage capabilities, P/N/M/F tests, and restore/migration duties. CI rejects absent/alternative owners, ownerless guards, untested enforcement, missing capabilities, and incomplete recovery. Adapters derive capability coverage at `0.21.0–0.22.0`; `0.140.2` freezes the production matrix; Phase O and `1.0.0` require zero missing or waived rows. |
| Evaluator invalidation could create an unbounded fleet-wide re-evaluation surge | Add `0.18.4` and `VIT-INV-008`: durable stable-generation tenant/provider/account jobs move through pending/fetch/evaluate/provider-blocked/completed/escalated states under leases/cursors, bounded concurrency/retry/provider-rate claims, global fair share, tenant ceilings, starvation bounds, privileged/near-term priority, and a non-borrowable cleanup lane. Old output remains unusable; stale evidence is fetched again; newer generations supersede older jobs. Carry storage conformance, HA, load, restore, `0.140.2`/`.6`, Phase O, pentest, and production proof for millions of snapshots, outages, hostile tenants, repeated replacement, failover, and starvation. |
| Independent remediation authority lacked a non-circular bootstrap and loss-recovery root | Add `0.18.5`, `VIT-INV-005`, and `VIT-INV-009`: signed first admission and recovery ceremony, separated requestor/approver/executor/risk-owner quorum, two independently administered channels for automatic recovery, KMS/secret/identity independence where practical, offline/manual recovery independent of the lost authority, no self-approval/remediation, monotonic compromise/loss/recovery epoch, monitoring, and periodic exercised evidence. Providers without the channels stay manual-only. Freeze choices at `0.140.1`/`.2`/`.6` and prove simultaneous business/remediation loss, compromise, circularity, provider-admin loss, KMS/channel outage, stale restore, and false exercises through adapters, Phase O, pentest, and production acceptance. |
| The canonical invariant registry covered only nine recent authorities | Expand `0.18.3` into a formal backfill through `0.18.2`: schema and event-schema activation, structural tenant scope, tenant-bound events, sensitive-payload lifecycle, aggregate state, one-stream atomic work, stream heads, audit intent, command receipts, outbox/inbox, lease fences, grants/redemption, authority/target fences, remote exceptions, quota claims/leases/transfers/policies/floors/platform admission/rollout roots, provider execution profiles, and remaining provider controls. Introducing phase markers drive coverage; every marker resolves to exactly one ownership and lifecycle row and every row resolves back. Define registry granularity as one row per independent ownership root so subordinate same-owner fields do not multiply rows while independently updated/restored state cannot hide. No hand-maintained minimum count remains. |
| The invariant checker validated shape but not claimed coverage or contract resolution | Give each invariant stable `VIT-ENF-*`, `VIT-CAP-*`, `VIT-TST-*`, `VIT-RCV-*`, and `VIT-FEN-*` IDs. Strengthen `0.18.3` and CI with bidirectional declaration/version checks, owner/guard shape, contract-ID resolution, P/N/M/F requirements, and lifecycle validation. Bind IDs to concrete enforcement/tests/recovery fields and storage-profile claims at `0.21.0–0.22.0`, owner migrations at `0.29.0`, freeze at `0.140.2`/`.6`, and require zero unresolved contracts at `1.0.0`. |
| Mass re-evaluation jobs lacked a durable complete-discovery root | Strengthen `0.18.4` with `VIT-INV-027`: evaluator epoch activation/revocation atomically creates an invalidation-campaign root binding old/new epochs and the requested authoritative membership generation. `VIT-INV-046` membership journals and `VIT-INV-047` scans produce a sealed shard manifest, one receipt per shard, explicit lifecycle dispositions, a final membership barrier, and zero-unexplained-mismatch reconciliation. Prove every activation/mutation/journal/move/seal/scan/receipt/barrier/job/supersession crash and carry the proof through storage, migration, HA, load, restore, option freezes, Phase O, pentest, and production acceptance. |
| Invariant supersession policy was prose-only | Add a machine-readable lifecycle companion registry at `0.18.3` with status, symmetric predecessor/successor IDs, effective version, stable old/new owner fence, mixed-version behavior, migration contract, and rollback floor. Active/superseded state must agree, history remains append-only, and owner splits/transfers require a new ID. Enforce in CI, storage conformance, `0.29.0` migrations, `0.140.2`/`.6`, Phase O, and `1.0.0`; complete the machinery before the first real transfer. |
| Ownership rows still combined independent roots and cross-owner laws | Split independently updated tenant/principal/session/delegation/role/relationship/policy epochs, local safety-floor ratchets, transfer source/destination, membership shards, and campaign scans into distinct `VIT-INV-*` roots. Register cross-owner dispatch, floor, conservation, campaign-completeness, and atomic-work guarantees as `VIT-LAW-*` records with one proof coordinator, explicit contributors/local linearization points/failure state/recovery proof, never a fictional shared owner. Carry the distinction through `0.18.3`, storage/conformance/migration, authorization, option freezes, Phase O, and `1.0.0`. |
| Invariant coverage and lifecycle checks could miss later documents or unsafe graphs | Make the checker scan all `docs/implementation/*.md` by default, including production and future phases. Explicitly bind one append-stable semantic enforcement ID to its negative-test ID and active/retired status; textual ordering is irrelevant. Reject missing law declarations/lifecycle/dependencies, duplicate child IDs, cycles, non-increasing successors, rollback floors predating predecessor activation, and inconsistent active/superseded/retired semantics. Realize every child at `0.22.0`, exercise migrations at `0.29.0`, freeze at `0.140.2`/`.6`, and require zero unresolved contracts at `1.0.0`. |
| Campaign completeness trusted a projection and underspecified shard membership | Replace projection authority with `VIT-INV-046` append-only membership shard journals and `VIT-INV-047` fenced campaign-shard scans. Capability changes atomically emit membership intents; cross-shard moves use source tombstone/destination lineage/handoff fencing; campaigns bind sealed shard generations/high-watermarks and require exactly one receipt per manifest shard, a final membership barrier, and continuous capability-owner mismatch reconciliation. Search/index state is accelerator-only. Prove every mutation, move, seal, cursor, receipt, barrier, late-arrival, restore, and successor crash from `0.18.4` through storage, migration, HA/load, `0.140.2`/`.6`, Phase O, pentest, and `1.0.0`. |
| Membership completeness could not prove source-side intent delivery | Add `VIT-INV-056` and strengthen `VIT-LAW-004` at `0.18.4`: bind a canonical capability-owner source-partition/topology manifest, monotonic source outbox sequences and campaign high-watermarks, exact source-sequence/digest destination receipts, and proof that every sequence is received or lineage-superseded. Fence source create/split/merge/move/retire and expose `MembershipDeliveryBlocked`; capability mismatch remains an independent backstop. Carry source/destination placement and RPO through `0.21.0–0.29.0`, option freezes, Phase O, pentest, and `1.0.0`. |
| Composite laws lacked declarations and lifecycle governance | Give every `VIT-LAW-*` an introducing marker plus lifecycle row with effective version, status, symmetric supersession, versioned dependency set, proof fence, mixed-version behavior, migration, rollback floor, and versioned recovery proof. The `0.18.3` checker enforces bidirectional coverage, acyclicity, version ordering, and status semantics; contributor or linearization changes are migrations. Freeze/realize the law contracts at `0.22.0`, `0.29.0`, `0.140.2`/`.6`, Phase O, and `1.0.0`. |
| Identity and provider epoch rows still combined independently updated roots | Keep `VIT-INV-038` for interactive sessions, `VIT-INV-040` for role assignments, and `VIT-INV-028` for execution profiles only. Add external-identity mapping (`0.52.0`), workload mapping (`0.52.1`), authenticator credential (`0.53.1`), role definition/group membership (`0.55.0`), and provider account/credential/broker-policy (`0.18.2`) roots. Dispatch and transmission laws reference the exact applicable roots; storage, restore, and mixed-version gates cannot substitute one combined epoch. |
| Transmission-start checks had no complete registered law | Add declared/lifecycle-governed `VIT-LAW-006` at `0.18.2`, coordinated only by `VIT-INV-006`, over dispatch/underlying authority roots, grant or exception guard, target, provider profile/account/credential/broker state, capability/evaluator/quarantine, lease/claimant, deadline/time, and quota boundary. Pre-claim validation/CAS failure is `DefinitelyNotStarted`; ambiguous claim/permit delivery is `OutcomeUnknown`; a committed claim is `StartClaimedReconciling`. The last two reconcile without ordinary retransmission. Carry through adapter conformance, authorization, HA, options, Phase O, and production acceptance. |
| Latest composite-law dependency sets retroactively imported future authority roots | Add canonical `docs/LAW_GENERATIONS.md` at `0.18.3`. Each generation records its effective milestone, predecessor, actual coordinator, exact dependency delta, canonical linearization/failure/recovery semantics, mixed-version intersection, activation fence, migration, rollback floor, dependency contract, and recovery contract. The latest ownership-registry row becomes only a resolved view. CI rejects a dependency whose invariant is not yet effective, skipped predecessors, broken contract labels, or drift between the latest generation and current view. Storage conformance at `0.22.0`, migrations at `0.29.0`, identity expansions at `0.51.1–0.59.0`, option freezes, Phase O, and `1.0.0` consume the exact generation rather than a flattened law. |
| Later implementation phases could omit authority declarations without detection | Add `docs/AUTHORITY_REVIEWS.md` and derive its coverage from every implementation milestone after `0.18.3`, including all option decisions and `1.0.0`. Exactly one disposition is required: exact declarations, exact existing roots/laws extended, reviewed `none`, or a conservative stable proposal. Proposals are planning placeholders only and must resolve before implementation; option decisions require zero proposals before Phase O, and production requires zero proposals repository-wide. CI rejects missing, duplicate, orphan, malformed, or declaration-mismatched reviews. |
| Law contract labels did not prove what coordinator, dependencies, semantics, or recovery they represented | Bind dependency and recovery labels inside the canonical generation row beside the exact coordinator, dependency delta, canonical semantics, activation/migration/rollback fields, and predecessor. Generation-numbered IDs are append-only; the checker derives the resolved set, validates effective-version ordering, and compares the latest IDs, coordinator, rollback floor, and dependency set with `docs/INVARIANT_OWNERSHIP.md`. Adversarial contract relabeling and latest-view drift tests are mandatory at `0.18.3`, `0.22.0`, `0.29.0`, profile freezes, Phase O, and production. |
| Transmission-start failure prose collapsed distinct retry and reconciliation states | Make `VIT-LAW-006` generation 1 canonical semantics distinguish `DefinitelyNotStarted`, `OutcomeUnknown`, and `StartClaimedReconciling`. Only a proven pre-claim validation/CAS failure permits ordinary retry; ambiguous claim/permit delivery and committed claims require reconciliation and do not authorize retransmission. Carry typed-codec, crash-point, failover, restore, and provider-evidence tests through storage conformance, HA/deployment choice, Phase O, pentest, and `1.0.0`. |
| Authority proposals could survive after implementation began | Strengthen `0.18.3` so the authority-review checker reads the one `Status:` field belonging to every post-bootstrap milestone. `proposed` is valid only while that status contains `planned`; a transition to implementing, implemented, awaiting pentest, or ready to tag resolves the entry to exact declarations, generation-qualified extensions, or reviewed `none` in the same commit. Carry the zero-proposal gate through all option decisions, Phase O, release candidates, and `1.0.0`. |
| Canonical law rows were structured but not machine-readable or content-bound | Add `docs/LAW_GENERATION_MANIFEST.md` and implement `LawGenerationManifestV1` at `0.18.3`: exact ordered fields, resolved sorted dependencies, semantic-contract ID and complete semantics, activation/migration/rollback/recovery bindings, canonical codec, and content digest. The checker recomputes every planning digest and compares normalized latest semantics/digest with the ownership view. Negotiate persistence at `0.21.0`, destructively conform at `0.22.0`, preserve predecessor/successor manifests at `0.29.0`, and require digest continuity through identity generations, option freezes, Phase O, restore, pentest, and production. |
| The generation checker rejected legitimate removal-only or semantics-only evolution | Replace the addition-only rule at `0.18.3` with a meaningful-delta rule. Generation one still declares the complete initial dependency set; every successor must change at least one dependency, coordinator, or canonical semantic field. Removal-only, coordinator-only, and semantics-only generations are permitted with fresh semantic/dependency/recovery contracts, activation fence, manifest digest, migration, intersection, and rollback evidence; a true no-op is rejected. Add positive fixtures for each legitimate form and negative no-op/re-add/remove-absent cases. |
| Conformance authority cited bare laws and could imply future generations | Require `extends` references to use `VIT-LAW-NNN@gNN`. The checker resolves the latest generation effective at the milestone, rejects bare/unknown/stale/future references, and keeps declaration markers bare only when introducing a law. Pin `0.22.0` and `0.29.0` to generations effective by those versions; Phase F activates later generations; option and Phase O claims resolve to the then-current generation. Production evidence accepts no flattened or future-generation claim. |
| A valid manifest digest was not a runtime trust anchor | Add `docs/LAW_MANIFEST_ADMISSIONS.md` and `LawManifestAdmissionSetV1` at `0.18.3`: only exact `(LawId, Generation, Digest)` tuples in an artifact-bound compiled catalog or a catalog signed by a dedicated platform-law authority are trusted. Bind catalog identity/epoch/digest/profile into `0.19.0` checkpoints, `0.21.0–0.22.0` storage, `0.29.0–0.30.0` migration/interchange, `0.140.1` crypto trust, `0.140.2` placement, `0.140.6` HA, `0.145.0` recovery, and `1.0.0` evidence. Self-consistent attacker-authored manifests and database-created trust fail closed. |
| Per-generation composite structure was insufficiently constrained | Strengthen the `0.18.3` schema/checker: generation one has no removals; every resolved generation has at least two unique roots and contains its coordinator; additions/removals are disjoint; and a claimed dependency delta changes the final set. Carry destructive fixtures into `0.22.0`, migrations, option freezes, recovery, pentest, and production. |
| Semantic IDs were still backed primarily by prose | Add `docs/LAW_SEMANTIC_REALIZATIONS.md` and a closed `LawSemanticId`/`LawSemanticRealization` implementation at `0.18.3`. Every ID binds compiled Rust transitions, typed outcomes, recovery code, and exact P/N/M/F contracts; unknown or mismatched IDs fail startup/restore. Prohibit prose interpreters, reflection, and plugin extension; require complete realization at `0.22.0`, Phase O, pentest, and `1.0.0`. |
| Canonical law fields admitted ambiguous spellings | Restrict manifest-v1 source values to printable ASCII and exact Markdown cell spacing; reject control characters, tabs/CR, non-ASCII/Unicode normalization alternatives, embedded pipes, leading-zero generations/contract versions, malformed identifiers, and noncanonical numeric SemVer before hashing. Add adversarial `0.18.3` and `0.22.0` fixtures and repeat through import/restore. |
| A terminal generation reference did not explicitly prove ancestry | Define `VIT-LAW-NNN@gNN` as the full `g01..gNN` predecessor closure. Authority reviews, admission catalogs, conformance reports, migrations, exports/imports, option freezes, backup/restore, Phase O, pentest, and `1.0.0` enumerate every ancestor tuple/digest and semantic realization; missing or untrusted ancestry fails closed. |
| The active law catalog had no invariant owner | Add global `VIT-INV-057`, local `VIT-INV-058`, and `VIT-LAW-007` at `0.18.3`. One `PlatformLawCatalogLineage` owner governs global propose/activate/supersede/revoke/emergency-distrust commands and expected-version CAS; independently updated local owners govern catalog/distrust/trusted-time ratchets and enforcement. The composite law joins both with platform safety-floor, dispatch, and transmission-start roots. Carry both capability/test/recovery/fence contracts through storage, migration, option freezes, Phase O, pentest, and production. |
| The planning admission catalog could activate future generations | Reclassify `docs/LAW_MANIFEST_ADMISSIONS.md` as a non-runtime planning superset and add `docs/LAW_ACTIVE_CATALOGS.md`. `0.18.3` produces the first immutable activation-floor catalog; every later law-effective milestone produces a strictly increasing explicit successor with actual predecessor digest, complete effective ancestry, compiled realization, and no future tuple. A catalog remains applicable until superseded. Storage, export/import, restore, HA, pentest, and `1.0.0` reject planning/active confusion. |
| Signed-catalog fields were outside the canonical digest | Define `LawManifestAdmissionPayloadV1` and `LawManifestAdmissionEnvelopeV1` at `0.18.3`. Content-bind payload/predecessor digests, ID/epoch, activation floor, exact product/edition/compatibility scope, validity policy/times/maximum uncertainty, signer/key/signature profile, trust-root epoch, revocation, and successor policy. Runtime serializes exactly `CompiledCatalog` or `SignedCatalog`, never a combined profile. Bind the envelope into `0.19.0` checkpoints, storage/migrations, `0.140.1` crypto/time freeze, HA/recovery, and production evidence. |
| Semantic realization checks were not stage-aware and lacked negative contracts | Give every `VIT-LSEM-*` explicit P/N/M/F contracts. Its realization gate is the later of the effective milestone and the `0.18.3` backfill. When that gate leaves planned status, CI requires transition/recovery files, semantic ID in the closed enum/dispatch table, typed symbols/outcomes, and every concrete test ID. Add destructive missing-file/enum/symbol/P/N/M/F tests at `0.18.3` and `0.22.0`, then require zero unresolved realizations through Phase O and `1.0.0`. |
| Global catalog lineage and local admission ratchets shared one owner row | Keep `VIT-INV-057` exclusively global and introduce `VIT-INV-058 LocalLawCatalogAdmissionState` at `0.18.3`. Each local node/partition owns a separate `CatalogAdmissionRatchetRow`; neither transaction domain can update the other. Regenerate `VIT-LAW-007@g01` so it composes both owners. Prove partial rollout, unreachable/stale nodes, revocation propagation, local/global failover, restore, and migration at `0.18.3–0.18.4`, storage at `0.21.0–0.30.0`, HA at `0.140.6`, DR at `0.145.0`, and production. |
| Active-catalog maximum versions left milestones without a defined catalog | Remove the maximum-version field and make an activated catalog remain applicable until explicit successor activation. The checked schedule contains increasing activation floors at law-generation changes, and CI enumerates every implementation milestone—including `0.19.0`, `0.30.2`, option decisions, Phase O, and `1.0.0`—to prove exactly one greatest-floor catalog with no gap or overlap. |
| The catalog stage gate accepted digest-shaped field text without cryptographic validation | At `0.18.3`, implement one project-owned `no_std` verifier core shared by runtime and `vitheim-law-catalog-verify`. A non-planned catalog gate invokes that tool over canonical binary bytes and expected build context. It recomputes payload/envelope digests, verifies full tuple ancestry and the predecessor artifact's actual digest, applies profile-specific fields, verifies compiled expected digest or signature/root, and matches exact product/edition/compatibility scope. Exercise the first predecessor at `0.18.4`, checkpoint at `0.19.0`, storage/import at `0.21.0–0.30.0`, crypto freeze at `0.140.1`, and all Phase O/release gates. |
| `BoundedWindow` lacked an authoritative time and anti-rollback contract | Put trusted-time enforcement in local `VIT-INV-058` at `0.18.3`: `TrustedCatalogTime` returns a conservative interval, uncertainty, continuity identity, and rollback/suspend evidence. Persist the last trusted lower bound and expiry tombstone; reject excessive uncertainty, clock rollback, unaccounted suspend, unavailable time, or restore below the greatest ratchet. Freeze source and maximum uncertainty at `0.140.1`, topology at `0.140.2`, HA/time-loss behavior at `0.140.6`, prove non-extension at `0.145.0`, and require it for production. |
| Global catalog activation lacked a durable protocol spanning independent local owners | Add separately owned `VIT-INV-059 LawCatalogRolloutState` and `VIT-LAW-008` at `0.18.3`, while preserving `VIT-INV-057`, `VIT-INV-058`, and `VIT-LAW-007`. A one-owner process manager seals an immutable catalog/topology/placement manifest and advances through a closed lifecycle including irreversible `ActivationAuthorized` and permanent pre-authorization losing `Superseded`. Entering authorization atomically persists its authenticated receipt/outbox and pins `ActiveRolloutGeneration`; abandon, supersede, manifest replacement, or ordinary successor creation is then forbidden. The global owner serializes activate versus revoke, and delayed delivery cannot bypass a revocation tombstone. Authenticated receipts travel through transactional outbox/inbox delivery; deadlines reconcile missing or contradictory results. The default through `1.0.0` is `AllRequired`; any quorum option requires durable fencing of every unprepared placement before activation. Bootstrap at `0.18.3`; race authorization against abandon/supersede/revoke, response loss, delay, and failover at `0.18.4`; carry the bundle through checkpoints/storage/migration; freeze it at `0.140.1`/`.2`/`.6`; and prove HA, restore, pentest, and production behavior without a distributed transaction. |
| A copied local catalog row could masquerade as admission for another workload or placement | Define one canonical `VIT-INV-058` ownership grain keyed by `(DeploymentId, RegionId, ServiceRole, EnforcementPartitionId, PlacementGeneration)`. Bind each local row and receipt to a verified non-clonable `WorkloadIdentity`, fresh `BootOrContinuityId`, binary capability and semantic-realization digests, catalog epoch/envelope digest, rollout/global receipts, and local fencing token. Disk/VM/pod/partition clones, backup restores, replacements, autoscaling, service splits, and region moves cannot inherit readiness: the current placement generation must fence its predecessor and obtain a fresh verified receipt. Persist copied rows only as evidence. Carry the schema through `0.18.3–0.30.0`, freeze workload identity and placement at `0.140.1`/`.2`, directly race clone/replacement/topology behavior at `0.142.0–0.143.0`, prove non-inheritance at `0.145.0`, and reject cloned admission in pentest and `1.0.0` evidence. |
| Catalog rollout implicitly depended on an undefined owner of “current topology” | Preserve rollout as a consumer and add `VIT-INV-060`, but execute no generation-2 transition before generation 2 is active. `0.18.3` permits only the compiled singleton. At `0.141.0`, keep the handoff `Uninitialized` and activate/converge epoch 13 through `VIT-LAW-008@g01`; after every local owner admits generation 2, generation 2 executes `InitializeTopologyAuthorityHandoff` to create an exact inert dormant singleton, verifies equality, and then commits the rollout/catalog/artifact/manifest-bound handoff. Only `Committed` issues receipts/commands. Recovery never infers initialization/commit, and static/dynamic sources are never co-authoritative. Crash every catalog/local-admission/initialization/verification/commit boundary, race later topology operations, preserve the three-state selector at restore, and require exact ancestry through production. |
| “Non-clonable workload identity” lacked an executable mechanism and receipt digests could be mistaken for authenticity | Define closed identity/receipt contracts at `0.18.3` and freeze at `0.140.1`. Hardware uses a non-exportable measured key. The orchestrator profile uses `WorkloadLeaseActionAuthorityPortV1`: the external issuer owns stable claim/action IDs, lease generation, sequence uniqueness, expiry/revocation, and exact idempotent replay; each protected Vitheim invariant owner atomically commits `ConsumedWorkloadLeaseActionClaim` with its transition/outcome. Lost issuance/consumption responses are distinct typed uncertainty, forbid reissue, and reconcile through issuer/local tombstones; restore honors greatest external/local high-watermarks. Receipt authentication is exactly signed, authority-MAC, or attested-channel admission; MAC generation is sender-only and receivers verify only. Test clone/offline use, split commit, response loss, mismatched-digest replay, expiry/revocation, MAC-role bypass, anchor corruption, failover, migration, and restore through `1.0.0`. |
| Activation authorization and the global activation result crossed owner boundaries without explicit authentication contracts | At `0.18.3`, define `CatalogActivationAuthorizationReceipt` and `CatalogGlobalActivationResultReceipt` under the same closed authentication profile. Bind rollout/manifest/generation, catalog/predecessor digests, authorization/request identity, expected and resulting global CAS versions, outcome, distrust/revocation state, owner fences, signer/key epochs, idempotency IDs, and replay tombstones. The global owner authenticates authorization before CAS; rollout authenticates the result before transition. For MAC, only the named sender may generate and receivers may only verify under KMS/HSM policy. Persist/checkpoint/migrate both receipts and tombstones at `0.19.0–0.30.0`, freeze crypto/storage/HA at `0.140.1`/`.2`/`.6`, and test substitution, replay, receiver forgery, key rotation, response loss, failover, restore, pentest, and production. |
| Concurrent catalog candidates had no permanent winner/loser meaning | Strengthen planned `VIT-LAW-008@g01` before its first real use: every lineage owns one monotonic `ActiveRolloutGeneration`; candidate creation claims it by expected-version CAS; only the exact current generation can prepare. Add terminal `Superseded` and irreversible `ActivationAuthorized`. Pre-authorization supersession advances the generation and tombstones the loser; authorization atomically persists receipt/outbox and pins the winner until the globally serialized activate-or-revoke result is reconciled. Late losing/delayed authorization is rejected and cannot bypass revocation. At `0.18.4`, race two candidates and authorization against abandon/supersede/revoke, lose responses, delay delivery, fail over, and prove one pinned authorized winner plus one permanent pre-authorization loser. Carry authorization/pin/tombstones through checkpoints, storage, migration, HA, restore, compatibility freeze, pentest, and production. |
| Dynamic topology affected rollout but did not independently fence normal operation | Add `VIT-LAW-007@g02` at `0.141.0` with VIT-INV-060 as a dependency. Before handoff commit use only the compiled singleton; after commit, local admission, readiness, dispatch, and transmission start each authenticate and match current topology/placement generation independently of rollout state or fence delivery. Replace a placement after the latest rollout, suppress the predecessor fence, and prove topology unavailability/mismatch blocks every path. |
| Action-claim language conflated read-only readiness, mutations, and owner protocols | Freeze a closed scope matrix at `0.140.1`: positive local receipt/admission creation, topology mutation, dispatch, and transmission start use single-use claims; readiness uses bounded reusable `OnlineWorkloadFreshnessProofV1`; authenticated global/rollout owner-to-owner control and revocation/distrust/fence application use their own protocol without claims. Persist and HA-test each boundary at `0.140.2`/`.6`; implement and certify it through `0.141.0–1.0.0`. |
| Claim replay after later expiry or revocation had ambiguous precedence | Consult the consumed tombstone first. Exact claim/action digest replay returns the stored historical typed outcome without repeating work even after later expiry or revocation; it grants no new authority. Only absent tombstones undergo current first-consumption validation, and mismatches always reject. Test crash-after-commit, expiry-before-response, revocation-before-replay, and restore-before-tombstone reconciliation from `0.18.4` through HA/restore/production. |
| Workload authentication could be mistaken for permission to mutate production topology | Require an independently issued unique `TopologyMutationAuthorizationReceiptV1` for handoff initialization/commit and every successor. Bind principal/session/delegation, issuance-time identity/role/policy epochs, change/incident/emergency evidence, approval/SoD, expected generation, successor digest, profile-applicable workload proof, expiry, and bounded break-glass review. VIT-INV-060 locally consumes the receipt, proof, tombstones, topology CAS, and fence outbox under VIT-LAW-008@g02; compromised but attested topology workloads remain unauthorized. |
| Topology authorization named no issuance owner and implied impossible cross-owner atomic epoch revalidation | Declare independent `VIT-INV-061 TopologyMutationAuthorizationState` at `0.141.0`. It exclusively owns lineage/generation, quorum/SoD, unique mutation/manifest issuance, revoke/supersede/expiry, idempotency/unknown response, independent break-glass recovery, and restore high-watermarks. Choose issuance-time linearization: validate current epochs, issue one narrow immutable grant with fixed `commit_before`; later change blocks new issuance but does not retroactively revoke that exact grant. VIT-INV-060 only consumes the authenticated grant and never mints it or claims a distributed transaction. |
| Authenticated topology receipts lacked currentness and profile discrimination | Extend `CurrentPlacementTopologyReceiptV1` with verifier challenge, monotonic receipt sequence, topology/placement generations, manifest/member tombstone/fence, owner identity/fence, time/expiry/uncertainty, signer/key epoch, and authentication profile. VIT-INV-058 ratchets last-observed generation/sequence; readiness binds them into freshness proof and dispatch/start use a fresh response or frozen maximum window. Discriminate authorization workload proof: orchestrator requires claim and canonical-none hardware fields; hardware requires proof and canonical-none claim fields. Test signed-old/cache replay, clock/restore rollback, failover, and mixed fields at `0.22.0`, option freezes, `0.141.0`, HA, DR, and production. |
| Topology-mutation grant expiry relied on an unqualified wall clock | Keep issuance-time linearization but bind mutation class, issued-at, commit-before, maximum uncertainty, trusted-time profile/epoch, and issuer continuity into the authenticated receipt. At `0.140.1`, freeze five minutes for initialization, two minutes for handoff commit or dynamic successors, and at most sixty seconds for break-glass. At `0.141.0`, fail issuance on unavailable/discontinuous/excessively uncertain time and make VIT-INV-060 persist a local lower-bound/profile/continuity/expired-receipt ratchet, consuming only when a conservative interval plus commit slack proves the CAS precedes expiry. At `0.142.0–0.143.0`, test issuer/consumer skew, NTP steps, suspend, failover, and expiry races; at `0.145.0`, merge greatest external/local time state so restore cannot extend a grant; at `1.0.0`, accept the explicit residual credential-compromise window only within those immutable ceilings and single-consumption rules. |
| Phase C omitted topology-authorization time state and client-side commit slack could not prevent a late database commit | At `0.21.0`, freeze canonical `TopologyMutationAuthorizationReceiptV1`, complete VIT-CAP-060/061 issuer/consumer time schemas, and mandatory `DeadlineConditionalTopologyCasV1`. Permit only an authoritative commit-time predicate or hard no-late-commit fence; client/statement timeouts and best-effort cancellation never qualify, and changing authorization linearization requires a successor law. At `0.22.0`, pause after locks/time/before CAS/during commit/timeout/response loss/failover and accept only a provably pre-expiry commit or transaction that cannot later commit. At `0.23.0–0.27.0`, every backend proves the mechanism and field parity or refuses VIT-CAP-060. At `0.29.0–0.30.0`, require non-lossy migration/downgrade rejection and same-or-stronger destination admission. At `0.140.2`, freeze SQLite/PostgreSQL production mechanisms; carry proof through `0.141.0`, HA, DR, and `1.0.0`. |
| Permanent topology-authorization replay state had no bounded lifecycle | At `0.21.0`, define `TopologyAuthorizationReplayLifecycleV1`: pre-allocation per-deployment and issuer/class rate/outstanding quotas, monotonic issuance sequence, exact replay horizon, authenticated predecessor-linked set/archive checkpoint, covered-through high-watermark, checkpoint-before-delete compaction, typed unavailable-history denial, growth accounting, and fail-closed saturation. At `0.22.0`, prove crash/concurrent-replay/horizon/archive/key-rotation behavior. At `0.23.0–0.27.0`, each backend proves bounded lifecycle parity or refuses VIT-CAP-060/061. At `0.29.0–0.30.0`, preserve hot records, checkpoint chain, sequence/key/high-watermarks and same-or-stronger proof behavior. At `0.140.2`, freeze exact quotas/horizon/algorithm/cadence/backlog/alerts; at `0.140.5`, minimize long-lived checkpoint data. Implement at `0.141.0`, failover-test at `0.143.0`, restore at `0.145.0`, soak at `0.146.0`, harden at `0.147.0`, and require the evidence at `1.0.0`. |
| Consumer compaction assumed dense issuer sequences and authorization floods could starve break-glass repair | At `0.21.0`, make VIT-INV-060 sparse by default; allow dense `ConsumerCompactionEligibleThrough` only with authenticated `TopologyAuthorizationIssuedRangeManifestV1`, trusted-time horizon/deadline proof, and terminal/permanently-unresolved local members. Late first presentation remains historical. Split admission into independent `Normal`, `Recovery`, and `BreakGlass` rate/outstanding counters with a small non-borrowable reserve and recovery lane; emergency issuance retains every security/replay gate. At `0.22.0`, test gaps, manifest faults, late presentation, normal exhaustion and reverse floods. Require parity or refusal in `0.23.0–0.27.0`; preserve profiles in `0.29.0–0.30.0`; freeze exact manifests/reserves at `0.140.2`, minimize at `0.140.5`, and carry through `0.141.0`, HA, DR, soak, hardening, compatibility, and `1.0.0`. |
| Issuance atomicity, outstanding release, caller fairness, and range-proof resource bounds were implied rather than explicit | At `0.21.0`, require one VIT-INV-061 local transaction for layered quota validation/counter reservation/sequence/receipt/idempotent result/outbox; release an outstanding reservation exactly once only from authenticated stable terminal evidence, never client timeout; add canonical principal-or-authority sub-limits; and define bounded root/chunk bytes, entries, decode allocation, verification work, proof depth and resumable cursor. At `0.22.0`, crash every write point and attack settlement/chunk limits. Prove or refuse parity at `0.23.0–0.27.0`; preserve state/limits at `0.29.0–0.30.0`; freeze exact values at `0.140.2`, minimize non-sensitive chunk evidence at `0.140.5`, and carry through `0.141.0`, HA `0.143.0`, DR `0.145.0`, soak `0.146.0`, hardening `0.147.0`, compatibility `0.148.0`, and `1.0.0`. |
| Lineage revoke/supersede was incorrectly usable as reservation-release evidence even though an already-issued receipt remains consumable until immutable `commit_before`; settlement could also lose its original quota bucket and pre-allocation rates were underspecified | Correct the contract at `0.21.0`: lineage change blocks only new issuance and retains every live receipt reservation; release requires consumer-authenticated consumption, conservative expiry beyond the exact deadline, consumer-authenticated definitely-not-committed/permanently-unresolved state, or a receipt-specific VIT-INV-060 revocation tombstone proven by an authenticated consumer terminal receipt. Define immediate individual revocation as a separate intent/fence/tombstone protocol serialized by VIT-INV-060, never evidence manufactured by VIT-INV-061. Embed the original deployment, issuer/class, principal-or-authority key, budget epochs, class, reserve source, units, and quantities in every reservation and settle those original counters atomically. Split pre-allocation accounting into authenticated-presentation, first-seen-request, successful-admission and outstanding layers. At `0.22.0`, prove lineage retention, issuer-forgery rejection, original-bucket settlement across policy/key/epoch change, all four layer semantics, and no partial/duplicate/underflow release. Require parity or refusal at `0.23.0–0.27.0`; preserve exact fields at `0.29.0–0.30.0`; freeze receipt revocation at `0.140.1`, storage/accounting at `0.140.2`, and HA recovery at `0.140.6`; implement at `0.141.0`, authenticate cross-service evidence at `0.142.0`, fail over at `0.143.0`, restore at `0.145.0`, soak at `0.146.0`, harden at `0.147.0`, freeze compatibility at `0.148.0`, and require external pentest evidence before `1.0.0`. |
| Authenticated denial results had no sequence-backed bounded compaction path, so maximum-rate unique denials could grow forever or become fresh after deletion and later policy change | At `0.21.0`, give every first-seen authenticated canonical request a VIT-INV-061 `TopologyAuthorizationRequestSequence`; exact retries reuse sequence, request-rate charge and outcome, while only success also receives `AuthorizationIssuanceSequence`. Define an authenticated predecessor-linked `TopologyAuthorizationRequestReplayCheckpointV1` over request/range, ID/digest, caller/authority, class, request charge, typed outcome, successful issuance link, archive/counter/schema/algorithm/key evidence. Keep exact denial replay for a bounded horizon; checkpoint before delete; treat late compacted requests as historical; return typed historical-state-unavailable on missing proof; and bound rows/bytes/backlog/archive/decode/work/jobs/latency. At `0.22.0`, crash and race sequence/checkpoint/delete/replay, policy change, archive loss and saturation. Require parity/refusal at `0.23.0–0.27.0`, preserve it at `0.29.0–0.30.0`, freeze storage at `0.140.2`, minimize long-lived commitments at `0.140.5`, freeze HA at `0.140.6`, implement at `0.141.0`, preserve across services at `0.142.0`, fail over at `0.143.0`, restore at `0.145.0`, soak at `0.146.0`, harden at `0.147.0`, freeze compatibility at `0.148.0`, and require external pentest evidence before `1.0.0`. |
| `TopologyAuthorizationConsumerTerminalReceiptV1` was named without a canonical field contract or closed outcome/authentication state machine | At `0.21.0`, bind deployment; VIT-INV-060 partition/generation/fence; authorization ID/issuance sequence/receipt digest/canonical optional revocation-intent digest; closed outcome; consumer result version/sequence and tombstone digest; deadline/trusted-time evidence; sender/key epoch/authentication profile; stable message/idempotency ID; and outbox sequence. Freeze terminal outcomes to `RevokedBeforeConsumption`, `AlreadyConsumed`, `Expired`, `DefinitelyNotCommitted`, and `PermanentlyUnresolved`. Put ongoing reconciliation only in `TopologyAuthorizationConsumerDispositionV1::Reconciling` with separate evidence/receipt types, and accept only the terminal receipt at settlement. VIT-INV-060 alone has sender authentication and VIT-INV-061 is verify-only; incomplete/open/conflicting/rolled-back/issuer-created/reconciling evidence retains capacity. At `0.22.0`, attack every field/outcome/sequence/role and prove reconciliation cannot type-check as settlement evidence. Require backend parity/refusal at `0.23.0–0.27.0`, non-lossy preservation at `0.29.0–0.30.0`, freeze authentication/types at `0.140.1`, storage at `0.140.2`, HA at `0.140.6`, implement at `0.141.0`, preserve role/type separation over RPC at `0.142.0`, fail over at `0.143.0`, restore at `0.145.0`, soak at `0.146.0`, harden at `0.147.0`, freeze compatibility at `0.148.0`, and require external pentest evidence before `1.0.0`. |
| The earlier “charge every authenticated presentation” rule contradicted exact-retry language that reused the same attempt charge, leaving replay-storm cost and logical-request accounting ambiguous | At `0.21.0`, replace the ambiguous attempt budget with `TopologyAuthorizationPresentationRateBudgetV1`, charged before protected idempotency lookup on every authenticated canonical presentation, and `TopologyAuthorizationRequestRateBudgetV1`, charged once per first-seen canonical ID/digest and bound to `TopologyAuthorizationRequestSequence`. Presentation saturation is a transient rate-limited result and cannot read or replace immutable request outcomes. Exact retries and concurrent identical presentations each pay presentation rate but reuse one request charge/sequence/outcome; changed-digest reuse pays presentation rate and rejects without another request charge. First-seen success additionally pays admission/outstanding; first-seen denial does not. At `0.22.0`, prove replay storms, concurrent identical calls, response-loss retry, retry under admission saturation and changed-digest conflict. Require backend parity/refusal at `0.23.0–0.27.0`, non-lossy rate-window/request-charge preservation at `0.29.0–0.30.0`, freeze storage at `0.140.2`, aggregate-only privacy at `0.140.5`, HA at `0.140.6`, implementation/RPC/failover/restore/soak/hardening/compatibility at `0.141.0–0.148.0`, and external pentest evidence before `1.0.0`. |
| `Reconciling` appeared inside a type named “terminal outcome”, relying on prose to prevent unsafe reservation release | At `0.21.0`, make the unsafe state unrepresentable: `TopologyAuthorizationConsumerTerminalOutcomeV1` contains only the five stable terminal variants; `TopologyAuthorizationConsumerDispositionV1` separates `Terminal(...)` from `Reconciling(TopologyAuthorizationConsumerReconciliationEvidenceV1)`; reconciliation emits its own authenticated receipt and may advance to terminal only with a greater result/outbox sequence; and `SettleTopologyAuthorizationOutstandingReservation` accepts only `TopologyAuthorizationConsumerTerminalReceiptV1`. At `0.22.0`, include compile/type rejection plus forged, reordered and conflicting evidence tests. Require adapter parity/refusal at `0.23.0–0.27.0`, exact migration/export preservation at `0.29.0–0.30.0`, type/authentication freeze at `0.140.1–0.140.2`, HA freeze at `0.140.6`, implementation through compatibility at `0.141.0–0.148.0`, and external pentest evidence before `1.0.0`. |
| The durable presentation limiter ran after authentication/canonicalization but before policy without a safe way to select `Normal`, `Recovery`, or `BreakGlass`, so caller-selected class could exhaust emergency capacity while normal-only charging could starve it; pre-authentication work was also only vaguely bounded | At `0.21.0`, add `TopologyAuthorizationIngressWorkBudgetV1` for deployment/listener bytes, connection/handshake concurrency, signature/MAC work, canonical decode allocation/depth/work and failures before durable state. Add closed `TopologyAuthorizationPresentationLaneV1`; derive it only from authenticated endpoint/audience and a versioned fenced credential/authority profile, never request content. Provision recovery/break-glass identities and audiences separately; make presentation/request capacity non-borrowable; require full-policy `TopologyAuthorizationBudgetClass` to exactly match the lane or reject before logical request allocation. At `0.22.0`, test ingress exhaustion, normal-to-emergency forgery, wrong audience/profile/mapping, normal-principal floods, break-glass-versus-recovery isolation, rotation/revocation/failover/restore and lane/class mismatch. Require parity/refusal at `0.23.0–0.27.0`, preserve mapping high-watermarks at `0.29.0–0.30.0`, freeze identity at `0.140.1`, storage/accounting at `0.140.2`, privacy at `0.140.5`, HA at `0.140.6`, implement through compatibility at `0.141.0–0.148.0`, and require external pentest evidence before `1.0.0`. |
| VIT-LAW-008@g02 canonical generation text still described the superseded attempt-rate model while its semantic realization and plans required presentation/request rates, denial checkpoints, original-claim settlement and terminal/reconciliation separation | Amend the not-yet-effective `0.141.0` generation-2 planning manifest rather than layering contradictory prose: update both canonical records with ingress/lane/presentation/request/admission/outstanding linearization, request/issuance sequences, denial checkpoint/archive permanence, original quota claims, terminal-only settlement and separate reconciliation, plus complete recovery and negative guarantees. Recompute the generation manifest digest and planning-catalog digest, update the admission record, and keep generation/current-view equality enforced by `scripts/check_law_generations.sh`. This is a pre-activation planning correction, not an in-place mutation of an effective production law; after activation, the same change would require a successor generation. |
| Presentation cost was required before protected lookup but was still described inside the first-seen request/issuance transaction | Freeze a two-stage protocol at `0.21.0`: `ChargeTopologyAuthorizationPresentation` durably commits a non-refundable presentation debit and bounded internal single-use evidence before lookup; `ConsumeTopologyAuthorizationPresentationCharge` consumes it in the logical request/outcome/issuance transaction. Crash after stage one leaves an orphaned spent charge and retry needs a new charge. Destroy every stage boundary at `0.22.0`, require adapter parity/refusal at `0.23.0–0.27.0`, preserve evidence/dispositions/continuity/checkpoints at `0.29.0–0.30.0`, freeze storage/privacy/HA at `0.140.2`, `0.140.5`, and `0.140.6`, and carry implementation, RPC isolation, HA, DR, soak, hardening, compatibility and pentest evidence through `0.141.0–1.0.0`. |
| Deployment/listener-scoped pre-auth budgets still allowed ordinary traffic to consume emergency accept, TLS, decode, executor, or pool resources before authenticated lane selection | At `0.21.0`, provision independent non-borrowable `Normal`/`Recovery`/`BreakGlass` listener/socket/file-descriptor, TLS-worker, decode CPU/memory, executor-queue, and pool resources beneath an aggregate fail-safe ceiling. Server-controlled listener/TLS routing selects only `TopologyAuthorizationIngressLaneV1` and grants no authority. Exhaust every resource independently at `0.22.0`, require backend/runtime parity or refusal at `0.23.0–0.27.0`, freeze concrete resource profiles at `0.140.2` and HA at `0.140.6`, then prove split-service, failover, restore, soak, hardening, compatibility and pentest behavior through `0.141.0–1.0.0`. |
| Presentation-lane mapping lacked one authoritative owner and a commit-time defense against rotation/revocation between authentication and request allocation | Make VIT-INV-061 the sole mapping-lineage owner at `0.21.0`; bind stable mapping identity/generation/fence/profile digest into charge evidence; require separated requestor/approver/activator roles for emergency promotion; and make stage two recheck the current mapping under lock. Drift returns `TopologyAuthorizationPresentationLaneChanged`, leaves the charge spent, and allocates no request. Prove promotion SoD, rotation/revocation TOCTOU, failover, restore and continuity fencing at `0.22.0`; preserve high-watermarks at `0.29.0–0.30.0`; freeze ownership/identity at `0.140.1`, storage at `0.140.2`, and HA at `0.140.6`; then implement and certify through `0.141.0–1.0.0`. |
| VIT-INV-060/061 prose referenced enforcement and negative-test children absent from their authoritative rows, and the checker could not detect undeclared or duplicate child IDs | At `0.21.0`, declare VIT-ENF-060-G/VIT-TST-060-N-G and VIT-ENF-061-F/G with matching VIT-TST-061-N-F/G, name VIT-INV-061 mapping/budget/charge/disposition/checkpoint rows and recovery state, and make the ownership checker reject referenced-but-undeclared or duplicate enforcement/test children. Add adversarial checker fixtures at `0.22.0`, preserve child IDs through `0.29.0–0.30.0`, and require exact declaration/realization coverage through `0.141.0–1.0.0`. |
| One production paragraph still placed the independently committed presentation debit inside the first-seen stage-two transaction | Correct `PRODUCTION_1_0.md` to say stage-two presentation-charge evidence consumption plus request-rate charging. Retain the debit exclusively in stage one at `0.21.0`; make the textual distinction a checker anchor and destructive transaction-boundary test at `0.22.0`; preserve it across adapters, RPC, HA, DR, compatibility and pentest through `0.23.0–1.0.0`. |
| A Phase C verification sentence lost the canonical-decode-before-owner-state and wrong-emergency-endpoint requirements | Restore the complete `0.22.0` adapter conformance sentence: bound every pre-auth byte/concurrency/handshake/signature/MAC/canonical-decode limit before owner state, then present normal credentials to recovery/break-glass endpoints and prove fail-closed rejection. Carry the unchanged conformance case through adapter parity and Phase O soak/pentest. |
| Presentation-charge disposition existed as a named type without closed variants, irreversible transitions, or fail-atomic ledger saturation | At `0.21.0`, freeze `ChargedAwaitingStageTwo` as the only nonterminal state and irreversible `Consumed`, `MappingChanged`, `ControlledAbortAbandoned`, and `ContinuityFencedOrphaned` terminal kinds; permit `CheckpointedCompacted` only after a predecessor-linked checkpoint preserving the original kind and result/evidence commitment. Stage one atomically writes debit/evidence/sequence/awaiting or fails before lookup on row/byte/backlog saturation. At `0.22.0`, exercise every valid edge and reject unknown, cross-terminal, rollback, timeout-derived and pre-checkpoint compaction transitions. Require adapter parity/refusal at `0.23.0–0.27.0`, migration preservation at `0.29.0–0.30.0`, storage/privacy/HA freeze at `0.140.2`, `0.140.5`, and `0.140.6`, and implementation/RPC/HA/DR/soak/hardening/compatibility/pentest evidence through `0.141.0–1.0.0`. |
| Durable presentation-charge storage and maintenance limits were global, so a Normal or break-glass flood could exhaust rows, awaiting records, checkpoint/archive I/O, or compaction workers needed by Recovery | At `0.21.0`, add `TopologyAuthorizationPresentationChargeLedgerCapacityV1`: independent non-borrowable `Normal`/`Recovery`/`BreakGlass` hot-row, byte, awaiting, checkpoint-backlog, checkpoint/archive-I/O, and compaction-worker ceilings below aggregate disk/work ceilings. Stage-one admission reserves its lane's terminalization/checkpoint obligation; Normal saturation rejects only Normal, and BreakGlass cannot block Recovery. At `0.22.0`, stall Normal maintenance, fill every Normal resource, prove both emergency lanes complete both stages, then saturate BreakGlass and prove Recovery plus aggregate bounds. Require adapter parity or VIT-CAP-061 refusal at `0.23.0–0.27.0`; preserve reservations/high-watermarks through `0.29.0–0.30.0`; freeze storage at `0.140.2` and HA at `0.140.6`; implement, RPC-isolate, fail over, restore, soak, harden, compatibility-freeze, and externally pentest through `0.141.0–1.0.0`. |
| The durable charge-capacity limits had no immutable activation/resizing lineage, so unsafe shrink, old-writer mutation, false physical increases or restore of an older larger ceiling could cancel obligations or overcommit Recovery | At `0.21.0`, make VIT-INV-061 own immutable `TopologyAuthorizationPresentationChargeLedgerCapacityProfileV1` successors with stable ID, monotonic generation/epoch, digest, predecessor, closed state and expected-version CAS. Govern emergency reductions and aggregate changes with approval/SoD; require each lane's new limit to cover usage, awaiting charges, reservations, backlog, maintenance and protected reserve or remain `PendingDrain`/reject; forbid transfer and require physical disk/I/O/worker evidence for increases. At `0.22.0`, race shrink with both stages/compaction, interrupt activation, fail over generations, restore older larger limits, forge provisioning evidence and attack Recovery reassignment. Require parity/refusal at `0.23.0–0.27.0`; preserve lineage/obligations at `0.29.0–0.30.0`; freeze storage at `0.140.2` and greatest-authenticated-generation recovery—not numeric maximum—at `0.140.6`; implement through RPC/HA/DR/soak/hardening/compatibility/external pentest at `0.141.0–1.0.0`. |
| A `PendingDrain` successor did not fence new stage-one admissions, so continuous traffic or an activation race could indefinitely replenish predecessor capacity or admit work that violates the successor | At `0.21.0`, atomically install lane-scoped `TopologyAuthorizationPresentationChargeLedgerCapacityDrainFenceV1` on PendingDrain, bind predecessor/successor generations/digests/lanes/sequence/continuity, permit one nonterminal successor, and require stage one to fit active plus pending limits or return typed pre-debit draining denial. Existing obligations retain completion reservations; activation/rejection locks and consumes the exact fence under expected-version CAS. At `0.22.0`, sustain traffic; race install/admit/activate/reject; crash/fail over/restore installed fences; attack stale workers and competing successors; prove Recovery isolation. Require parity/refusal at `0.23.0–0.27.0`, non-lossy migration/import at `0.29.0–0.30.0`, storage freeze at `0.140.2`, HA recovery at `0.140.6`, and implementation/RPC/HA/DR/soak/hardening/compatibility/external-pentest proof through `0.141.0–1.0.0`. |
| Direct activation of an apparently safe or aggregate-only reduction could race admission; caller/incomplete lane coverage could bypass a reduced shared ceiling; greatest-generation recovery could select unactivated history; and standalone fence helpers could split lifecycle authority | At `0.21.0`, require every canonical lane/reserve/aggregate/storage/I/O/worker reduction to traverse Proposed -> PendingDrain -> Active; permit direct Proposed activation only after overflow-safe typed proof that all fields are equal/increasing, treating unknown/omitted/incomparable/mixed schemas as reductions. Derive affected lanes and reduced aggregate dimensions from the canonical diff, fence every capable consumer, use fixed lane/aggregate row locking, and expose only atomic fence-installed/consumed events. Define `CapacityRecoveryStateV1` from the greatest authenticated committed activation record, optional pending successor/exact fence, and lineage/activation high-watermarks. At `0.22.0`, race initially safe and aggregate-only reductions with traffic; attack typed comparison, derived coverage, lock ordering, higher unactivated generations, contradictory/multiple-active/half-state recovery, and direct helper calls. Require adapter parity/refusal at `0.23.0–0.27.0`, exact migration/import at `0.29.0–0.30.0`, storage/recovery freeze at `0.140.2`/`0.140.6`, and implementation/RPC/HA/DR/soak/hardening/compatibility/external-pentest proof through `0.141.0–1.0.0`. |
| PendingDrain itself could deny service without action-bound authority, while recovery depended on an underspecified activation record that could be deleted, forked, reordered or contradicted by the active row | At `0.21.0`, add single-action `CapacityDrainAuthorizationV1` for every Normal/Recovery/BreakGlass/aggregate begin-drain/activate/reject/abandon operation, binding exact deployment/tenant/predecessor/successor/diff/derived coverage/policy/change-or-incident/requestor/approvers/activator/quorum/SoD/expiry/nonce/idempotency and rechecking at install/activation. Add canonical non-wrapping predecessor-linked `CapacityProfileActivationRecordV1`; commit record/head/supersession/activation/optional fence event/audit/idempotent result/outbox atomically, and checkpoint the full head plus active/pending/fence tuple before deletion. At `0.22.0`, attack unauthorized/expired/replayed/self-approved/substituted drains, rejection cycling, policy/predecessor races, record gaps/forks/reorder/duplicates/wrap, response loss, checkpoint/high-watermark rollback and active-row disagreement. Require adapter parity/refusal at `0.23.0–0.27.0`, exact migration/import at `0.29.0–0.30.0`, auth/storage/privacy/recovery freezes at `0.140.1`, `0.140.2`, `0.140.5`, `0.140.6`, and implementation/RPC/HA/DR/soak/hardening/compatibility/external-pentest proof through `0.141.0–1.0.0`. |
| Drain authorization was called non-replayable without defining exact response-loss retry, its own durable closed lifecycle, trusted-time/key rollback resistance, long-drain expiry behavior, or checkpoint-complete replay state | At `0.21.0`, add `CapacityDrainAuthorizationConsumptionV1` with only Issued, Consumed(action/request/result digests), ExpiredUnused and RevokedUnused plus canonical `CapacityDrainActionResultV1` and typed conflict. Atomically commit consumption/tombstone/result/mutation/event/audit/outbox; exact action/idempotency/request retry returns the prior result and changed bytes/digest/action writes nothing. Bind not-before/issued/expiry, maximum uncertainty, trusted-time profile/epoch, issuer continuity, signer/key epoch and authentication profile; ratchet time/key/consumption/result state. A begin-drain grant need only be valid when consumed; activation requires fresh current authority, while revocation leaves the fence safe until activation or separately authorized rejection. At `0.22.0`, test response loss/exact and conflicting retry/consumed reuse/expiry during drain/policy revocation/time loss/rollback/suspend/old restore/key rotation/checkpoint deletion. Require adapter parity/refusal at `0.23.0–0.27.0`, preservation through migration/import at `0.29.0–0.30.0`, exact crypto/storage/privacy/HA decisions at `0.140.1`/`.2`/`.5`/`.6`, and implementation/RPC/HA/DR/soak/hardening/compatibility/external-pentest evidence through `0.141.0–1.0.0`. |
| Hot drain replay compaction retained only digests/high-watermarks, which cannot reproduce an exact result or safely classify sparse arbitrary IDs, and missing archives could otherwise make consumed authority appear unseen | Select authenticated sparse replay archives at `0.21.0`; reject permanent unbounded retention. `CapacityDrainAuthorizationReplayCheckpointV1` entries bind action/idempotency ID, request/authorization digests, lifecycle, complete result or authenticated reference, action/profile/scope/policy, predecessor checkpoint, encoding/key epoch and archive commitment. Exact-set membership returns historical result/conflict; non-membership is never a dense watermark inference. Freeze bytes/entries/chunks/depth/decode/work/jobs budgets, durable cursor, verified immutable upload followed by atomic local head/cursor/exact-delete, Recovery maintenance reservation/backpressure, and typed historical-state-unavailable denial. At `0.22.0`, test sparse IDs, late exact/changed retry, outage, result-reference/chunk/key loss, fork, proof exhaustion and compaction crash. Require adapter parity/refusal at `0.23.0–0.27.0`, migration/import preservation at `0.29.0–0.30.0`, storage/privacy/HA decisions at `0.140.2`/`.5`/`.6`, and implementation/RPC/HA/DR/soak/hardening/compatibility/pentest proof through `0.141.0–1.0.0`. |
| Sparse replay archives lacked one cumulative authoritative head and implied an impossible cross-system archive/delete transaction, allowing stale individual-root non-membership, snapshot races, staged/orphan authority or unsafe cleanup | At `0.21.0`, add one per-tenant/deployment `CapacityDrainAuthorizationReplayHeadV1` with non-wrapping sequence, predecessor/root/scope, expected-version CAS, key/publication and covered-row version. Check non-membership against the greatest committed head plus current hot rows. Freeze Staged→Verified→CommittedHead→HotRowsDeleted→OrphanGcEligible publication: immutable chunks upload/verify first; one local database transaction co-commits head CAS and exact covered-row deletion; unknown external outcomes retain rows, local unknowns reconcile, readers ignore noncommitted data, and GC requires no reference or authenticated successor equivalence. At `0.22.0`, race consumption/compaction, publishers, stale readers, coalescing, delayed visibility, unknown outcomes, GC and rollback. Require adapter parity/refusal at `0.23.0–0.27.0`, migration/import preservation at `0.29.0–0.30.0`, storage/privacy/HA freeze at `0.140.2`/`.5`/`.6`, and implementation/RPC/HA/DR/soak/hardening/compatibility/pentest proof through `0.141.0–1.0.0`. |
| Cumulative replay publication did not linearize an already verified proof with the later hot-row absence and first-execution claim, allowing proof for `H` to be combined with absence created by `H+1` or mismatched replica views | At `0.21.0`, add `CapacityDrainReplayAdmissionGuardV1` and typed `CapacityDrainReplayHeadChanged`: read writer-authoritative head `H`, bounded-verify its proof outside the database transaction, then in one local write transaction lock head followed by exact action/idempotency key, require exact `H`, check current hot state, uniquely insert the replay row and atomically commit the existing result bundle. Head change restarts without writes; unique conflict resolves retry/conflict; compaction shares the head-first lock order; replica/follower/cache/weak-snapshot reads cannot authorize and unsupported adapters refuse `VIT-CAP-061`. At `0.22.0`, pause after proof, after head lock and before unique insert while racing compaction and first execution, and inject mismatched head/hot replica views. Require adapter isolation/locking parity or refusal at `0.23.0–0.27.0`, migration/import preservation at `0.29.0–0.30.0`, storage and HA option freezes at `0.140.2`/`.6`, then implementation, RPC provenance, HA, DR, soak, hardening, compatibility freeze, external pentest and production evidence at `0.141.0–1.0.0`. |
| Repeated replay-head changes had only per-job proof bounds, failover/retry could reset cumulative work, replay-key uniqueness scope was adapter-defined, and compaction/admission fairness was unspecified | At `0.21.0`, freeze `CapacityDrainReplayKeyV1 { tenant_id, deployment_id, action_kind, action_id, idempotency_id }`, with action ID and idempotency ID independently tenant/deployment-unique and required to resolve to one row; one-sided reuse conflicts. Add durable per-logical-attempt `CapacityDrainReplayRestartBudgetV1` with finite automatic restarts, cumulative proof bytes/decode/work/elapsed time/observed-head advances, no reset across head change/failover/crash/cursor/process/adapter retry, and typed no-write `CapacityDrainReplayAdmissionContended` distinct from unavailable history. Freeze finite authenticated-admission/compaction quanta, bounded caller-independent yield and protected Recovery capacity. At `0.22.0`, continuously advance heads, churn publications, reset processes/cursors/adapters, race both unique indexes and attempt Normal/unauthenticated starvation. Require exact adapter constraints and durable accounting or refusal at `0.23.0–0.27.0`, migration/import preservation at `0.29.0–0.30.0`, storage/fairness/HA freezes at `0.140.2`/`.5`/`.6`, and implementation/RPC/HA/DR/soak/hardening/compatibility/external-pentest/production proof at `0.141.0–1.0.0`. |
| Durable logical attempts lacked a closed lifecycle, single-active join rule, fenced crash takeover, cleanup prerequisite and aggregate capacity policy, allowing bounded individual work to create unbounded abandoned state | At `0.21.0`, add `CapacityDrainReplayAdmissionAttemptV1` with Active↔RestartPending and irreversible Succeeded/HistoricalConflict/HistoricalUnavailable/Contended/ExpiredNoExecution terminals. Permit one nonterminal attempt per canonical key; identical key/digest requests join its budget, changed material conflicts. Bind owner workload, boot/continuity, lease generation, fencing token and CAS; takeover preserves counters/deadline, stale owners deny, cancellation/disconnect cannot reset, and success co-commits with replay/action state. Add `AdmissionAttemptCapacityV1` bounds for active/terminal rows/bytes, queues, principals, takeover work, terminalization backlog and cleanup workers, reserve terminalization/cleanup before admission, protect Recovery, and require checkpoint plus result/audit linkage before envelope-only cleanup. At `0.22.0`, test joins/conflicts, every crash edge, takeover/fence races, no-write-terminal-to-success denial, premature cleanup, every saturation dimension and Recovery under Normal floods. Require adapter parity/refusal at `0.23.0–0.27.0`, migration/import at `0.29.0–0.30.0`, storage/privacy/HA freezes at `0.140.2`/`.5`/`.6`, and implementation/RPC/HA/DR/soak/hardening/compatibility/pentest/production proof at `0.141.0–1.0.0`. |
| Attempt capacity reservation, lifecycle transfer, checkpoint release and envelope-deletion release were not one explicit transactional ledger, leaving split creation, duplicate join reservation, stale execution and double-decrement interpretations to adapters | At `0.21.0`, add `AdmissionAttemptCapacityLedgerV1`, canonical original reservation set and stable settlement ID/record. Creation locks replay head→optional settlement journal head→key/attempt→capacity→profile/fence/domain, checks/reserves every active/terminal/terminalization/checkpoint/cleanup/deletion bucket, inserts the attempt and commits all-or-none; joins persist no waiter and allocate nothing. Every transition uses that order. Success rechecks Active/key/owner/boot/lease/fence/CAS/deadline/budget/head/authority and co-commits action plus active-to-terminal transfer; no-write terminals transfer atomically. Checkpoint settles only terminalization/backlog; physical envelope deletion settles envelope/cleanup/deletion; unique reservation-set/leg records reconcile duplicates/unknowns and preserve original buckets across profile changes. At `0.22.0`, crash/fault every lock/reserve/insert/transition/checkpoint/delete/settlement/response edge. Require adapter parity/refusal at `0.23.0–0.27.0`, migration/import conservation at `0.29.0–0.30.0`, storage/privacy/HA freezes at `0.140.2`/`.5`/`.6`, and implementation/RPC/HA/DR/soak/hardening/compatibility/pentest/production proof at `0.141.0–1.0.0`. |
| Physical-deletion settlement evidence had no bounded post-delete replay contract, so retaining every row was unbounded while deleting rows could let a late duplicate decrement capacity twice | At `0.21.0`, retain every earlier exact-once/bounded/conservative guarantee while reusing authenticated sparse publication with two domain-separated heads and one namespace for checkpoint/deletion legs. `CapacitySettlementJournalHeadV1` advances only in a local checkpoint-or-deletion settlement transaction over its canonical ordered row bundle and implies no archive availability. `CapacitySettlementArchiveReplayHeadV1` advances only after upload/verification; its final transaction locks archive head→journal head→exact captured hot-row IDs/version/range, rechecks continuity, and atomically CAS-installs the archive head plus deletes only those rows. Lookup revalidates H; absent-envelope non-membership never decrements. At `0.22.0`, retain prior tests and add journal/upload/verify/archive-CAS/delete, duplicate/compaction, captured-range, stale-H, mixed-trigger and restore/migration races. Require both CAS boundaries and unified namespace or adapter refusal at `0.23.0–0.27.0`, migration/import proof at `0.29.0–0.30.0`, option freezes at `0.140.2`/`.5`/`.6`, and implementation through production proof at `0.141.0–1.0.0`. |
| The settlement replay head was assigned two incompatible linearization points: the local physical-deletion transaction and the later verified archive-publication transaction | At `0.21.0`, make the local journal head and verified archive replay head different canonical types and state explicitly that neither substitutes for the other. The journal advances for every checkpoint or deletion settlement; bind archive coverage to an exact journal sequence/digest and hot-row version/range. At `0.22.0`, prove each head advances only at its own boundary, publication never over-deletes, a proof for H is revalidated, and archive non-membership cannot release an absent envelope. At `0.23.0–0.27.0`, adapters must demonstrate both CAS boundaries or refuse `VIT-CAP-061`. Preserve both chains and their relationship at `0.29.0–0.30.0` and `0.140.6`; freeze formats at `0.140.2`/`0.148.0`; exercise HA/DR/soak/pentest at `0.143.0`/`0.145.0`/`0.146.0`/`0.149.0`; ship only after the `1.0.0` production gate proves no head conflation. |
| Checkpoint settlements were defined per leg but were not explicitly included in the settlement journal/archive atomic bundle, allowing adapters to journal deletion legs while routing checkpoint legs through a weaker or different replay path | At `0.21.0`, use one unified settlement namespace. Checkpoint settlement locks replay head→journal head→key/attempt→capacity→domain and atomically commits original terminalization/backlog decrements, immutable per-leg rows, journal append, attempt checkpoint, audit and result; deletion follows the same protocol for separate legs. Archive replay covers mixed triggers. At `0.22.0`, test checkpoint response loss, duplicate retry before/after compaction, publication races, mixed archives, trigger/leg substitution, between-stage restore/migration and unavailable checkpoint proof. Adapters must prove parity or refuse at `0.23.0–0.27.0`; preserve stages at `0.29.0–0.30.0`; freeze schema/privacy/recovery at `0.140.2`/`.5`/`.6`; prove implementation/RPC/HA/DR/soak/audit/compatibility/pentest/production at `0.141.0–1.0.0`. |
| The invariant-owner recovery paragraph still exposed a singular settlement replay tuple after Phase C required distinct journal/archive heads and complete between-stage recovery state | At `0.21.0`, make both heads, both non-wrapping predecessor/sequence chains and authenticated coverage, root/key/publication/cursor, exact covered/current rows, settlement/trigger/ordered-bundle/result identity, attempt-checkpoint linkage, exact settled/remaining legs and conservative original-bucket balances one mandatory `CapacityRecoveryStateV1` tuple. At `0.22.0`, round-trip it, omit each security field and require decode/admission failure, reject singular-head decoding, and restore every checkpoint-to-deletion boundary without reopening or pre-settling legs. `0.29.0` migrates only complete two-head state and `0.30.0` refuses lossy/singular import. Freeze storage/recovery at `0.140.2`/`.6`, prove HA/DR at `0.143.0`/`0.145.0`, freeze compatibility at `0.148.0`, externally pentest omission/default/singular-admission attacks at `0.149.0`, and retain the gate through `1.0.0`. |
| Requiring a registered singular-head migration did not define evidence, head meaning, chain anchoring, row/archive/gap classification, quarantine or response-loss reconciliation, and one head cannot generally authenticate two histories | Repository history shows no tag and only the `0.1.0` scaffold; no Vitheim release admitted a singular settlement schema. Therefore do not add `CapacitySettlementLegacyHeadMigrationV1`. At `0.21.0`, only new empty deployments create both canonical genesis heads. At `0.22.0`, present singular inputs claiming local journal, verified archive, both, empty or complete meaning—including partial or apparently complete rows/chunks/keys/checkpoints/balances—and require identical pre-mutation quarantine; lost refusal/retry creates no head, anchor, tombstone, balance or supersession. `0.29.0` accepts only the complete authenticated two-head tuple, leaves singular sources untouched, keeps destinations fenced/unready and charges conservative; any future real compatibility population requires a new source-specific reviewed milestone. `0.30.0` refuses singular import; `0.140.6` freezes no-migration recovery; `0.145.0` drills backup refusal; `0.148.0` excludes the format; `0.149.0` pentests spoofed schema/evidence and response loss; `1.0.0` ships no singular compatibility claim. |
| Migration/import had bounded individual codecs and cursors but no one durable operation-wide work budget, so millions of valid small records, cumulative crypto/proof work, staging, retries, failover and abandoned cleanup could each bypass a different local bound | Add destination-local `MigrationImportWorkBudgetV1` at `0.29.0`, without making its scheduler an authority root. One immutable operation key admits at most one nonterminal job; exact retries join and changed material conflicts. Persist the frozen profile and monotonic source/decoded-byte, item/blob/chunk, hash/signature/proof, temporary/staged storage, row, file/stream, checkpoint/resume/reconnect/native-retry, conservative-time, cleanup/reconciliation and principal/tenant/deployment concurrency counters across crash, cursor/process replacement, response loss, failover and restore. Pessimistically precharge bounded quanta and reserve staging, verification, result/checkpoint, rollback/quarantine cleanup and protected Recovery capacity at admission. Typed exhaustion preserves the source, fences destination promotion, permits only bounded digest/metadata quarantine and converges cleanup; only an explicitly authorized predecessor-bound profile successor may increase limits. `0.30.0` reuses that exact lineage across export enumeration, transfer, decode, verification and destination staging. Freeze exact limits/topology at `0.140.2`/`.6`; prove HA, DR and hostile soak at `0.143.0`, `0.145.0` and `0.146.0`; audit at `0.147.0`; freeze compatibility at `0.148.0`; externally pentest at `0.149.0`; carry clean continuity through `0.150.0` and require the unchanged contract at `1.0.0`. |
| The migration registry was correctly denied domain authority, but no atomic protocol connected its budget/job terminal state to multiple invariant-owner admissions, leaving exhaustion, cancellation, takeover, partial preparation, cleanup, response loss, failover and restore able to race promotion | At `0.29.0`, add closed `MigrationImportJobLifecycleV1`, canonical `MigrationImportAdmissionCandidateV1`, ordered owner manifest, authenticated dormant-generation `MigrationImportOwnerPreparationReceiptV1` and `MigrationImportActivationBarrierV1`. Every existing owner validates and prepares only its own non-authoritative generation; the registry coordinates completeness without domain authority, while the following disposition formalizes its security-control authority. Through `1.0.0`, support only a co-located activation profile: one fixed-order destination-local transaction rechecks the still-`AdmissionPrepared` job, budget/final counters, lease/fence, absence of terminal failure, trusted-time-bound activation authority, staged root, complete unique receipt set and unchanged owner versions, then activates all owner generations with barrier/job result/audit/outbox or none. Every pre-activation failure advances a permanent candidate fence; after activation cleanup touches only staging. A non-co-located selector would itself become authority and is refused pending a future reviewed invariant/composite law. `0.30.0` applies the same cutover to cross-backend import; freeze storage and HA choice at `0.140.2`/`.6`; prove failover, restore and contention at `0.143.0`, `0.145.0` and `0.146.0`; audit at `0.147.0`; freeze codecs at `0.148.0`; externally pentest at `0.149.0`; carry evidence through `0.150.0` and production. |
| Migration/import job, budget, reservation, lifecycle, lease/fence, candidate/tombstone, barrier and authorization-consumption state remained prose-only authority, while owner selection and activation authorization lacked a trusted derivation and closed independently issued lifecycle | At `0.29.0`, declare `VIT-INV-062 MigrationImportJobAuthorityState` with complete VIT-ENF/CAP/TST/RCV/FEN contracts and `VIT-LAW-009 AtomicMigrationImportActivation`. Derive `MigrationImportOwnerManifestV1` in trusted code from source/destination schemas, migration-plan digest, the authenticated dependency closure of the currently admitted destination VIT-LAW-009 generation and a versioned contributor algorithm; importer input is evidence only and no separate invariant catalog exists. Owners authenticate dormant receipts through owner-held capabilities and VIT-INV-062 receives verify-only access. Add independently issued candidate/scope/staged-root/manifest/counter/policy/SoD/time/key/continuity-bound `MigrationImportActivationAuthorizationV1` with only Issued, Consumed, ExpiredUnused and RevokedUnused states; consume/tombstone it atomically with every selected owner activation, barrier/result/audit/outbox. Register successor VIT-LAW-009 generations at `0.51.1`, `0.52.0`, `0.52.1`, `0.53.0`, `0.53.1`, `0.55.0`, `0.56.0`, `0.57.0`, `0.59.0` and `0.141.0` as new invariant owners become applicable. Freeze crypto/time/issuer at `0.140.1`, storage at `.2`, identity/SoD at `.3`, HA/restore at `.6`, then prove implementation, HA, DR, soak, audit, compatibility, external pentest, RC and production continuity through `1.0.0`. |
| The owner manifest referenced an undefined active invariant catalog, and independent issuer revocation had no destination-local linearization point against activation | Do not create a new authority root. At `0.29.0`, make the authenticated dependency set of the currently admitted destination VIT-LAW-009 generation the complete owner universe; bind its tuple and canonical manifest digest into `MigrationImportOwnerManifestV1`, reject stale/future generations, dependency omission/injection, contributor-algorithm mismatch, digest substitution and self-consistent forged pseudo catalogs. Retain RevokedUnused and add authenticated `MigrationImportActivationRevocationIntentV1` with candidate/authorization/scope, issuer continuity, non-wrapping sequence, signer/key profile, reason, trusted time and idempotency. Only VIT-INV-062's destination-local same-row CAS from Issued to RevokedUnused is effective; atomically persist inbox receipt, sequence high-watermark, intent/tombstone/result/audit/outbox. Exact duplicate returns its result, changed intent conflicts, and late intent after Consumed returns the activation result without reversal. Carry both rules into `0.30.0`; freeze authentication/time at `0.140.1`, storage/lock/CAS at `.2`, issuer/SoD semantics at `.3`, HA/restore at `.6`, RPC at `0.142.0`, HA at `.143`, DR at `.145`, soak at `.146`, audit at `.147`, compatibility at `.148`, external pentest at `.149`, RC at `.150` and production at `1.0.0`. |
| Revocation could arrive before authorization admission, and VIT-INV-062 was both coordinator and an undifferentiated member of the import owner closure, allowing delayed authority to cross revocation or the live registry to import/replace itself | At `0.29.0`, add explicit `AdmitMigrationImportActivationAuthorization` and close the shared row over RevokedBeforeAdmission, Issued, Consumed, ExpiredUnused and RevokedUnused. An authenticated revocation against an absent row creates a permanent candidate/authorization-digest tombstone; late admission joins it. Scope `MigrationImportActivationRevocationSequenceKeyV1` to issuer identity/continuity, tenant/deployment, exact authorization identity/digest and candidate, and bind intent lifetime to the target authorization so unrelated sequences or transport delay cannot suppress revocation. Also type the admitted VIT-LAW-009 closure: VIT-INV-062 is the one live non-importable destination coordinator, while all other applicable entries are importable domain contributors. At `0.30.0`, keep destination job/control state outside staging, refuse nonterminal source jobs, cycles and stable-identity collisions, archive terminal source history only after activation as inert evidence, and migrate the coordinator schema only through a separate predecessor-owned drain/checkpoint/dormant-local-successor/CAS handoff. Freeze crypto/time at `0.140.1`, storage/state/locks at `.2`, identity/SoD/bootstrap roles at `.3`, HA/restore at `.6`, RPC at `0.142.0`, then prove HA at `.143`, DR at `.145`, contention at `.146`, audit at `.147`, compatibility at `.148`, external pentest at `.149`, RC at `.150` and unchanged production enforcement at `1.0.0`. |
| Coordinator bootstrap lacked stable identity, closed transitions, independent exact-target authorization, budgets/results and one global generation guard; terminal registry history lacked executable append semantics; activation lock declarations disagreed | At `0.29.0`, add `MigrationImportCoordinatorGenerationV1` and require every VIT-INV-062 mutation to lock/recheck it immediately after the deployment-retirement fence. Define stable-ID `MigrationImportCoordinatorBootstrapV1`, its closed lifecycle, independently admitted predecessor/successor/schema/code/semantic/law/checkpoint/change/SoD/time-bound authorization, work budget/reservations/cleanup, atomic generation/fence handoff and canonical exact-retry result/conflict. Freeze activation as deployment-retirement-fence→active-coordinator-generation→job→candidate/barrier→authorization→ordered-domain-owner→control-settlement-archive-head→control-settlement-journal-head→recovery-capacity-parent-ledger→backend-storage-cost-active-recost-campaign-slot→backend-storage-cost-recost-campaign-fence→corruption-control-reserve→history-obligation→corruption-fence→corruption-control-lineage→corruption-control-lineage-checkpoint→lineage-disposition→recovery-authorization→clearance-anchor-source-manifest-head→clearance-anchor-source-manifest-authorization→corruption-clearance-anchor-registry→corruption-clearance-scope→corruption-clearance-authorization→corruption-clearance-attempt→corruption-rebuild→corruption-rebuild-rejection-authorization→archive-head→history/idempotency→recovery-lineage-budget→attempt/successor-budget→retention/legal-hold→audit/result/outbox. Also register `MigrationImportRegistryHistoryV1` and `AppendMigrationImportRegistryHistory` with authenticated provenance/scope/original result/export-import manifests/archive namespace/retention/sequence/idempotency, bounded rows/bytes/work, protected cleanup and a closed append/recovery disposition; archive failure never rolls back or ambiguates activation. At `0.30.0`, consume that exact semantic for cross-backend history. Freeze crypto at `0.140.1`, storage/order at `.2`, roles at `.3`, HA at `.6`, carry RPC at `0.142.0`, then test stale transactions, old binaries, competing successors, replay/revoke/cancel, response loss, rollback, restore, failover and archive failure through `0.143.0–0.150.0`, external pentest and unchanged `1.0.0` enforcement. |
| One bootstrap grant had to remain Issued through a long drain, so revocation-before-admission, mid-drain expiry and cancellation could recreate authority or strand the unique bootstrap; history Pending was created only by later append work, leaving an activation-to-obligation crash gap | At `0.29.0`, retain the generation fence but split bootstrap into action-bound `MigrationImportCoordinatorBootstrapBeginAuthorizationV1`, fresh `MigrationImportCoordinatorHandoffAuthorizationV1` and `MigrationImportCoordinatorBootstrapCancellationAuthorizationV1`. Give each destination-local same-row admission/revocation ordering and pre-admission tombstone. Consume begin at drain installation; bind handoff to `MigrationImportCoordinatorBootstrapCheckpointV1` and owner-authenticated `MigrationImportCoordinatorSuccessorVerificationReceiptV1`; make cancellation an executable exact-result command. Begin/handoff authority loss safely terminalizes, clears drain, preserves Recovery evidence and advances the predecessor fence. Also make activation atomically create `MigrationImportRegistryHistoryAppendObligationV1` as bounded Pending or explicit NoHistory/NotRequested; append advances `MigrationImportRegistryHistoryArchiveHeadV1`, and cleanup waits for terminal `MigrationImportRegistryHistoryAppendCheckpointV1`. At `0.30.0`, preserve obligation delivery/restore and consume only Pending. Freeze crypto at `0.140.1`, storage/order at `.2`, roles at `.3`, HA at `.6`, carry RPC at `0.142.0`, then prove authorization reorder/expiry at every state, cancellation response loss/race, crash around activation, missing worker, cleanup race and Pending restore through `.143`, `.145`–`.150`, pentest and `1.0.0`. |
| Bootstrap grants had pre-admission revocation states but no typed delivery/apply protocol, allowing remote-effect inference, grant-specific adapter behavior or one action sequence to suppress another | At `0.29.0`, add one action-discriminated `MigrationImportCoordinatorBootstrapAuthorizationRevocationIntentV1`, exact-grant/action `MigrationImportCoordinatorBootstrapAuthorizationRevocationSequenceKeyV1`, destination-only apply operation and canonical result/conflict. Bind bootstrap/action/authorization digest, both coordinator generations, issuer continuity, target-covering lifetime, reason, nonce and idempotency. The same-row destination transaction alone commits inbox, sequence, tombstone, required bootstrap terminalization, result/audit/outbox; late consumed intent returns the original action result. `0.30.0` transports but never applies this authority remotely. Freeze authentication/time at `0.140.1`, rows/locks at `.2`, issuer/SoD at `.3`, HA/restore at `.6`, RPC semantics at `0.142.0`, then test reorder, cross-action suppression, duplicate/change conflict, lost response, failover and restore through `.143`, `.145`–`.150`, external pentest and `1.0.0`. |
| History append budget exhaustion was named ManualRecoveryRequired but treated as terminal, so checkpoint and cleanup could delete the only material needed for the promised recovery | At `0.29.0`, replace it with nonterminal ManualRecoveryPending. Retain bounded descriptors, exhausted budget/result and reservations; prohibit terminal checkpoint and cleanup. Add independently admitted `MigrationImportRegistryHistoryRecoveryAuthorizationV1` and `ResolveMigrationImportRegistryHistoryRecovery` with closed action semantics refined below. Canonical results/conflicts, exact retries, SoD, retention and restore preserve evidence. `0.30.0` preserves this lineage across backends. Freeze crypto/time at `0.140.1`, storage/order at `.2`, recovery roles at `.3`, retention at `.5`, HA/restore at `.6`, carry RPC at `0.142.0`, then prove worker loss, budget substitution, cleanup races, forged waiver/abandonment and restore through `.143`, `.145`–`.150`, external pentest and `1.0.0`. |
| RetryAppend successor exhaustion silently reached AbandonedWithEvidence, allowing retry authority to exercise abandonment authority; repeated fresh grants could also reset resource ceilings | At `0.29.0`, close the matrix: RetryAppend reaches Appended or ManualRecoveryPending only; Waive alone reaches WaivedFenced; Abandon alone reaches AbandonedWithEvidence. Successor exhaustion commits `MigrationImportRegistryHistorySuccessorBudgetExhausted` and remains pending, so abandonment needs a fresh Abandon grant. Add `MigrationImportRegistryHistoryRecoveryLineageBudgetV1` with immutable cumulative work/time/attempt/byte/successor ceilings spanning initial work and every authorization/backend. Preserve it at `0.30.0`; freeze crypto at `0.140.1`, rows/locks at `.2`, roles at `.3`, HA at `.6`, RPC at `.142`, then prove repeated-exhaustion/non-reset semantics through `.143`, `.145`–`.150`, external pentest and `1.0.0`. |
| History-recovery authorization declared revocation states but no authenticated delivery/apply protocol, leaving pre-admission ordering and remote effect undefined | At `0.29.0`, add `MigrationImportRegistryHistoryRecoveryAuthorizationRevocationIntentV1`, exact obligation/authorization sequence key, destination apply and canonical result/conflict. Action is derived from exact canonical authorization bytes as refined below, never repeated as authority. The same-row local transaction alone commits inbox, sequence and RevokedBeforeAdmission/RevokedUnused tombstone plus audit/outbox; late consumed intent returns the recovery result. `0.30.0` transports without effect. Freeze signer/time at `0.140.1`, storage/CAS at `.2`, issuer/SoD at `.3`, HA/restore at `.6`, RPC at `.142`, then test reorder, duplicate/change conflict, expiry/consume races, response loss and rollback through `.143`, `.145`–`.150`, pentest and production. |
| Waiver and abandonment lacked canonical custody records and commit-time retention/classification/legal-hold authority, so stale or underprivileged recovery authority could weaken evidence obligations | At `0.29.0`, add canonical waiver and abandonment records binding obligation/descriptors/result, retention/classification policy identity/generation/digest, legal-hold state/epoch, records/compliance/legal approval with approver/operator SoD, evidence-retention floor, reason/change/incident and audit/outbox. Commit rechecks all current state; active hold categorically denies any weakening terminal without emergency override. Preserve at `0.30.0`; freeze authentication at `0.140.1`, rows at `.2`, roles at `.3`, evidence/erasure at `.5`, HA/restore at `.6`, RPC at `.142`, then attack stale policy/hold, forged approval, floor weakening and cleanup through `.143`, `.145`–`.150`, external pentest and `1.0.0`. |
| The history-recovery lineage budget called its ceilings immutable but allowed an undefined later policy increase, leaving adapters free to invent capacity without a typed authorization, CAS, result or hard bound | Choose the stricter profile: no lineage-budget amendment or increase operation exists through `1.0.0`. At `0.29.0`, add `MigrationImportRegistryHistoryRecoveryPlatformHardMaximumV1`; require every initial cumulative ceiling to fit it and prohibit policy/profile/issuer/action/backend/idempotency/migration/restore/rollback from raising ceilings, reducing consumed counters, replacing lineage or reusing successor ordinals. `0.30.0` preserves exact ceilings/counters across backends. Freeze storage/refusal at `0.140.2`, HA/restore at `.6`, RPC at `.142`, prove races and old-backup resistance at `.143`/`.145`, hostile successor/policy churn at `.146`, audit at `.147`, freeze bytes at `.148`, externally pentest capacity-creation paths at `.149`, carry evidence through `.150` and enforce unchanged at `1.0.0`. Any future amendment requires a post-`1.0.0` versioned authority review and is unsupported by this plan. |
| Recovery revocation described only absent, issued and consumed behavior; ExpiredUnused and delayed operations against every tombstone were implicit, and the revocation wire type did not enumerate all authentication/time fields | At `0.29.0`, close `MigrationImportRegistryHistoryRecoveryAuthorizationStateV1` over Absent, RevokedBeforeAdmission, Issued, Consumed, ExpiredUnused and RevokedUnused. Define a total same-row admission/expiry/revocation/consumption table with canonical admission, expiry, revocation and recovery results: exact operations join the winning result, changed canonical material conflicts without mutation, and ExpiredUnused always returns expiry without conversion. Bind signer identity, key identity/epoch, authentication profile, issued-at, not-before, exact target expiry, maximum uncertainty and trusted-time profile/epoch directly in the revocation intent. Preserve at `0.30.0`; freeze crypto/time at `0.140.1`, storage/CAS at `.2`, HA at `.6`, RPC at `.142`, race every state at `.143`/`.146`, restore at `.145`, audit/freeze/pentest at `.147`–`.149`, then RC and production. |
| History-recovery authorization and result used one flat payload, so waiver/abandonment could carry meaningless retry budgets and codecs could disagree about mixed or unknown action material | At `0.29.0`, introduce closed `MigrationImportRegistryHistoryRecoveryActionV1` and `MigrationImportRegistryHistoryRecoveryResultPayloadV1`. RetryAppend alone carries predecessor/successor budget material; Waive and Abandon carry only their matching authority/policy/custody material. Other variants are canonically absent. Reject unknown/duplicate discriminants, mixed or action-inapplicable fields, non-minimal options, `Some(empty)`, and changed-action reuse of an authorization identity before allocation/admission. `0.30.0` preserves exact bytes. Freeze authentication at `0.140.1`, codecs/storage at `.2`, roles at `.3`, custody at `.5`, HA at `.6`, RPC at `.142`, then fuzz and race through `.143`, `.145`–`.147`, freeze at `.148`, externally pentest type-confusion at `.149`, and retain identical RC/`1.0.0` behavior. |
| The cumulative history lineage covered initial work in prose but was created/locked only around recovery, allowing initial counters to be omitted and lazily reconstructed after exhaustion | At `0.29.0`, activation atomically commits `MigrationImportRegistryHistoryRecoveryLineageBudgetV1` beside every Pending obligation with exact hard-maximum profile ID/version/digest, immutable ceilings, zero counters, ordinal zero and no predecessor. Add closed `MigrationImportRegistryHistoryLineageDispositionV1`: NoHistory/NotRequested instead commit `MigrationImportRegistryHistoryNoExecutableLineageV1`. Initial append locks lineage before its attempt budget and co-charges both atomically. Missing/mismatched lineage for Pending/ManualRecoveryPending returns `MigrationImportRegistryHistoryLineageStateMissing` only after committing the exact-obligation corruption fence defined below and cannot be repaired lazily. `0.30.0` preserves exact state across backends; freeze storage at `0.140.2`, recovery at `.6`, RPC at `.142`, race/fault at `.143`/`.146`, restore at `.145`, audit/freeze/pentest at `.147`–`.149`, then RC and unchanged production enforcement. |
| Explicit expiry could mutate Absent into ExpiredUnused without stored authenticated authority, enabling adapter-defined guessed-identity denial tombstones; operations also lacked one shared representation for returning another operation's winning result | At `0.29.0`, restrict `ExpireMigrationImportRegistryHistoryRecoveryAuthorization` to stored Issued grants and trusted time. Expiry/consumption on Absent returns typed no-write `MigrationImportRegistryHistoryRecoveryAuthorizationNotAdmitted`; admission of a complete authentic expired grant is the sole Absent-to-ExpiredUnused path. Add closed `MigrationImportRegistryHistoryRecoveryAuthorizationOutcomeV1` with Admitted, Expired, Revoked, Consumed and NotAdmitted, returned by all four operations with typed conflict outside the union. `0.30.0` preserves signatures/bytes; freeze crypto/time at `0.140.1`, storage/API at `.2`, HA at `.6`, RPC at `.142`, race at `.143`/`.146`, restore at `.145`, audit/freeze/pentest at `.147`–`.149`, then RC/production. |
| Recovery authorization and result encoded action both in an outer envelope and an inner tagged union, while revocation repeated action beside an exact authorization digest, permitting contradictory dual authority | At `0.29.0`, make the `MigrationImportRegistryHistoryRecoveryActionV1` and `MigrationImportRegistryHistoryRecoveryResultPayloadV1` variant tags the sole authoritative action. Remove the outer action field; revocation targets exact canonical authorization identity/digest and derives the tag from those bytes. Any denormalized action column/index is derived, never independently writable or hashed, and equality is verified on every read; mismatch is corruption. `0.30.0` preserves sole-tag bytes. Freeze authentication at `0.140.1`, codecs/storage/index rules at `.2`, roles at `.3`, HA at `.6`, RPC at `.142`, then race/fuzz derived mismatches and legacy dual encodings through `.143`, `.145`–`.147`, freeze at `.148`, externally pentest at `.149`, and retain identical RC/production meaning. |
| NotRequested could decline eligible archival during activation without the classification/legal-hold/evidence-floor/compliance/legal/SoD controls required by later Waive/Abandon, creating an earlier custody bypass | At `0.29.0`, add `MigrationImportRegistryHistoryNotRequestedRecordV1` binding source manifest, eligible digest/count, retention/classification policy identity/generation/digest, legal-hold state/epoch, evidence floor, decision provenance, compliance/legal approvals and SoD, activation result, commit-time receipt and audit/outbox. Activation locks/rechecks current retention/legal-hold authority under the canonical order; active-hold weakening denies. Keep NoHistory separate as authenticated zero-count/empty-set proof. `0.30.0` preserves both exact meanings. Freeze authentication at `0.140.1`, storage/order at `.2`, roles at `.3`, custody at `.5`, HA/restore at `.6`, RPC at `.142`, race at `.143`, restore at `.145`, fuzz at `.146`, audit/freeze/pentest at `.147`–`.149`, then RC and unchanged production enforcement. |
| Missing lineage said it quarantined an obligation but had no owned state, scope, transition, result or clearance protocol, allowing ephemeral errors, invented dispositions, tenant-wide quarantine, retry loops or unsafe cleanup | Keep append disposition unchanged. At `0.29.0`, add activation-created obligation-scoped `MigrationImportRegistryHistoryCorruptionFenceV1` with non-wrapping generation and closed Healthy/Fenced/ClearedAfterRestore state, `FenceMigrationImportRegistryHistoryCorruption`, canonical result/conflict and universal fence-before-budget locks. Detection becomes effective only when the local fence/result/audit/outbox transaction commits; append/recovery/cleanup recheck it and cannot create lineage or infer counters. `RestoreMigrationImportRegistryHistoryAtomicBundle` consumes separately admitted single-use authority and can clear only through the ratcheted anchor, durable attempt and canonical counter rules refined below; incomplete evidence leaves a fence. Preserve state/results at `0.30.0`; freeze authentication at `0.140.1`, storage at `.2`, roles at `.3`, custody at `.5`, HA at `.6`, RPC at `.142`, race/restore/fuzz/audit/freeze/pentest through `.143`, `.145`–`.149`, then RC/production. |
| The unified recovery outcome still exposed three incompatible top-level conflict types, and same-target operations lacked explicit CAS-loser semantics | At `0.29.0`, add closed `MigrationImportRegistryHistoryRecoveryAuthorizationOperationConflictV1` wrapping only Authorization, Revocation and Recovery changed-material detail; existing conflicts remain canonical inner variants. Add `Fenced(CorruptionResultV1)` to the outcome because corruption is observed state, not conflict. All four authorization operations expose `Result<OutcomeV1, OperationConflictV1>`. Different valid operations on the same exact authorization are not changed material. After CAS loss, reread/reapply the state table: admission may precede RevokedUnused or ExpiredUnused, and consumption/expiry/revocation serialize with the first terminal winner. Conflict only on changed target bytes/identity/digest/scope or reused idempotency identity with changed canonical material. Preserve at `0.30.0`; freeze API/auth at `0.140.1`, storage at `.2`, HA at `.6`, RPC at `.142`, race at `.143`/`.146`, restore at `.145`, audit/freeze/pentest at `.147`–`.149`, then RC/production. |
| Append acquired the corruption fence before lineage budget while detection acquired lineage budget before the fence, and the fence had no activation-created genesis, permitting deadlock, backend-dependent missing-row behavior and absence-as-healthy bypass | At `0.29.0`, freeze the universal order active-generation→obligation→corruption-fence→lineage-disposition→recovery-authorization→clearance-authorization→archive-head→history/idempotency→lineage-budget→attempt/successor-budget→retention/legal-hold→audit/result/outbox. Every operation skips only inapplicable positions without reordering. Activation atomically creates Healthy fence generation zero beside every obligation/disposition/evidence; absence, rollback or wraparound denies. Detection locks the fence before reading missing lineage and increments Healthy/ClearedAfterRestore to Fenced; authorized restoration increments Fenced to ClearedAfterRestore. `0.30.0` preserves exact order/state across adapters; freeze storage/absent-row semantics at `0.140.2`, HA at `.6`, RPC at `.142`, model-check races/deadlocks at `.143`/`.146`, restore at `.145`, audit/freeze/pentest at `.147`–`.149`, then RC/production. |
| Corruption clearance could restore authoritative counters/results/heads under an unnamed Recovery/restore authority and accepted completeness only through the local observation, creating a new authority root and allowing an authentic but stale rollback bundle | At `0.29.0`, define `MigrationImportRegistryHistoryCorruptionClearanceAuthorizationV1` with an independent issuer/admitter boundary, exact obligation/fence generation/corruption digest/proposed root/current heads, trusted time/authentication/quorum/SoD, total admission/revocation/expiry/consumption lifecycle, canonical outcome/conflict/retry and immutable verification budget. Bind its anchor set to the destination-ratcheted registry refined below. The atomic restore consumes one Issued grant and proves coverage through every mandatory registry anchor before updating counters/heads and moving to ClearedAfterRestore. Unprovable state returns typed evidence and remains permanently fenced; the only fallback is the predecessor-unique rebuild refined below. Preserve at `0.30.0`; freeze anchor/authentication profiles at `0.140.1`, rows/locks at `.2`, roles at `.3`, evidence retention at `.5`, HA at `.6`, RPC at `.142`, race/DR/fuzz/audit/compat/pentest at `.143`, `.145`–`.149`, then RC/production. |
| Activation authorization did not bind whether history would be Pending, NoHistory or NotRequested, so an executor could substitute a custody-reducing disposition after general activation approval | At `0.29.0`, bind the exact `MigrationImportRegistryHistoryLineageDispositionV1` tag into `MigrationImportAdmissionCandidateV1`, `MigrationImportActivationBarrierV1` and `MigrationImportActivationAuthorizationV1`. Pending binds obligation/budget/lineage-profile digests; NoHistory binds zero-eligibility proof digest; NotRequested binds custody record/approval digest; all bind retention/classification/legal-hold identities/epochs. Commit-time recheck remains mandatory and any tag, evidence or epoch change returns `MigrationImportActivationHistoryDispositionConflict` and requires fresh authorization. Preserve at `0.30.0`; freeze authentication at `0.140.1`, storage/codecs at `.2`, issuer/SoD at `.3`, custody at `.5`, HA at `.6`, RPC at `.142`, race/restore/fuzz/audit/freeze/pentest at `.143`, `.145`–`.149`, then RC/production. |
| Durable lineage corruption was encoded as LineageCorrupt inside an operation conflict wrapper even though no request identity or idempotency material conflicted | At `0.29.0`, remove LineageCorrupt from `MigrationImportRegistryHistoryRecoveryAuthorizationOperationConflictV1` and add `Fenced(MigrationImportRegistryHistoryCorruptionResultV1)` to the closed outcome. Admission/consumption observe Fenced without mutation; expiry/revocation may still safely terminalize an existing grant but cannot clear/bypass the fence. Reserve conflicts for canonical Authorization/Revocation/Recovery material mismatch. Preserve wire meaning at `0.30.0`; freeze API at `0.140.1`, codecs/storage at `.2`, RPC at `.142`, race/fuzz at `.143`/`.146`, restore at `.145`, audit/compat/pentest at `.147`–`.149`, then RC/production. |
| A correctly signed but stale clearance anchor set could claim to be “greatest-known,” while open signed-checkpoint/external-commitment/witness “and/or” semantics let the clearance issuer omit a stronger class or quorum | At `0.29.0`, add destination-ratcheted `MigrationImportRegistryHistoryCorruptionClearanceAnchorRegistryV1`, non-wrapping generation/predecessor, versioned mandatory anchor classes/per-class quorum, component-wise monotonic high-watermarks and independently authenticated `MigrationImportRegistryHistoryCorruptionClearanceAnchorCollectionReceiptV1`. Neither local corrupt state nor clearance roles nominate it. Admission and final restore recheck the current registry; pre-admission stale input is no-write and advancement during verification consumes the stale grant without clearing via typed `MigrationImportRegistryHistoryCorruptionClearanceAnchorSetStale`. Preserve exact ratchet/receipt at `0.30.0`; freeze authentication/class/quorum at `0.140.1`, rows/CAS/order at `.2`, role separation at `.3`, retention at `.5`, HA at `.6`, RPC at `.142`, race/DR/fuzz/audit/compat/pentest at `.143`, `.145`–`.149`, then RC/production. |
| Clearance had six authorization states but no normative operation table or durable attempt, leaving proof charging, external-evidence outages, takeover, crash recovery, terminal consumption, CAS losers and “conservative counters” adapter-defined | At `0.29.0`, freeze the six-state admission/expiry/revocation/restore table and one `MigrationImportRegistryHistoryCorruptionClearanceAttemptV1` per authorization with stable identity, Prepared/Verifying/EvidenceWait/Cleared/Unprovable/BudgetExceeded/AnchorSetStale/AuthorizationLost lifecycle, lease/fence, cursor, cumulative counters and canonical result. Precharge every proof execution; crash/cancel/takeover retains charges and a re-execution precharges again. EvidenceTemporarilyUnavailable resumes the same Issued attempt; Unprovable, BudgetExceeded and post-admission AnchorSetStale consume without clearing; expiry/revocation terminalizes AuthorizationLost. The refined fence-wide scope and typed state algebra below govern replacement capacity and restoration; the counter join is only the compatible consumed-work G-counter case, not a blanket field-wise merge. Preserve isomorphically at `0.30.0`; freeze storage/table at `0.140.2`, worker roles at `.3`, HA at `.6`, RPC at `.142`, model-check/fault/restore/audit/compat/pentest at `.143`, `.145`–`.149`, then RC/production. |
| Successor rebuild lacked a durable predecessor relationship, uniqueness and archive read-selection rule, so multiple generations could rebuild one fence and expose competing current histories | At `0.29.0`, add one immutable `MigrationImportRegistryHistoryCorruptionRebuildRecordV1` parent keyed by old obligation/fence generation and binding corruption result, anchor registry/set/receipt, terminal unprovable scope result, stable rebuild identity, bounded proposal ceiling and disjoint old-evidence/new-live namespaces. Retryable candidate failures are immutable bounded proposal attempts and do not consume the parent; malformed, stale or unauthorized proposals are no-write. The parent moves from Open to Activated for at most one successor, while PermanentlyRejected requires separate action-bound authority, custody justification, trusted time, SoD and audit. Activation atomically rechecks the old fence, permanent-unprovable scope/result, current anchor/source state, coordinator generation and legal-hold/custody state. Reads/exports select neither before activation and only the mapped successor afterward while retaining old evidence. Preserve/check the parent/proposal/rejection model at `0.30.0`; freeze storage at `0.140.2`, roles at `.3`, custody at `.5`, HA at `.6`, RPC at `.142`, race/DR/fuzz/audit/compat/pentest at `.143`, `.145`–`.149`, then RC/production. |
| Clearance authorization was unique only per authorization, so competing valid grants could occupy one fence and replacement after BudgetExceeded could reset cumulative proof work indefinitely | At `0.29.0`, create `MigrationImportRegistryHistoryCorruptionClearanceScopeV1` atomically with each Fenced generation. It owns the sole live authorization identity and non-wrapping generation, immutable `MigrationImportRegistryHistoryCorruptionClearanceScopeHardMaximumV1`, lifetime cumulative verification counters and Open/PermanentlyUnprovable/Cleared/RebuildActivated disposition. Expiry, revocation, AnchorSetStale or per-grant BudgetExceeded may release occupancy only while preserving charges; EvidenceWait retains it. Replacement authority receives only checked remaining fence-lifetime capacity. Authenticated Unprovable or lifetime exhaustion permanently tombstones clearance, and every terminal disposition invalidates stale grants. Preserve exactly at `0.30.0`; freeze authentication/source bindings at `0.140.1`, rows/CAS/locks at `.2`, scope/issuer/worker SoD at `.3`, HA at `.6`, RPC/races/restore/fuzz/audit/compat/pentest at `.142`–`.149`, then RC and production. |
| The clearance-anchor registry had no explicit genesis and “full queried source set” had no independently governed expected set, permitting lazy adapter initialization or clearance-role weakening of source classes, quorum, time and key continuity | At `0.29.0`, independently initialize the governed `MigrationImportRegistryHistoryCorruptionClearanceAnchorSourceManifestV1` lineage before activation as refined below; activation then binds its current committed head and atomically creates canonical obligation-scoped registry generation zero. Missing state is corruption and adapters may never synthesize either lineage. The manifest binds exact expected source identities/classes, generation/digest and policy generation, quorum/nonresponse rules, collection interval/expiry/uncertainty/trusted-time profile and key continuity. Any weakening requires the complete independently admitted policy-transition lifecycle refined below. Collection receipts prove responses and nonresponses against that full expected set. Preserve exact genesis/manifest state at `0.30.0`; freeze authentication/policy at `0.140.1`, storage/activation order at `.2`, independent roles at `.3`, retention at `.5`, HA at `.6`, RPC/races/restore/fuzz/audit/compat/pentest at `.142`–`.149`, then RC and production. |
| A component-wise maximum was conservative only for monotonic work counters; applying it to ceilings, remaining capacity, causal heads, balances or reservations could synthesize a state that never existed | At `0.29.0`, define partial typed `MigrationImportRegistryHistoryCorruptionClearanceStateJoinV1`: compatible consumed-work G-counters use checked maxima; immutable ceilings and profiles require exact equality; remaining capacity is derived by checked subtraction; ordinals and journal/archive/checkpoint heads select one authenticated causal chain; balances and reservations come from one internally consistent authenticated snapshot. Forked, incomparable, overflowing, conservation-breaking or otherwise undefined evidence remains fenced. Require property/model tests for idempotence, associativity, commutativity, conservation, overflow and incompatible evidence. Preserve at `0.30.0`; freeze types/codecs at `0.140.2`, model-check at `.143`, restore at `.145`, fuzz at `.146`, audit/compat/pentest at `.147`–`.149`, then RC and production. |
| A terminal Rejected state on the predecessor-unique rebuild row could consume the only successor mapping without defining rejection authority or permitting a corrected candidate | At `0.29.0`, separate the immutable rebuild parent from bounded `MigrationImportRegistryHistoryCorruptionRebuildProposalAttemptV1` rows. Invalid, stale, unauthorized and malformed candidates return `MigrationImportRegistryHistoryCorruptionRebuildProposalNotAdmitted` without writing either parent or attempt. Retryable validation failure records only its proposal attempt. `PermanentlyRejectMigrationImportRegistryHistoryCorruptionRebuild` requires independent `MigrationImportRegistryHistoryCorruptionRebuildRejectionAuthorizationV1`, action/parent binding, custody reason, trusted time, SoD and audit, while activation can still select at most one successor and rechecks every fence/anchor/coordinator/custody precondition atomically. Preserve at `0.30.0`; freeze storage at `0.140.2`, roles at `.3`, custody at `.5`, HA at `.6`, RPC/race/restore/fuzz/audit/compat/pentest at `.142`–`.149`, then RC and production. |
| The anchor source-manifest lineage had successor semantics but no authoritative genesis/current-head protocol, leaving the “current” manifest adapter- or deployment-defined | At `0.29.0`, add stable tenant/deployment `MigrationImportRegistryHistoryCorruptionClearanceAnchorSourceManifestLineageV1` with Uninitialized/Active state, a CAS-protected operational-active head and a separate transition-candidate head. `InitializeMigrationImportRegistryHistoryCorruptionClearanceAnchorSourceManifest` is the sole independently authorized absent-to-generation-zero transition. Genesis and successors use exact non-wrapping predecessor links and the staged transition/checkpoint activation protocol refined below; fork/duplicate/gap/reorder/wrap denies. Restore/import selects the greatest externally witnessed authenticated Active record reachable from genesis and equal to the operational-active head; one reachable non-authoritative candidate may remain, and neither raw maximum generation nor a local row alone is authoritative. Preserve at `0.30.0`; freeze checkpoint authentication/time profile at `0.140.1`, rows/CAS/codecs at `.2`, publication/activation roles at `.3`, retention at `.5`, HA at `.6`, RPC/race/DR/fuzz/audit/compat/pentest at `.142`–`.149`, then RC and production. |
| Registry advancement could leave an otherwise Open clearance scope permanently bound to an old manifest/registry generation | At `0.29.0`, make `AdvanceMigrationImportRegistryHistoryCorruptionClearanceAnchorRegistry` lock registry→scope→active authorization→attempt and commit one idempotent advancement/rebinding result. An Open scope without a live grant updates only its binding while preserving generations, hard maximum and lifetime counters. A live grant/attempt terminalizes AnchorSetStale, retains every charge, releases the slot, advances authorization generation and installs the new binding. PermanentlyUnprovable/Cleared/RebuildActivated scope bytes never change. Preserve at `0.30.0`; freeze rows/order at `0.140.2`, roles at `.3`, HA at `.6`, RPC/race/restore/fuzz/audit/compat/pentest at `.142`–`.149`, then RC and production. |
| Source-manifest weakening and permanent rebuild rejection were powerful signed grants without complete destination-local admission/revocation/expiry lifecycles | At `0.29.0`, give both exact action/target/predecessor/proposed-successor-or-canonical-absence bytes and closed Absent/RevokedBeforeAdmission/Issued/Consumed/ExpiredUnused/RevokedUnused rows. Freeze explicit admission, target-scoped non-wrapping revocation sequence, pre-admission and Issued revocation, Issued-only expiry, destination tombstone/result/audit/outbox, total operation table and CAS-loser reread/reapply. Their revocation intents use the complete canonical preimages refined below rather than inheriting identity/time/key fields implicitly. Consumption is atomic with manifest initialization/weakening or parent PermanentlyRejected. Remote issuance/revocation alone has no effect. Preserve at `0.30.0`; freeze crypto/time at `0.140.1`, storage at `.2`, identities/SoD at `.3`, retention at `.5`, HA at `.6`, RPC/race/DR/fuzz/audit/compat/pentest at `.142`–`.149`, then RC and production. |
| Ordinary storage saturation could prevent corruption fencing/scope creation, and the clearance-scope maximum had no authoritative derivation | At `0.29.0`, every Pending/NoHistory/NotRequested activation creates non-borrowable `MigrationImportRegistryHistoryCorruptionControlReserveV1` from the deployment Recovery partition and the obligation-wide control lineage refined below. It covers bounded rows/bytes/proof/retry/time/audit/outbox for every future fence episode, scope, grant/attempt terminalization and result. Trusted code derives stored hard maxima from authenticated active `MigrationImportRegistryHistoryCorruptionControlCapacityProfileV1`, bounded by immutable `MigrationImportRegistryHistoryCorruptionControlPlatformHardMaximumV1`; detector/adapter input cannot set them. Profile reductions preserve existing reservations. Saturation tests prove normal work cannot block fencing/terminalization, while insufficient initial or future minimum capacity makes activation no-write or the lineage permanently quarantined. Preserve at `0.30.0`; freeze rows/reservations/profile/derivation at `0.140.2`, deployment/HA at `.6`, RPC evidence at `.142`, race/DR/saturation/audit/compat/pentest at `.143`, `.145`–`.149`, then RC and production. |
| A locally self-consistent older database could roll back the source-manifest current head because genesis, activation records and head were all stored in the same rollback domain | At `0.29.0`, add the governed staged protocol refined below with distinct operational-active and transition-candidate local heads plus proposal and active external high-watermarks. Restore requires the operational head to equal externally Activated membership while a candidate may be empty, equal or one reachable non-authoritative descendant. Missing selected-Active proof is unavailable/rolled-back; an unresolved candidate blocks itself without poisoning its predecessor. Preserve all artifacts at `0.30.0`; freeze crypto/profile at `0.140.1`, rows/CAS at `.2`, roles at `.3`, retention at `.5`, HA at `.6`, and prove RPC/race/DR/fuzz/audit/freeze/pentest through `.142`–`.149`, RC and production. |
| Clearing corruption and later re-fencing the same obligation created a fresh scope, allowing episode work and protected reserve capacity to reset across fence generations | At `0.29.0`, create `MigrationImportRegistryHistoryCorruptionControlLineageV1` atomically with every history disposition and original reserve. It survives all fence generations with non-wrapping episode ordinal, maximum episodes, the split typed ledgers refined below, active fence/scope and Active/PermanentlyQuarantined/Rebuilt/ReleasePending/Released disposition. Clearance never resets it; insufficient future capacity quarantines. Rebuild terminalizes the predecessor. Release requires custody-safe proof/checkpoint and rollback-safe physical reclaim. Preserve isomorphically at `0.30.0`; freeze rows/locks/accounting at `0.140.2`, roles at `.3`, custody/retention at `.5`, HA at `.6`, and prove RPC/model/DR/fuzz/audit/freeze/pentest through `.142`–`.149`, RC and production. |
| Destructive source-manifest weakening and permanent rebuild-rejection revocation intents did not explicitly carry their own full authentication/time/idempotency preimages | At `0.29.0`, both canonical revocation intent types bind revocation issuer identity and continuity, signer identity, key identity/epoch and authentication profile, issued-at/not-before, target-covering expiry/not-after, maximum uncertainty and trusted-time profile/epoch, closed reason, nonce and a distinct revocation idempotency identity. The canonical not-after must cover the complete target-authorization lifetime. Unknown, duplicate, omitted, empty, substituted or action-inapplicable fields deny before allocation/admission; exact replay returns the recorded local result and changed material conflicts. Preserve exact bytes at `0.30.0`; freeze crypto/time/key rotation at `0.140.1`, codecs/storage at `.2`, issuer/admitter SoD at `.3`, retention at `.5`, HA at `.6`, RPC/race/restore/fuzz/audit/compat/pentest at `.142`–`.149`, then RC and production. |
| A proposal checkpoint could be externally published but never locally activated, causing a greatest-checkpoint restore rule to misclassify the deployment as rolled back forever | At `0.29.0`, add durable publication attempts with stable idempotency, per-witness request/receipt identities, bounded reconciliation and the protected completion reserve refined below. The external journal records ProposedPublished, Activated, Aborted or authenticated PermanentlyUnresolved under separate proposal and active high-watermarks. Local CAS advances only the candidate head; external Activated plus current profile compatibility/ratchet and the winning activation-fence sequence alone advance the operational head. Unknown response is reconciled, never guessed, and orphan cleanup requires abort/supersession membership. Preserve exactly at `0.30.0`; freeze authentication/status at `0.140.1`, rows/CAS at `.2`, roles at `.3`, evidence at `.5`, HA at `.6`, then `.142`–`.149`, RC and production. |
| Corruption-episode creation reserved worst-case work while proof execution also advanced lineage counters, allowing adapters to double charge or disagree about remaining capacity | At `0.29.0`, split the control-capacity envelope as refined below. Lifetime work moves WorkAvailable→EpisodeWorkReserved→nondecreasing WorkSpent atomically with scope state; physical creation moves ReservedUnoccupied→Occupied and later authenticated reclaim moves Occupied→ReclaimPending→Released. Kind-specific transfer IDs prevent double application and no cross-kind/dimension movement is permitted. Preserve exact ledgers at `0.30.0`; freeze storage/accounting at `0.140.2`, roles at `.3`, custody at `.5`, HA at `.6`, then `.142`–`.149`, RC and production. |
| The selected checkpoint publication profile was not structurally bound, allowing caller/adapter witness-mode or quorum substitution and ambiguous restore policy | At `0.29.0`, add stable profile lineage/head plus the concrete trust/lifecycle and compatibility protocol refined below. Profiles bind exact witnesses/quorum/authentication/time and identity/generation/digest in every artifact. NonWeakening/Weakening governs policy strength; MayActivateExisting/TerminalizationOnly/EmergencyDistrust governs outstanding attempts, and a monotonic ratchet plus one concrete linearizable governance-root activation-fence port is rechecked at final activation. No caller selects mode. Preserve at `0.30.0`; freeze crypto/root/rotation/classification at `0.140.1`, rows/codecs at `.2`, roles at `.3`, retention at `.5`, HA at `.6`, then `.142`–`.149`, RC and production. |
| Control-reserve release settled legs locally without explicit membership in the authenticated settlement journal/archive heads, so an older restore could resurrect or settle capacity twice | At `0.29.0`, instantiate the unified settlement protocol with separate local journal and verified archive heads, the shared lock rank and ReleasePending. Begin-release releases ReservedUnoccupied only with the matching Recovery-parent credit and moves Occupied to ReclaimPending without credit while atomically advancing transfer/journal/disposition/result; verified archive plus exact deletion moves ReclaimPending to Released only in the same transaction as its parent credit and finalizes the lineage. Restore proves heads, coverage, exact membership and physical-ledger conservation. Preserve at `0.30.0`; freeze rows/order/compaction at `0.140.2`, roles at `.3`, retention at `.5`, HA at `.6`, then `.142`–`.149`, RC and production. |
| External publication could outlive available local capacity for activation, abort or unresolved terminalization | At `0.29.0`, atomically admit `MigrationImportRegistryHistoryCorruptionClearanceAnchorSourceManifestPublicationAttemptCompletionReserveV1` with every attempt before its first send. It covers all witness/reconciliation, terminal evidence, transition/result, audit/outbox, checkpoint/archive and settlement work. Insufficient capacity makes publication no-write; unknown status retains the reserve; release requires a complete terminal checkpoint, authenticated settlement membership and the same atomic Recovery-parent credit protocol. Preserve at `0.30.0`; freeze crypto bindings at `0.140.1`, storage at `.2`, roles at `.3`, retention at `.5`, HA at `.6`, and test RPC/model/DR/fuzz/audit/freeze/pentest at `.142`–`.149`, RC and production. |
| One capacity equation conflated irreversible work with reclaimable physical occupancy | At `0.29.0`, define `MigrationImportRegistryHistoryCorruptionControlLifetimeWorkBudgetV1` for proof/retry/hash/signature/bytes_processed/time and `MigrationImportRegistryHistoryCorruptionControlPhysicalCapacityLedgerV1` for rows/bytes_stored/audit/outbox/checkpoint capacity. Freeze their separate equations, typed transfers, nondecreasing WorkSpent/Released, signed backend storage-cost profile and Recovery parent-ledger double entry; archive/deletion never refunds work, work completion never releases storage and child Released never occurs without parent credit. Preserve at `0.30.0`; freeze storage at `0.140.2`, roles at `.3`, retention at `.5`, HA at `.6`, then `.142`–`.149`, RC and production. |
| A LocalActivationCommitted transition could finalize under a publication profile that had since been revoked or emergency-distrusted | At `0.29.0`, anchor profile genesis/rotation in a non-recursive provisioned/compiled root and explicit bootstrap/prepare/activate/abort/checkpoint/high-watermark/restore operations. Every successor records MayActivateExisting, TerminalizationOnly or EmergencyDistrust and advances a monotonic revocation ratchet. Profile transition, distrust and manifest activation serialize through the concrete linearizable `MigrationImportRegistryHistoryManifestGovernanceActivationFencePortV1`; authenticated receipt/status and finality prove one sequence winner across response loss. Finalization rechecks current head/ratchet; the latter two externally abort LocalActivationCommitted without changing operational Active when they won the fence, while a prior activation winner is immutable and later distrust governs successors. Preserve at `0.30.0`; freeze root/crypto/fence at `0.140.1`, rows at `.2`, roles at `.3`, retention at `.5`, HA at `.6`, then `.142`–`.149`, RC and production. |
| The canonical lock order contradicted fence-before-lineage prose and reserve settlement omitted the history obligation | At `0.29.0`, freeze `MigrationImportRegistryHistoryLockRankV1` with Recovery-parent→child-reserve→obligation→fence→lineage→lineage-checkpoint and require acquisition-trace conformance. Release also locks settlement heads before that prefix and rechecks custody authority at its later rank. No adapter-local mapping exists. Preserve at `0.30.0`; freeze storage/order at `0.140.2`, roles at `.3`, HA at `.6`, and race/DR/fuzz/audit/freeze/pentest at `.143`, `.145`–`.149`, RC and production. |
| Restore tests required external Activated state to equal a generic local head even though unresolved descendants were allowed | At `0.29.0`, replace the generic local head with explicit `OperationalActiveHeadV1` and `TransitionCandidateHeadV1`. Restore requires only the operational head to equal externally Activated membership; the candidate may be empty, equal or one reachable non-authoritative descendant. Preserve at `0.30.0`; freeze rows at `0.140.2`, HA at `.6`, and prove RPC/model/DR/fuzz/audit/freeze/pentest through `.142`–`.149`, RC and production. |
| Child capacity could reach Released without an atomic parent Recovery-pool credit, allowing leaks, duplicate credits or split parent/child state | At `0.29.0`, add co-located `MigrationImportRegistryHistoryRecoveryCapacityParentLedgerV1` and stable parent/child transfer IDs/rows. ParentTotal = ParentAvailable + exact active-child encumbrances + exact campaign-RecostPending encumbrances + exact pending-successor encumbrances. Allocation atomically debits parent and creates child ReservedUnoccupied; final child release and parent credit commit together with settlement evidence. Campaign application atomically moves equal amounts from RecostPending to pending successor; forward/inverse transfers and activation reclassification preserve the same total. One logical operation deterministically maps to one transfer ID whose immutable result binds expected/resulting versions, balances, membership and settlement digest; timeout cannot mint another ID. Recovery verifies both sides from one snapshot and never chooses a side or guesses compensation. The shared lock rank places the parent and campaign fence before every child reserve, and publication-attempt completion reserves use the same protocol. Exact retry joins; changed/one-sided/gapped material fences; a backend without parent/child transactionality refuses. At `0.30.0`, preserve versions/transfers/equation without source-state inference. Freeze rows/order at `0.140.2`, roles at `.3`, retention at `.5`, HA at `.6`; carry RPC/model/DR/fuzz/audit/freeze/pentest through `.142`–`.149`, then RC and production. |
| The governance-root external activation fence was an abstraction without a capability contract, so independent signatures could be mistaken for linearizable CAS | At `0.29.0`, define `MigrationImportRegistryHistoryManifestGovernanceActivationFencePortV1` with domain-separated tenant/deployment/profile-lineage key, expected sequence/predecessor, closed ActivateManifest/AbortManifest/SealPermanentlyUnresolved/RotateProfile/EmergencyDistrust/RetireDeploymentIdentity operation and digest, stable request identity, one atomic winning-value install, authenticated receipt/status lookup, unknown-outcome reconciliation, non-wrapping finality/fork/rollback detection, quorum-intersection or consensus assumptions, rotation verification and typed unavailable history. Manifest-terminal operations additionally install one immutable transition terminal map; retirement permanently closes the old key under its independent authority. Request identity permanently binds one digest; CAS and status reads are linearizable, final winner/loser results are immutable, `n + 1` requires readable final `n`, and compaction retains authenticated request-result membership. Independent signatures or last-write-wins storage cannot claim it. At `0.30.0`, preserve exact request/receipt/status/finality. Freeze crypto/consensus assumptions at `0.140.1`, storage at `.2`, roles at `.3`, evidence at `.5`, HA at `.6`; prove RPC/concurrency/DR/fuzz/audit/compat/pentest at `.142`–`.149`, then RC and production. |
| `bytes_stored` had no portable backend meaning, allowing a logically conserved ledger to understate real storage | At `0.29.0`, bind every physical child allocation to signed/versioned `MigrationImportRegistryHistoryBackendStorageCostProfileV1`: backend family/version, schema/index generation, artifact/canonical-encoding kind, fixed overhead, canonical positive rational expansion numerator/nonzero denominator, rounding/allocation unit and measurement semantics. Charge conservatively from canonical encoded size with wide checked ceiling arithmetic and canonical boundary/golden vectors covering fractional rounding, fixed/index overhead, aggregation and overflow; an unbounded combination is unsupported. Runtime disk/WAL/page/temp guards remain separate and may fence earlier. At `0.30.0`, destination import recomputes charges from canonical artifacts under the destination profile and never copies source counters. Freeze profile/rows/vectors at `0.140.2`, signer/owner at `.3`, evidence at `.5`, HA at `.6`; carry RPC/model/DR/fuzz/audit/compat/pentest through `.142`–`.149`, then RC and production. |
| Publication-root loss/compromise and compatibility over outstanding attempts lacked closed `1.0.0` decisions | At `0.29.0`, scope each publication-profile lineage to one tenant/deployment and persist an Empty-or-one nonterminal attempt slot; its exact tenant/deployment/profile-generation/version/attempt digest is CAS-filled and only the matching authenticated terminal checkpoint clears it. Restore rejects stale Empty contradicted by external evidence, so succession classifies zero or one attempt without unbounded enumeration. For `1.0.0`, choose permanent fail-closed reprovisioning: no offline/break-glass in-place root recovery exists. Immutable retirement evidence binds the old identity/head/high-watermark/fence/root/incident/time; a separately authenticated replacement genesis may cite it only as provenance and inherits no profile, fence, slot, ratchet, high-watermark, authorization or idempotency state. A future recovery quorum requires a new reviewed law generation and can never be inferred. Preserve at `0.30.0`; freeze scope/root choice at `0.140.1`, rows at `.2`, roles at `.3`, retention at `.5`, HA at `.6`; prove RPC/race/DR/fuzz/audit/compat/pentest at `.142`–`.149`, then RC and production. |
| Root-loss reprovisioning could deadlock because no independently authenticated protocol created retirement evidence | At `0.29.0`, define independently rooted `MigrationImportRegistryHistoryDeploymentIdentityRetirementV1`, closed Pending/Retired/EvidenceUnavailable states and the sole `RetireMigrationImportRegistryHistoryDeploymentIdentity` command. One local transaction first commits Pending/safety fence/stable request/outbox; its independently authenticated RetireDeploymentIdentity operation then wins the shared governance sequence and permanently closes the old key without a publication-root signature or distributed transaction. Lost response status-reconciles before terminalization. The retirement authority may safety-fence and record retirement/evidence unavailability only; it cannot rotate, activate, terminalize a manifest, import/export custody or transfer authority. Retired binds exact heads/fence/terminal/incident/custody/time evidence. EvidenceUnavailable permits separately provisioned empty bootstrap under a genuinely new identity while permanently quarantining old custody; missing retirement blocks old-custody movement, not empty bootstrap. Preserve at `0.30.0`; freeze trust/semantics at `0.140.1`, rows at `.2`, roles at `.3`, retention at `.5`, HA at `.6`; prove RPC/races/DR/fuzz/audit/compat/pentest through `.142`–`.149`, then RC and production. |
| External Activated, Aborted and PermanentlyUnresolved dispositions lacked one concrete mutual-exclusion protocol | At `0.29.0`, extend the existing governance-fence port to closed ActivateManifest/AbortManifest/SealPermanentlyUnresolved/RotateProfile/EmergencyDistrust/RetireDeploymentIdentity operations. Manifest-terminal requests bind one transition key and expected ProposedPublished disposition; the same linearizable CAS advances the governance sequence and installs one immutable terminal receipt/status mapping. The external journal/high-watermarks are authenticated projections, never independent writers. Abort before LocalActivationCommitted races on the same sequence, and exact retry/status reconciles response loss; no history may contain two terminal meanings. Preserve exact keys/receipts/status at `0.30.0`; freeze crypto/consensus at `0.140.1`, persistence at `.2`, roles at `.3`, retention at `.5`, HA at `.6`; prove RPC/model/DR/fuzz/audit/compat/pentest through `.142`–`.149`, then RC and production. |
| Signed backend storage-cost profiles lacked governed succession and complete-domain weakening proof | At `0.29.0`, add independently rooted cost-profile lineage/current head, Proposed/Active/Superseded/Rejected transition, activation record, checkpoint/high-watermark and exact-retry operations. A trusted bounded symbolic classifier analytically compares the complete supported rounded-affine artifact-kind/size domain without enumerating every rounding breakpoint. Only a completely proved possible lower charge is ValidWeakening and may consume destructive authorization; unknown, invalid, malformed, overflowed or over-budget comparison is InvalidOrUnverifiable terminal no-write. Golden samples alone never prove non-weakening. One epoch-cut campaign reserves the complete aggregate parent delta, logs live mutation and keeps pending successor charges reversible before its authenticated complete checkpoint activates the profile/backend selector. Preserve at `0.30.0`; freeze trust at `0.140.1`, storage/algorithm at `.2`, roles at `.3`, retention at `.5`, HA at `.6`; carry RPC/model/DR/fuzz/audit/compat/pentest through `.142`–`.149`, then RC and production. |
| Parent-capacity restore verification could require an unbounded active-child scan | At `0.29.0`, atomically maintain per-kind aggregate encumbrance, exact membership/commitment and predecessor-linked parent checkpoints with every transfer. Add VerificationPending/Ready/Fenced state, stable cursor, cumulative work budget and protected Recovery capacity. Restore gates each affected capacity partition, then streams bounded child chunks against a fixed checkpoint; only exact membership/aggregate/transfer/equation proof becomes Ready. Crash resumes without cursor recreation or free rescan; mismatch, churn, unavailable history or exhaustion fences, and a backend without bounded snapshot continuation refuses. Preserve at `0.30.0`; freeze schema/bounds at `0.140.2`, roles at `.3`, evidence at `.5`, HA at `.6`; prove RPC/model/DR/fuzz/audit/compat/pentest through `.142`–`.149`, then RC and production. |
| Retirement authority could destroy availability without a single-use admitted grant, while an unnamed or absent local safety fence could be bypassed by already-admitted work | Strengthen `0.29.0` with `MigrationImportRegistryHistoryDeploymentIdentityRetirementAuthorizationV1`: Absent/RevokedBeforeAdmission/Issued/Consumed/ExpiredUnused/RevokedUnused, independent issuer and destination-local admission, exact tenant/deployment/root/head/fence/incident/reason/time/quorum/SoD/nonce/idempotency binding and same-row admission/revocation/expiry/consumption CAS. Genesis creates `MigrationImportRegistryHistoryDeploymentRetirementFenceV1` as Operational; absence fails closed, and admission/readiness/dispatch/external-send start/commit recheck Operational/RetirementPending/Retired. Pending installation atomically consumes Issued authority and reserves complete non-borrowable reconciliation/custody/terminal/audit/outbox capacity. Preserve exact tombstones/fence/reserve at `0.30.0`; freeze authentication at `0.140.1`, rows/locks at `.2`, roles at `.3`, evidence at `.5`, HA at `.6`; carry RPC/model/DR/fuzz/audit/compat through `.142`–`.148`, externally pentest theft/replay/fence-bypass/reserve-exhaustion at `.149`, then RC and production. |
| Large storage-profile re-costing could partially charge children and then fail before selector activation because no all-or-none campaign owned the complete delta and abort semantics | Strengthen `0.29.0` with `MigrationImportRegistryHistoryBackendStorageCostProfileRecostCampaignV1`: fixed pre-cut child snapshot/membership commitment plus a parent campaign epoch/fence and bounded post-cut allocation/release log, dual-profile admission, bounded complete-delta preflight cursor, one atomic reservation of the entire aggregate into a parent RecostPending bucket, separate active and pending-successor charges, stable forward/inverse transfers, durable lease/cursor/cumulative budget/retry limits/completion reserve and a complete checkpoint before atomic profile/backend selector activation. Explicit owner-authorized resumable abort reverses every amount whose exact accounting evidence is proved; missing or contradictory evidence conservatively retains encumbrance in permanent quarantine rather than inventing a credit. Define every crash and response-loss boundary as exact retry, resume, abort, recoverable fence or quarantine. Preserve all campaign state at `0.30.0`; freeze atomic rows/transfers/order at `0.140.2`, roles at `.3`, evidence at `.5`, HA at `.6`; prove RPC/race/DR/fuzz/audit/compat/pentest through `.142`–`.149`, then RC and production. |
| A complete-domain storage-cost comparison could itself become unbounded by enumerating all ceiling/allocation breakpoints | Strengthen `0.29.0` by admitting only a bounded rounded-affine profile family with maxima for entries, artifact kinds, canonical size, allocation unit, rational numerator/denominator and classifier proof bytes/nodes. `MigrationImportRegistryHistoryBackendStorageCostProfileClassifierWorkBudgetV1` precharges a closed-form symbolic dominance proof over algebraic crossings/residue classes; it never enumerates the full size domain. ValidNonWeakening and ValidWeakening require complete semantic proof; unsupported/unknown functions or artifacts, overflow, exceeded maxima/budget and malformed/unverifiable proofs are InvalidOrUnverifiable terminal no-write without sampled or authority fallback. Golden/property vectors compare the symbolic result with exhaustive small-domain evaluation. Freeze arithmetic/proof/authentication at `0.140.1`, storage bounds at `.2`, classifier/authorizer SoD at `.3`; carry HA/RPC/fuzz/audit/compat through `.142`–`.148`, externally pentest CPU/memory exhaustion and misclassification at `.149`, then RC and production. |
| Parent verification had bounded quanta but no proof that reserved capacity could finish a valid maximum-sized fixed snapshot | Strengthen `0.29.0` with `MigrationImportRegistryHistoryRecoveryCapacityParentVerificationReservationV1`. Checkpoint creation derives checked full-snapshot capacity from authenticated child count/encoded bytes, commitment depth/chunks, maximum decode/hash/proof work per entry, bounded adapter scan overhead and terminal result/audit/outbox/recovery requirements, then atomically encumbers it before the checkpoint becomes a restorable head. Insufficient or unbounded capacity leaves the predecessor authoritative. Restore may stream smaller quanta but consumes the one non-recreatable complete reservation. Preserve at `0.30.0`; freeze derivation/profile/rows at `0.140.2`, verifier/allocator separation at `.3`, retained evidence at `.5`, HA at `.6`; prove maximum snapshots, failover, DR, exhaustion, compatibility and external pentest through `.142`–`.149`, then RC and production. |
| Mapping invalid or unverifiable storage-cost profiles to Weakening allowed destructive authority to override accounting validity | Correct `0.29.0` to make `MigrationImportRegistryHistoryBackendStorageCostProfileSuccessorClassificationV1` a closed ValidNonWeakening/ValidWeakening/InvalidOrUnverifiable result. Only ValidWeakening enters `WeakenBackendStorageCostProfile`; unsupported functions, unknown artifacts, arithmetic overflow, exceeded format/proof/work limits, malformed or unverifiable proof and classifier-budget exhaustion are terminal no-write InvalidOrUnverifiable. Preserve exact outcomes and rejection evidence at `0.30.0`; freeze proof/authentication at `0.140.1`, storage/codec bounds at `.2`, classifier/authorizer separation at `.3`, retention at `.5`, HA at `.6`; fuzz and audit through `.142`–`.148`, externally pentest semantic-bypass and exhaustion at `.149`, then require clean RC and production continuity. |
| A fixed re-cost snapshot could be invalidated indefinitely by live child allocation/release, or live mutation could escape successor charging | At `0.29.0`, add a parent-ledger campaign epoch/fence. Pre-cut children remain in the authenticated snapshot; every post-cut allocation/release transaction locks the active slot then campaign fence, applies active-profile plus pending successor accounting and appends a stable event to a bounded protected post-cut log. A durable fold cursor/cumulative checkpoint processes the log while Open. A close-margin blocks new allocations while a non-borrowable release lane remains available; Open→Finalizing fixes the high-watermark only when its remaining tail fits hard entry/byte/work maxima. Finalization folds only that tail and activation verifies constant-sized roots/counters/versions. Put active slot then campaign fence after the parent ledger in `MigrationImportRegistryHistoryLockRankV1`. Preserve epoch/log/fold/high-watermark at `0.30.0`; freeze transaction/order/bounds at `0.140.2`, roles at `.3`, evidence at `.5`, takeover at `.6`; prove RPC/race/DR/churn/soak/audit/compat/pentest through `.142`–`.149`, then RC and production. |
| Aborted campaigns could retain applied staging deltas and exhaust Recovery capacity without activating a profile | At `0.29.0`, split active cost charge from `MigrationImportRegistryHistoryBackendStorageCostProfilePendingSuccessorChargeV1`. Both encumber the parent, but campaign application creates only profile/campaign-bound pending charges and atomically moves the same amount out of campaign RecostPending. Activation reclassifies the campaign aggregate through one activation record without changing parent total and requires RecostPending zero. Only the explicit campaign owner may authorize `AbortMigrationImportRegistryHistoryBackendStorageCostProfileRecostCampaign`; its stable result/conflict protocol reverses all provable RecostPending and pending-successor amounts with deterministic inverse transfer IDs and atomic parent credits. Retry exhaustion fences and never authorizes abort or permanent retention; genuinely unprovable accounting instead uses the separately authorized permanent-quarantine protocol and cannot mint a refund. Preserve forward/inverse/result state at `0.30.0`; freeze rows at `0.140.2`, roles at `.3`, evidence at `.5`, HA at `.6`; prove response-loss, repeated abort and conservation through `.142`–`.149`, then RC and production. |
| The retirement fence was absent from the shared lock rank and completion-reserve admission could be read as a preparatory transaction | Correct `0.29.0` so the deployment-retirement fence is the first rank for every deployment-local read/mutation, before coordinator, parent-capacity or domain rows. Retirement's one transaction consumes Issued authorization, advances Operational→RetirementPending, creates Pending, reserves complete terminalization capacity and writes audit/outbox or commits none; no preparatory reserve exists. The campaign fence is separately ranked after its parent ledger. Preserve exact order and atomic bundle at `0.30.0`; freeze transactions/order at `0.140.2`, role boundaries at `.3`, evidence at `.5`, HA at `.6`; model-check deadlock/partial-commit at `.143`, restore at `.145`, fuzz at `.146`, audit at `.147`, freeze legacy orders at `.148`, externally pentest at `.149`, then RC and production. |
| RecostPending was absent from the authoritative parent equation even though preflight debited ParentAvailable into that bucket | Correct `0.29.0` to enforce ParentTotal = ParentAvailable + AggregateActiveChildEncumbrance + AggregateCampaignRecostPending + AggregatePendingSuccessorEncumbrance + AggregateWorkspaceEncumbrance. Preflight increases campaign pending by the exact parent debit; each application atomically decreases it and increases pending successor identically; workspace reservation atomically moves ParentAvailable into its exact workspace member; abort and authenticated workspace settlement use stable inverse transfers; activation only reclassifies pending successor. Preserve all four aggregates, exact membership, transfers and checkpoints at `0.30.0`; freeze rows/checked transactions at `0.140.2`, roles at `.3`, evidence at `.5`, HA at `.6`; prove conservation in RPC/model/DR/fuzz/audit/compat/pentest through `.142`–`.149`, then RC and production. |
| One-active-campaign intent lacked a structural parent-owned cardinality guard | At `0.29.0`, add `MigrationImportRegistryHistoryBackendStorageCostProfileActiveRecostCampaignSlotV1` keyed by parent and selector scope. Start atomically CASes None to exact campaign/epoch/profiles/version; exact retry joins and changed material conflicts. The slot remains occupied while Fenced and clears only through `MigrationImportRegistryHistoryBackendStorageCostProfileRecostCampaignTerminalCheckpointV1`, which binds the terminal campaign+Closed fence, final accounting, authority/result, workspace/cleanup, audit/outbox and slot-release identity. Clearing co-commits or later locks and verifies that checkpoint. Preserve at `0.30.0`; freeze uniqueness/CAS/checkpoint/order at `0.140.2`, roles at `.3`, evidence at `.5`, takeover at `.6`, then RPC/race/DR/fuzz/audit/compat/pentest `.142`–`.149`, RC and production. |
| Fenced re-cost campaigns had no authorized, idempotent recovery protocol or legal transition table | At `0.29.0`, freeze total campaign and mutation-fence product states plus durable ResumeTowardActivation/CompleteAbort intent. Recovery under independent authority advances lease/fence generation while preserving cursor/work/fold/reservations: prior Preflighting returns to Preflighting, prior RecostPending/Applying to Applying, and prior Complete to Complete after bounded root revalidation; it never finishes a remaining maximum snapshot in one transaction. Abort closes the fence; Closed never reopens. Missing/contradictory evidence requires the separate one-shot quarantine authority below, not a generic recovery choice. Freeze commands/results/product/prior-state rows at `0.140.2`, SoD at `.3`, evidence at `.5`, takeover at `.6`; prove RPC/model/DR/fuzz/audit/compat/pentest `.142`–`.149`, then RC and production. |
| Final cost-profile activation did not explicitly consume still-current authority in the same transaction as selector/accounting activation | At `0.29.0`, make activation one bundle that locks/rechecks retirement fence, active slot, campaign/profile heads, classifier proof, final checkpoints, workspace and accounting; consumes the still-Issued destructive grant for ValidWeakening or current ordinary-owner result for ValidNonWeakening; moves Proposed→Active and predecessor→Superseded, Complete→Activated and fence→Closed; activates selector, reclassifies accounting, terminal-checkpoints the slot and writes stable result/record/audit/outbox or none. Preserve consumed authority and result at `0.30.0`; freeze preimages at `0.140.1`, transaction at `.2`, SoD at `.3`, evidence at `.5`, HA at `.6`; race expiry/revocation and pentest split activation through `.142`–`.149`, then RC and production. |
| Finalization could be interpreted as replaying an unbounded post-cut overlay inside the activation transaction | At `0.29.0`, add a durable post-cut fold cursor and cumulative authenticated checkpoint while Open. Open→Finalizing fixes a high-watermark only when the unfurled tail fits platform-hard entry/byte/work limits sized for the exclusive interval; Finalizing folds only that tail. Activation verifies constant-sized roots, counters, versions and final checkpoint, never the full children/log set. Preserve fold state at `0.30.0`; freeze bounds/storage at `0.140.2`, roles at `.3`, evidence at `.5`, HA at `.6`; test churn/tail exhaustion/model/DR/fuzz/audit/compat/pentest `.142`–`.149`, then RC and production. |
| Logical steady-state re-costing did not durably reserve schema/index migration peak capacity | At `0.29.0`, require `MigrationImportRegistryHistoryBackendStorageCostProfileMigrationWorkspaceReservationV1` for shadow data/index bytes, WAL/journal amplification, verification, rollback/cleanup, worker/I/O and crash/failover terminalization. Reused staging must implement the exact Reserved→Building→Catchup→Verified→activation/abort cleanup→Cleaned/Quarantined lifecycle, bounded cursor/physical mutation high-watermark, source capacity ledger/aggregate ceiling, typed build/synchronize/verify/cleanup/quarantine/settle commands and exact-once release after authenticated deletion. Workspace is campaign-owned at the existing parent-ledger→slot→campaign-fence ranks. Preserve full lifecycle at `0.30.0`; freeze rows/transactions at `0.140.2`, roles at `.3`, custody at `.5`, failover at `.6`; exercise RPC/model/DR/fuzz/audit/compat/pentest `.142`–`.149`, then RC and production. |
| A pre-cut release during Preflighting could attempt to credit RecostPending before that bucket existed | Correct `0.29.0` with a three-case release matrix. Before reservation, release only active charge, append a stable tombstone and subtract/exclude the child through the bounded preflight fold without successor credit. After reservation but before application, decrease campaign RecostPending and credit the parent. After application or for a post-cut child, decrease pending successor and credit the parent. The fixed checkpoint transaction locks the log/fold and binds one folded-log high-watermark so no release falls between calculation and reservation. Preserve tombstones/cut at `0.30.0`; freeze rows/transactions at `0.140.2`, roles at `.3`, evidence at `.5`, HA at `.6`; race/fault/restore/fuzz/audit/freeze/pentest through `.142`–`.149`, then RC and production. |
| Recovering a preflight-origin Fenced campaign required potentially unbounded remaining snapshot work in one recovery transaction | Correct `0.29.0` so recovery performs constant-sized revalidation, advances lease/fence generation and returns prior Preflighting to Preflighting with its cursor/work/fold/reservations intact. Prior RecostPending/Applying returns to Applying and prior Complete to Complete after checkpoint revalidation; proved abort enters Aborting after fence closure. Preserve prior state/counters at `0.30.0`; freeze rows/CAS/bounds at `0.140.2`, recovery SoD at `.3`, evidence at `.5`, takeover at `.6`; model/fault/DR/fuzz/audit/compat/pentest `.142`–`.149`, then RC and production. |
| Campaign state, mutation-fence state and terminal slot release lacked one complete product/terminal-checkpoint contract | At `0.29.0`, freeze valid campaign+Open/Finalizing/Closed combinations. Fenced preserves Open/Finalizing; Fenced+Open denies allocations but allows only evidence-complete protected releases, Fenced+Finalizing denies mutation, abort/quarantine close, and Closed never reopens. Activation/abort/quarantine share one expected-state/fence-generation/terminal-result CAS. Add the terminal checkpoint binding final accounting, authority/result, workspace/cleanup, audit/outbox and slot release. Preserve at `0.30.0`; freeze rows/CAS at `0.140.2`, roles at `.3`, evidence at `.5`, HA at `.6`; model every combination and pentest terminal forgery through `.143`–`.149`, then RC and production. |
| Migration workspace reservation and permanent quarantine lacked complete operational and authority lifecycles | At `0.29.0`, add the workspace state/cursor/physical high-watermark/source ledger/aggregate/commands/settlement lifecycle above. Add a distinct permanent-quarantine authorization with Absent/RevokedBeforeAdmission/Issued/Consumed/ExpiredUnused/RevokedUnused, exact evidence/retained capacity/workspace/policy/hold/time/quorum/SoD/nonce/idempotency binding, independent issuer/admitter/operator and sole consuming command. Consumption atomically commits Fenced→PermanentlyQuarantined, Closed, successor Rejected, predecessor Active, retained/quarantined capacity, terminal checkpoint, slot release, audit/outbox or none. Preserve at `0.30.0`; freeze auth preimage at `0.140.1`, rows/transactions at `.2`, SoD at `.3`, tombstones/custody at `.5`, HA at `.6`; RPC/model/DR/fuzz/audit/compat/pentest `.142`–`.149`, then RC and production. |
| Workspace had an internally conserved capacity equation but no unambiguous debit in the authoritative Recovery parent equation, permitting parent and workspace reservations to spend the same capacity | Correct `0.29.0` by selecting integrated parent accounting, not a carved-out pool. Add exact workspace membership/aggregate to ParentTotal. Reservation atomically decreases ParentAvailable and creates the exact workspace encumbrance through `MigrationWorkspaceParentTransferV1`; deletion-proved settlement atomically removes it and credits the identical amount through `MigrationWorkspaceParentInverseTransferV1` plus a predecessor-linked settlement checkpoint. Quarantined workspace stays a whole parent member until the broader custody-safe parent release; campaign-level evidence cannot partially refund it. Preserve transfers/membership at `0.30.0`; freeze schema/transactions at `0.140.2`, allocator/worker/settler separation at `.3`, custody at `.5`, HA at `.6`; prove via RPC/model/DR/fuzz/audit/compat/pentest `.142`–`.149`, then RC and production. |
| Physical workspace verification and logical post-cut finalization could each be valid while describing different cuts | Correct `0.29.0` so `MigrationWorkspaceCheckpointV1` binds campaign/epoch, selector/profile digests, fixed snapshot, exact closed post-cut high-watermark/fold root, equal physical high-watermark, source/successor roots/counts, writer fence, reservation/cursor/result and verification profile. Catch-up may advance while Open but Verified requires Complete+Finalizing, closed logical HWM, exact physical catch-up and fenced old writers. Finalizing admission bounds both tails; activation requires checkpoint identity/root equality and rejects independently valid different cuts. Preserve all fields at `0.30.0`; freeze preimage at `0.140.1`, rows/transactions at `.2`, roles at `.3`, evidence at `.5`, HA at `.6`; model, restore, fuzz, audit, compatibility-test and externally pentest through `.143`–`.149`, then RC and production. |
| Terminal workspace cleanup inherited active-slot→campaign-fence locking after the terminal checkpoint had cleared that slot | Correct `0.29.0` so cleanup locks the parent ledger, the current slot only for rank serialization, then the old campaign's stable Closed fence by identity. It may proceed if the slot is None or names a newer campaign, cannot mutate current selector/profile/slot/new campaign, and runs in hard-bounded quanta that release the shared slot. Retain the old fence until `MigrationWorkspaceSettlementCheckpointV1` binds deletion, old terminal checkpoint, reservation/state, parent inverse credit, result, audit/outbox and predecessor linkage. Preserve at `0.30.0`; freeze rows/order/bounds at `0.140.2`, roles at `.3`, custody at `.5`, HA at `.6`; model starvation/stale mutation at `.143`, drill/fuzz/audit/compat/pentest `.145`–`.149`, then RC and production. |
| Permanent-quarantine authorization named revocation states but omitted the remote-to-destination revocation transport and apply protocol | Correct `0.29.0` with issuer-only signed `PermanentQuarantineAuthorizationRevocationIntentV1` and target-scoped non-wrapping sequence. Bind exact authorization digest, campaign/slot/epoch/action, issuer/key continuity, issued/not-before/covering-expiry/not-after, uncertainty/time profile, reason, nonce and idempotency. Only destination `Apply...Revocation` verifies and atomically stores inbox/tombstone/result plus Absent→RevokedBeforeAdmission or Issued→RevokedUnused; exact replay joins, stale/lower/changed/terminal conflicts and a sequence cannot suppress another target. Preserve at `0.30.0`; freeze preimage/sequence at `0.140.1`, rows/transactions at `.2`, issuer/applier SoD at `.3`, tombstone custody at `.5`, HA at `.6`; exercise RPC/model/DR/fuzz/audit/compat/pentest `.142`–`.149`, then RC and production. |

Recommendations that merely restated existing controls remain in their original
versions. Backend-specific and protocol-specific implementations are admitted
at their first consuming milestone, then revalidated and frozen by the
pre-production decisions. Only the selected profiles proceed through Phase O
and appear in `1.0.0` claims.

This review closes architectural planning rather than adding another product
family. The capability scope is now considered sufficient to begin the
implementation ladder. Future plan changes should correct discovered security,
correctness, operability, or feasibility issues; additional pre-`1.0.0`
capabilities require explicit maintainer scope approval.
