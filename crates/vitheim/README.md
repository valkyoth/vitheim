# vitheim

Private, dependency-free facade for Vitheim's deterministic `no_std` core.

This crate is not published to crates.io. Its API is pre-production and may
change throughout the `0.x` release line.

